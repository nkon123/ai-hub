# 구현 전 결정 또는 PoC 가정

결정되지 않은 항목을 Claude Code가 임의 확정하지 않도록 관리한다. `PoC 가정`은 구현을 진행하기 위한 기본값이며 운영 적용 전에 재검토한다.

| ID | 항목 | PoC 가정 | 운영 전 결정 필요 | 영향 모듈 |
|---|---|---|---|---|
| D-001 | Portal 인증 | 개발용 Test Identity Adapter | 사내 SSO/OIDC/SAML 연동 방식 | M01/M02/M11 |
| D-002 | 중앙 DB | PostgreSQL | 운영 DB 표준과 HA | M02/M12 |
| D-003 | 파일 저장소 | 로컬 File System Adapter | 사내 Object Storage/NAS | M03/M11 |
| D-004 | Portal 배포 | 내부 Container 실행 | 운영 Platform/Kubernetes 여부 | M01/M02/M03 |
| D-005 | Desktop OS | Windows 10/11 x64 | 회사 표준 OS·배포도구 | M04/M12 |
| D-006 | Desktop Runtime 구조 | 별도 Loopback Process 권장 | 단일 Process 허용 여부 | M04/M05 |
| D-007 | Agent Workflow | 선언형 Workflow + Framework Adapter | LangGraph 고정 여부 | M05/M06 |
| D-008 | 실행 코드 Package | PoC 운영 배포 금지 | Sandbox/Code Signing 정책 | M05/M11 |
| D-009 | Embedding 모델 | Office Profile Alias로 제공 | 사업장별 승인 모델 | M06/M07/M08 |
| D-010 | Chat 모델 | Office Profile Alias로 제공 | Frontier/Local 모델 목록 | M05/M06 |
| D-011 | Vector Store | Chroma | 운영 규모에서 다른 Store 전환 여부 | M07/M08/M09 |
| D-012 | 한국어 BM25 | 공백 Tokenizer 기준선 | 형태소 분석기와 사전 | M07/M08/M09 |
| D-013 | Knowledge 품질 기준 | Recall@5 80%, P95 2초 목표 | 업무별 기준 | M09/M11 |
| D-014 | MCP Connector | Mock 또는 승인된 Oracle Read-only | 실제 DB/시스템 범위 | M10/M11 |
| D-015 | MCP 인증 | Mock User Context | 사내 인증 Context 전달 방식 | M05/M10/M11 |
| D-016 | Package Signature | Checksum 필수, Signature Hook만 | PKI/서명키 운영 | M03/M04/M11 |
| D-017 | 폐쇄망 Revocation | Bundle에 Revocation List 포함 | 갱신 주기와 강제 수준 | M03/M04/M11 |
| D-018 | Package 크기 | 정책 설정, 실제 제한은 환경 측정 | Agent/Knowledge/Bundle 최대 크기 | M03/M04 |
| D-019 | Service Portal 테스트 | 실제 Test/Approved Knowledge + Test Model, MCP는 Mock | 운영 Test Environment와 데이터 분리 수준 | M01/M02/M05/M08/M12 |
| D-020 | Service Composer | 단계형 Wizard | 향후 제한형 시각 편집 필요 여부 | M01/M02/M06 |
| D-021 | 자산 명칭 | 사용자/신규 코드에 Knowledge 사용 | 기존 시스템 용어 매핑 | 전 모듈 |
| D-022 | 조직·사업장 권한 | Role+Organization+Site | 실제 HR/권한 시스템 연동 | M02/M08/M10/M11 |
| D-023 | Audit 보관 | 설정 가능한 정책으로 구현 | 법무·보안 보관기간 | M02/M05/M10/M11 |
| D-024 | 진단 로그 | Sanitized Local Bundle | 중앙 수집 허용 여부 | M04/M05/M11 |
| D-025 | Source 원본 배포 | Manifest·Parsed·Chunk 필수, 원본 선택 | 저작권·보안 기준 | M07/M09/M11 |
| D-026 | Hosted Chat Base URL | `https://ai.company.local/chat/{slug}` 형태의 내부 URL | 실제 Domain·Reverse Proxy·TLS | M01/M02/M03/M11 |
| D-027 | Hosted Chat 인증 | 사내 Test Identity, 비민감 Demo에 만료 Token 허용 | SSO/OIDC와 Demo 정책 | M01/M02/M11 |
| D-028 | Hosted Runtime 배치 | Agent Runtime Core의 중앙 Server Mode | Container 수평확장·GPU/Model Gateway | M03/M05/M12 |
| D-029 | Chat 대화 보관 | 기본 Audit에 질문·답변 원문 미저장 | 업무별 보관기간·피드백 학습 사용 | M02/M05/M11 |
| D-030 | Desktop UI 기술 | **DECIDED: Electron.js** (2026-08-02 확정). **패키징 방식도 DECIDED (2026-08-04)**: electron-builder + NSIS, Windows x64, Per-machine 설치(`apps/desktop-client/electron-builder.yml`, 상세는 `11-desktop-packaging-and-distribution.md`) | Code Signing은 여전히 미결정 — 회사 인증서 자체가 아직 없음(D-048로 분리) | M04/M05/M12 |
| D-031 | Python 패키지 관리 | **DECIDED: uv** (2026-08-02) | 폐쇄망 캐시 서버 설정 방식 | 전 Python 모듈 |
| D-032 | Portal API Phase 0 DB | **DECIDED: SQLite(Phase 0) → PostgreSQL(Phase 1+)** | 운영 PostgreSQL HA 구성 | M02/M12 |
| D-033 | Portal Web 디자인 시스템 | **DECIDED: Tailwind CSS v4 + `@tailwindcss/postcss`** (2026-08-03). 이유: 기존 인라인 style/emoji 위주 UI를 일관된 색상·간격 토큰과 재사용 컴포넌트(`app/_components/ui.tsx`)로 대체. 폐쇄망 설치: `pnpm-lock.yaml`에 `tailwindcss`, `@tailwindcss/postcss`, `postcss`, `autoprefixer` tarball 해시가 기록되므로 사내 npm 미러/verdaccio에 동일 버전을 사전 캐싱 후 `pnpm install --offline`으로 설치 | 사내 npm 미러 캐시 정책, 디자인 토큰 확정 시 `@theme` 값 재검토 | M01 |
| D-034 | Registry 생략 | Run 요청에 knowledge_id/question을 직접 포함, agent-runtime이 표준 Agent/Prompt/Office Profile 로컬 사본을 로드해 실행 (service_id는 형식만 검증, 조회 없음). **UPDATE (2026-08-09, 부분 해소)**: 아래 참고 — Agent/Prompt Registry 등록·조회는 구현되었고, Service Registry(service_id 실제 조회)와 Office Profile/Indexing Profile Registry는 여전히 미구현이다 | 남은 항목은 아래 "남은 것" 참고 | M01/M02/M05 |
| D-034 (계속) | 위 항목의 2026-08-09 해소분 — P04/P05 화면과 agent-runtime Registry 조회 | **DECIDED/부분 해소 (2026-08-09)**: 이 결정이 21곳에서 인용되던 최대 구조적 공백이었다 — 이번 세션에서 그 중 "Agent/Prompt를 Portal에 등록하고 agent-runtime이 실제로 조회해 실행한다"는 핵심 절반을 해소했다. **1) M02 Portal API(`apps/portal-api/src/portal_api/routers/assets.py`)**: `POST /api/v1/assets`는 이미 generic이었다(모든 자산 type을 동일하게 저장) — 여기에 등록 시점 Manifest Schema 검증을 신규 추가했다(`ai_asset_schemas.validate` 호출, 실패 시 `400 VALIDATION_ERROR` + `details.errors`에 필드별 오류 목록). 이전에는 AGENT/PROMPT/MCP_TOOL은 물론 KNOWLEDGE도 등록 시점에 스키마 검증이 전혀 없었다(사후 `POST .../versions/{id}/validate`만 있었고 DRAFT 생성 자체는 막지 않았다). 신규 `POST /api/v1/manifests/validate`(Dry-run, 아무것도 저장하지 않음)를 P05 Wizard의 제출 전 검증 단계용으로 추가했다. 07-data-api-contracts.md §3.2가 필수 API로 명시했지만 실제로는 존재하지 않았던 `GET /api/v1/asset-versions/{version_id}`(버전·Manifest·상태 단건 조회)를 새로 구현했다 — agent-runtime의 Registry 조회가 이 endpoint의 첫 실사용처다. Prompt 전용 `GET /api/v1/asset-versions/{version_id}/template`(template 파일 원문 조회, `type=="prompt"`만 허용, 범용 파일 서버가 아님)도 추가했다. **부수 발견**: 이 두 GET 추가 과정에서 `main.py`의 `include_router` 순서가 `assets_router`를 `reviews_router`보다 먼저 등록하고 있어, 신규 `GET /asset-versions/{version_id}`(단일 세그먼트 catch-all)가 기존 `GET /asset-versions/lifecycle`(P16, reviews.py의 리터럴 경로)을 가려버리는 라우팅 충돌을 발견·수정했다(`reviews_router`를 먼저 등록하도록 순서 변경, 회귀 테스트 `test_asset_versions_lifecycle_route_not_shadowed` 추가). **2) M05 agent-runtime(`services/agent-runtime/src/agent_runtime/manifests.py`)**: 이 파일이 D-034의 실제 워크어라운드였다("표준 Agent/Prompt 로컬 사본만 로드") — 이제 명시적인 3단계 해석 순서를 갖는다(모듈 docstring에 그대로 기술): **① `agent_profile ∈ {"standard-agent","standard-db-agent"}`**(기존과 동일, `services/agent-runtime/config/`의 로컬 사본만 사용, Registry를 절대 호출하지 않음 — 4개 게시 챗봇의 무정지 보장 근거) **② `input.registry_agent_version_id`+`registry_prompt_version_id`가 함께 제공된 경우**(`resolve_registry_agent_config`가 신규 `agent_runtime.adapters.registry.HttpAssetRegistryResolver`로 위 두 신규 portal-api endpoint를 호출, 두 버전 모두 `status=="APPROVED"`가 아니면 `AGENT_VERSION_NOT_APPROVED`/`PROMPT_VERSION_NOT_APPROVED`로 명확히 거부 — D-044가 Knowledge에 적용한 것과 동일한 승인 게이트를 Agent/Prompt에도 적용, `ai_asset_schemas.validate`로 Manifest 스키마도 재검증, 성공 시 `_registry_cache`에 영구 캐시) **③ 아무것도 지정하지 않으면 ①의 `standard-agent`로 기본값**. 인증은 D-001 Test Identity Adapter의 고정 dev 토큰(`AgentRuntimeSettings.portal_api_token`, 기본 `dev-user-token`)을 사용한다 — 아직 서비스 자격증명 개념이 없는 PoC 상태의 명시적 타협. **라이브 검증으로 발견·수정한 실제 버그**: 최초 구현은 `AssetRegistryResolver.get_asset_version`이 반환하는 dict에 최상위 `type`/`name` 필드가 있다고 가정했으나, 실제 portal-api의 `AssetVersionOut`에는 그런 필드가 없고 `type`은 오직 `manifest.type` 안에만 있다 — Fake Resolver를 쓰는 단위/통합 테스트는 이 가정을 그대로 fixture에 반영해 통과했지만, 실제 `:8003` portal-api 인스턴스로 라이브 검증하자 모든 Registry 조회가 `AGENT_ASSET_WRONG_TYPE`으로 잘못 거부되는 것을 발견했다. `_fetch_approved_version`이 `manifest.type`을 읽도록 수정하고, 테스트 fixture에서 존재하지 않던 최상위 `type`/`name` 필드를 제거해 실제 API 응답 모양과 정확히 일치시켰다(라이브 검증이 아니었다면 이 버그는 계약대로 보이는 Fake 위에서 영원히 발견되지 못했을 것). **3) M01 Portal UI**: P04(`/assets/new`, 자산 유형 선택)와 P05(`/assets/new/[type]`, Agent/Prompt/MCP Tool 등록 Wizard)를 신규 구현 — 상세는 D-066 UPDATE 참고. **검증(라이브, `:8003`/`:8102`)**: 실제 portal-api에 AGENT 자산을 등록 → 스키마 위반 Manifest가 필드별 오류와 함께 거부됨을 확인 → AGENT+PROMPT를 TECHNICAL→SECURITY→RELEASE 3단계 검토 체인을 통해 실제로 APPROVED로 승격 → agent-runtime이 이 둘을 Registry에서 조회해 정상적으로 Run을 수행함(Knowledge 검색 결과 0건이라 `INSUFFICIENT_EVIDENCE`로 종료 — Registry 해석 자체는 성공, D-036 Hallucination Guard가 정상 동작한 것) → portal-api(`:8003`) 프로세스를 강제 종료해 Registry 완전 장애 상태를 만든 뒤 (a) `agent_profile` 미지정 기본 경로와 `standard-db-agent` 명시 경로 모두 정상 완료됨(Registry가 전혀 호출되지 않음을 resolver call count로 확인) (b) 이미 한 번 성공한 동일 registry id 쌍의 **재실행**도 캐시 덕분에 정상 완료됨 (c) **새로운** registry id 조합만 `ASSET_REGISTRY_UNAVAILABLE`로 깔끔하게 거부됨(크래시 없음)을 모두 실측 확인. 테스트로 만든 AGENT/PROMPT 자산·검토 행 전부 정리 완료(`assets`/`service_deployments`/`distribution_requests`/`service_versions` 개수 확인은 진행 노트 참고). **남은 것**: (i) **Service Registry는 여전히 없다** — `service_id`는 여전히 형식만 검증되고 실제 ServiceDefinition/ServiceVersion 조회로 이어지지 않는다(`workflow.py`의 `_derive_service_uuid` 결정적 파생도 그대로 유지). Service Composer(`/services/new`)가 구성하는 Agent/Knowledge/Prompt 바인딩과 위 신규 registry_agent_version_id/registry_prompt_version_id 경로는 아직 서로 연결되어 있지 않다 — Service Definition에 등록된 Agent/Prompt가 자동으로 이 경로를 타지 않는다(연결하려면 별도 PR로 Service Composer가 이 두 필드를 채워 `POST /local/v1/runs`에 실어 보내야 한다). (ii) D-044(Bundle Builder가 Agent/Prompt를 `standard-agent`/`standard-prompt` 정적 사본으로 대체)는 미변경 — 그 대체 로직을 실제 Registry 조회로 전환하는 것은 범위 밖으로 남긴다. (iii) Office Profile/Indexing Profile Registry도 여전히 없다(각각 D-034 계열의 별도 공백으로 문서화되어 있음, 위 §office_profile 로딩은 그대로 로컬 정적 파일). (iv) `services/agent-runtime/config`의 로컬 사본을 완전히 제거하는 것은 목표가 아니다 — 4개 게시 챗봇을 위한 영구적인 Registry-독립 Fallback으로 의도적으로 유지한다 | (i)~(iii) 각각 별도 PR로 진행. Service Composer가 Registry 기반 Agent/Prompt를 선택할 수 있게 되면 그 선택을 `registry_agent_version_id`/`registry_prompt_version_id`로 Run 요청에 실어 보내는 다음 단계로 자연스럽게 이어진다 | M01/M02/M05 |
| D-035 | Agent Runtime 무인증/CORS | search-runtime/indexing-runtime과 동일하게 인증 없음, CORS는 portal-api와 동일한 allow_origins=[http://localhost:3000] 설정으로 브라우저 EventSource 직접 구독 허용 | Hosted 모드 인증·Origin 제한 정책 | M01/M05/M11 |
| D-036 | 근거 없음(0건 Citation) 처리 | RunResponse.status에 INSUFFICIENT_EVIDENCE 추가, Citation 0건이면 LLM 미호출 후 즉시 종료 | 부분 근거/낮은 신뢰도 케이스의 세분화된 상태 필요 여부 | M05/M06 |
| D-037 | 게시(Publish) Job 수행 주체 | 명세상 M03(Distribution) 책임이나, Hosted 게시는 파일 패키징 없이 Snapshot 조립 + 상태 전이뿐이므로 M02 portal-api에서 동기 처리하고 계약(`PublishJobResponse{job_id, deployment_url}`)은 Job 형태를 유지 | Offline Bundle 구현 시 M03으로 이관, 비동기 Job·진행률·재시도(HOST-012) 도입 | M02/M03 |
| D-038 | Hosted Chat Slug 해석 경로 | agent-runtime이 portal-api의 무인증 서버간 엔드포인트 `GET /api/v1/deployments/by-slug/{slug}`를 호출해 배포·Knowledge를 해석. 미존재/비활성 모두 동일한 404 응답으로 Slug 존재 여부 미노출(명세 §9) | 서비스간 인증(mTLS/내부 토큰), 캐싱·무효화 전략 | M02/M05/M11 |
| D-039 | 게시 Gate 범위 | **UPDATED (2026-08-03)**: Service Definition Schema 검증 + Knowledge 참조 해석 + 인덱싱 COMPLETED + **Knowledge AssetVersion이 APPROVED 상태**(신규, `CHATBOT_KNOWLEDGE_NOT_PUBLISHABLE`) + Model Alias 존재 + Preview 1회 이상 완료(UI Gate). ServiceVersion 자체의 검토·승인 상태 전이는 여전히 미적용(AssetVersion만 M11 검토 Workflow 적용) | 명세 §6.5의 Test Case 3종·Citation 정책 Gate, ServiceVersion 자체의 검토/승인 워크플로 연동 | M01/M02/M11 |
| D-040 | 게시 권한(RBAC) | **UPDATED (2026-08-03)**: `POST /deployments/{id}/publish`는 이제 `packages/security-policy`의 `DEPLOYMENT_PUBLISH` 권한(RELEASE_MANAGER/ADMIN만 허용 — CREATOR 자가 게시 제거)을 사용한다. `assets.py`/`services.py`의 주요 Mutating 엔드포인트(자산 생성·조회, 검토 제출·목록·상세·결정, 중단, 폐기, Service/Deployment 생성)에도 M11 정책 기반 권한 검사를 적용했다. 미적용: PATCH `/service-versions/{id}`, `/service-versions/{id}/resolve`, `/mock-test`, `/preview-sessions`(엔드포인트 자체가 아직 Stub) | 승인자와 게시자 분리(SoD)는 역할로만 구분되고 Workflow로 강제되지 않음; 나머지 Service Composer 엔드포인트 구현 시 권한 매핑 필요 | M02/M11 |
| D-041 | AssetVersion 검토 Workflow 범위 | **DECIDED (2026-08-03)**: `docs/implementation-spec/07-data-api-contracts.md` §3.6/§4.1에 따라 TECHNICAL→SECURITY→RELEASE 순차 검토 체인을 `packages/security-policy`(순수 정책)+`apps/portal-api`(`routers/reviews.py`)로 구현했다. `submit`은 READY_FOR_REVIEW를 거치지 않고 DRAFT/CHANGES_REQUESTED에서 바로 IN_REVIEW로 전이한다(READY_FOR_REVIEW는 향후 명시적 "제출 준비" 단계를 위해 상태 자체는 남겨둠). ReviewRequest에 `assigned_to` 자동 배정 로직은 없음(항상 NULL, 역할 보유자 누구나 결정 가능). **후속 항목 "ServiceVersion 자체 검토 체인"은 2026-08-08 구현 완료 — D-063 참고** | ReviewRequest 자동 배정, 동시성 제어(현재 "PENDING 아닌 검토 재결정 차단"만 존재, ReviewRequest에 `revision`/`etag`은 없음) | M01/M02/M11 |
| D-042 | 챗봇 Quick Create 게시 흐름의 역할 분리 | Portal Web의 개발용 역할 전환(`app/_components/role-context.tsx`)이 단일 토큰으로 `POST /services`(SERVICE_CREATE) → `POST /deployments`(DEPLOYMENT_CREATE) → `POST /deployments/{id}/publish`(DEPLOYMENT_PUBLISH)를 한 번의 "게시하기" 클릭에서 순차 호출한다. D-040에 따라 SERVICE_CREATE/DEPLOYMENT_CREATE는 CREATOR/ADMIN만, DEPLOYMENT_PUBLISH는 RELEASE_MANAGER/ADMIN만 보유하므로 **RELEASE_MANAGER 역할로는 이 한 번의 클릭을 끝까지 완료할 수 없다**(1~2단계에서 403). PoC 가정: `StepPublish.tsx`는 클라이언트에서 DEPLOYMENT_PUBLISH 보유 역할(RELEASE_MANAGER/ADMIN)일 때만 버튼을 활성화해 게시 단계의 403은 막았지만, Service/Deployment 생성 단계의 403 가능성은 서버 오류 메시지를 그대로 노출하는 기존 일반 오류 배너에 맡겼다(발표 데모에서는 ADMIN 역할로 전 과정을 시연) | Quick Create를 "구성 저장(CREATOR)"과 "게시(RELEASE_MANAGER)" 두 단계로 분리하거나, Service/Deployment 생성에도 DEPLOYMENT_PUBLISH 보유자를 허용할지 결정 | M01/M02/M11 |
| D-043 | Portal API DB 마이그레이션 도구 | **DECIDED (2026-08-03)**: `apps/portal-api`에 Alembic 도입(명세 §11). 이유: `init_db()`의 `create_all`은 누락된 테이블만 만들고 기존 테이블은 ALTER하지 않아, 모델에 컬럼을 추가할 때마다(`asset_versions.approved_at/deprecated_at`, `service_deployments.suspended_by/suspended_at/suspend_reason`) 운영 중인 `portal.db`가 "no such column"으로 응답 불능이 되고 수동 `ALTER TABLE`로 복구해야 했다(총 3회 반복, progress-log.md 2026-08-03 항목 참고). `apps/portal-api/migrations/`(async 템플릿, `env.py`가 `portal_api.config.settings.database_url`과 `Base.metadata` 전체를 사용)에 현재 스키마와 동일한 단일 baseline revision(`dfc042a00019`)을 생성하고, 기존 `portal.db`는 `alembic stamp head`로 표시(데이터 무손실 확인, 백업은 `portal.db.pre-alembic.bak`). 앱 시작(`init_db()`)은 더 이상 `create_all`을 호출하지 않고 스키마를 Alembic 전용으로 소유하며, 테스트(`tests/integration/portal_api/conftest.py`)는 별도 in-memory 엔진에서 계속 `create_all` 사용. 절차: 모델 변경 → `make migration name=...` → 생성된 파일 검토 → `make migrate`. 폐쇄망 설치: `uv.lock`에 `alembic==1.18.5` 해시가 이미 기록되어 있으므로(의존성 자체는 이전에 `pyproject.toml`에 추가됨) 사내 PyPI 미러에 동일 버전을 사전 캐싱 후 `uv sync --offline`으로 설치 | 향후 PostgreSQL(D-032 Phase 1) 전환 시 SQLite 전용 방언 차이(예: 컬럼 삭제/타입 변경 시 batch mode) 재검토 | M02/M12 |

| D-044 | Offline Bundle의 SERVICE_VERSION 승인 게이트 | **DECIDED (2026-08-03)**: `POST /api/v1/distributions`가 요구하는 "승인된 버전만 배포"(§3.3)를 SERVICE_VERSION Root에 적용할 방법이 없다 — D-041에 따라 ServiceVersion 자체의 검토/승인 Workflow가 아직 구현되지 않아 `status`는 항상 `DRAFT`에 머문다. PoC 가정: "승인됨"을 "해당 ServiceVersion으로 게시(Publish)된 DeploymentRevision이 최소 1개 존재함"으로 조작적으로 정의한다. 그 Revision의 `resolved_dependency_snapshot`(게시 시점에 고정된 Knowledge Version/Index 경로)을 Bundle의 유일한 진실 공급원으로 사용하고, distribution-service는 이를 재해석하지 않는다(§3.3 "최신 버전을 무조건 자동 선택하지 않는다"). 미게시 ServiceVersion은 `VALIDATION_ERROR`로 거부한다. 같은 이유로 Service Definition의 `agent_ref`/`prompt_bindings`가 가리키는 Agent/Prompt는 Registry에 등록되어 있지 않으므로(D-034), Bundle Builder는 이를 `services/agent-runtime/config/standard-agent`, `standard-prompt`의 정적 사본으로 대체 포함한다(파일을 직접 복사하되 `agent_runtime` Python 패키지는 Import하지 않음) | ServiceVersion 자체의 검토/승인 Workflow 구현 후 재검토(D-041), Agent/Prompt Registry 등록 구현 시 Bundle Builder를 실제 Registry 조회로 전환 | M02/M03 |

| D-045 | Knowledge 평가(M09)의 문서 매칭·Quality Gate 범위 | **DECIDED (2026-08-03)**: (1) **문서 id 매칭 규칙**: search-runtime의 Citation(`services/search-runtime/src/search_runtime/hybrid.py`)은 §3.12가 요구하는 `document_id`가 아니라 `document_path`/`document_title`만 반환한다(M08 PoC가 §3.12보다 먼저 구현됨). `packages/evaluation-runner`는 이를 재현 가능하게 다시 정의하지 않고, `document_path`(없으면 `document_title`)의 파일명 stem(확장자 제거, 소문자)을 문서 id로 조작적으로 사용한다(`evaluation_runner.matching`). Evaluation Dataset의 `expected_document_ids`/`forbidden_document_ids`도 동일 규칙(예: `remote-work-policy.md` → `remote-work-policy`)으로 작성해야 한다. (2) **Quality Gate 범위**: §4.7의 6개 기준 중 수치화 가능한 3개(Recall@5 ≥80%, 이전 승인 버전 대비 Recall@5 5%p 이상 하락 금지, P95 ≤2초)와 Evaluation Dataset의 `forbidden_document_ids`로 계산 가능한 금지 문서 오검색 비율(신규, 기준 0%)만 `packages/evaluation-runner/config/quality-gate.yaml`(YAML, 코드에 하드코딩하지 않음)로 관리한다. "모든 ACL Test 통과"와 "Package Smoke Test 통과"는 ACL 케이스 데이터셋과 `packages/knowledge-packager`가 아직 없어 범위 밖이다. (3) **평가 데이터셋 검토 상태**: `evaluation-dataset.schema.json`에 `review_status`(`AI_GENERATED_UNREVIEWED`/`EXPERT_REVIEWED`) 필드를 추가해 §4.3 "생성형 AI가 만든 질문은 사람 검토 없이 기준 데이터로 사용하지 않는다"를 스키마 수준에서 표시한다. `fixtures/valid/hr-policy-knowledge/evaluation-dataset.json`는 Claude Code가 초안 작성했으므로 `AI_GENERATED_UNREVIEWED`로 표시했다 — CLI(`evaluate-knowledge run`)는 이 상태일 때 stderr 경고를 출력하고 Data Card에도 동일 주의 문구를 삽입한다. 실제 검증: 시딩된 재택근무 정책 Knowledge(`d9e660b7-ca76-4f46-899e-2e1621bac139`)를 살아있는 search-runtime(:8300)에 대해 실행한 결과 Recall@5 100%였으나, "범위 밖 질문" 케이스(WFH-006)에서도 유일한 문서가 항상 반환되어 forbidden_hit_rate_max 게이트가 실패했다 — 이는 search-runtime에 관련성 없음/결과 없음 임계값이 없다는 실제 품질 이슈를 드러낸 것이며 evaluation-runner의 결함이 아니다 | search-runtime에 §3.12 `document_id` 필드 추가 시 매칭 규칙을 실제 id 비교로 전환, ACL 평가 데이터셋과 Package Smoke Test 도입 시 Gate 확장, Evaluation Dataset이 실제 업무 전문가 검토를 거쳐 `EXPERT_REVIEWED`로 전환 | M08/M09/M11 |
| D-046 | 무관한 질문의 근거 차단 층위 | **DECIDED (2026-08-05)**: search-runtime에 D-036 Guard를 결정적으로 만드는 관련도 임계값을 도입했다. **발견 경위(2026-08-03)**: search-runtime은 관련도 임계값이 없어 항상 top-k를 반환하므로, D-036 Hallucination Guard(Citation 0건 시 LLM 미호출)는 인덱스가 비었을 때만 발동하고 주제 밖 질문에서는 발동하지 않았다(`forbidden_hit_rate` 16.7% FAIL). **실측(재택 근무 정책 Knowledge, `d9e660b7-ca76-4f46-899e-2e1621bac139`, cosine space, `qwen3-embedding:0.6b`, in-scope/out-of-scope 각 6개 질문의 top-1 cosine similarity)**:<br>&nbsp;&nbsp;- Query를 원문 그대로 임베딩(기존 동작): in-scope 0.362~0.661, out-of-scope 0.195~0.396 → **분포가 겹침(separation −0.034)**. 단순 임계값만으로는 작동 불가 — in-scope 질문 "장비 지원은 무엇이 있나요?"(0.362)가 out-of-scope 질문 3개보다도 낮게 나와, 무관한 질문을 막는 임계값은 이 정상 질문도 함께 막는다.<br>&nbsp;&nbsp;- Query에 Qwen3 instruct prefix(`Instruct: Given a web search query, retrieve relevant passages that answer the query\nQuery: `) 부여: in-scope 0.446~0.709, out-of-scope 0.196~0.391 → **깨끗하게 분리(separation +0.056)**. **prefix 부여가 임계값을 가능케 한 핵심 조치** — Qwen3-Embedding은 Query 쪽에만 instruction을 붙이고 문서 쪽은 원문 그대로 두는 비대칭 검색을 위해 설계되었으므로, indexing-runtime의 문서 임베딩은 그대로 두고(재인덱싱 불필요, 기존 인덱스 4개·게시된 챗봇 4개 영향 없음) search-runtime의 Query 임베딩에만 prefix를 적용했다.<br>**기본 임계값**: `min_relevance_score` 기본값 **0.42** — 위 실측의 분리 구간(out-max 0.391, in-min 0.446) 중간값. `packages/schemas/profiles/retrieval-profile.schema.json`에 `min_relevance_score`(0=필터링 비활성, 하위 호환)와 `query_instruct_prefix`로 계약화했고, `services/search-runtime/src/search_runtime/settings.py`가 정책 기본값을 코드에 하드코딩하지 않고 env(`SEARCH_MIN_RELEVANCE_SCORE`/`SEARCH_QUERY_INSTRUCT_PREFIX`)로 override 가능하게 관리한다(요청별 override는 `SearchRequest.min_relevance_score`/`query_instruct_prefix`, `main.py`의 기존 `alpha`/`top_k`와 동일 패턴). **BM25-only Chunk 규칙**: 필터링이 켜져 있을 때(`min_relevance_score > 0`) BM25로만 찾고 벡터 매치가 없는 Chunk는 **무조건 drop**한다 — 그 Chunk가 관련도 기준을 통과했다는 cosine similarity 근거 자체가 없고, 이를 유지하면 키워드만 일치하는 무관한 Chunk가 "결정적 차단"이라는 목적을 우회할 수 있기 때문이다(`services/search-runtime/src/search_runtime/hybrid.py`). 필터링은 RRF 융합 이후 top_k 후보에 대해서만 cosine similarity(`1 - Chroma distance`)로 판정하며, RRF 융합 점수(`1/(k+rank)`) 자체는 순위 기반이라 절대적 의미가 없으므로 필터 기준으로 사용하지 않는다. **응답 계약(Additive)**: `SearchResult`에 `min_relevance_score_applied`(실제 적용된 임계값), 각 Citation에 `similarity`(cosine similarity, BM25-only이며 필터링 비활성 시 `null`)를 추가했다 — 기존 필드는 변경 없음, `agent_runtime.adapters.search.HttpKnowledgeAdapter`와 `evaluation_runner.search_client.SearchResponse.from_dict`는 알려진 키만 읽으므로 영향 없음을 확인했다. 단위 테스트 8개(`tests/unit/search_runtime/`, Ollama/Chroma 미사용, BM25/Chroma/embed_query 전부 Fake)로 전량 미달 시 0건, 혼합 시 임계값 이상만 유지, threshold=0 하위 호환, BM25-only 규칙, prefix 실제 적용을 검증 | 실제 search-runtime(:8300) 재기동 후 평가 러너로 `forbidden_hit_rate` 0%·`Recall@5` 100% 유지 확인은 세션 소유자가 직접 수행 예정(재기동은 본 작업 범위 밖) | M05/M08/M09 |
| D-047 | Desktop 배포 시 Python Local Agent Runtime/Ollama 동봉 방식 | **발견 (2026-08-04, Desktop 패키징 작업 중)**: `09-functional-requirements-matrix.md`의 M04-F12("Python 런타임 포함 Windows 배포")는 D-030(Electron.js 확정) 이전 문구로 보인다. Electron 자체는 Node.js/Chromium을 내장해 "런타임 동봉"을 이미 충족하지만, D-006이 규정하는 Local Agent Runtime(`services/agent-runtime`)은 여전히 별도 Python 프로세스(D-031 `uv`, `uv run uvicorn agent_runtime.main:app --port 8100`)이고 Ollama(`exaone3.5:7.8b`/`qwen3-embedding:0.6b`)도 별도 실행체다. `apps/desktop-client/electron/connections.ts`가 `127.0.0.1:8100`(Runtime)과 `127.0.0.1:11434`(Ollama)를 독립적으로 Health-check하는 것도 이 둘이 Electron 셸과 무관하게 사전 준비되어야 함을 보여준다. Desktop Installer(electron-builder/NSIS)는 이 둘을 자동으로 설치하지 않는다. PoC 가정: `docs/implementation-spec/11-desktop-packaging-and-distribution.md` §4.2의 반입 순서는 잠정적으로 "선택지 B(Local Agent Runtime/Ollama를 별도 설치 프로그램으로 분리 배포)"를 전제로 서술했으나 이는 문서 작성 편의를 위한 가정일 뿐 확정이 아니다 | 아래 세 선택지 중 확정 필요 — (A) electron-builder `extraResources`로 임베디드 Python+`services/agent-runtime` 의존성을 Desktop Installer에 동봉(Ollama는 이 방식으로 동봉 곤란), (B) Local Agent Runtime/Ollama용 별도 설치 프로그램을 분리 배포, (C) Ollama+모델+Python Runtime이 사전 설치된 Golden Image 전제. 상세 비교는 `11-desktop-packaging-and-distribution.md` §6 | M04/M05/M12 |
| D-048 | Desktop Installer(Windows) 코드 서명 | **발견 (2026-08-04, D-030 패키징 방식 확정과 분리)**: D-030의 패키징 방식(electron-builder+NSIS)은 결정됐지만 서명은 별개로 남는다 — 서명되지 않은 `.exe`는 Windows SmartScreen 경고와 사내 EDR 차단·격리 위험이 있다(참고: 같은 세션에서 macOS 개발 워크스테이션이 서명되지 않은 Electron 런타임을 XProtect가 격리한 사례가 있었다 — 원인은 다르지만 "신뢰 서명 부재 → 보호 계층 차단"이라는 결과 패턴은 동일). 회사의 Authenticode 코드 서명 인증서 자체가 아직 존재하지 않는다. `apps/desktop-client/electron-builder.yml`에는 실제 값 없이 두 서명 경로(`.pfx`+`CSC_LINK`/`CSC_KEY_PASSWORD` 환경변수 자동 인식, 또는 HSM/토큰 기반 `certificateSubjectName`/`certificateSha1`)의 자리만 주석으로 마련해두었다 | 인증서 종류(OV vs EV — EV는 즉시 SmartScreen 평판을 얻지만 HSM/토큰 전용이라 `.pfx` 내보내기 불가, OV는 저렴하지만 평판이 배포량에 따라 서서히 쌓임), 발급 주체·예산·갱신 주기, HSM/토큰 운영 방식과 빌드 파이프라인 접근 통제, 타임스탬프 서버 선정. 상세는 `11-desktop-packaging-and-distribution.md` §3 | M04/M12 |

| D-049 | office-mcp-server(M10) Tool 이름·Context 계약 정합 | **DECIDED (2026-08-05)**: (1) **Tool 이름 Dot 표기**: `05-mcp-security-governance.md` §5는 `db_metadata.get_tables`/`db_metadata.get_columns`/`table_count.query` 세 Tool을 정의하지만 `packages/schemas/manifests/mcp-tool-manifest.schema.json`의 `tool_name` 정규식은 Dot을 허용하지 않았고(기존 `fixtures/valid/mcp-readonly-oracle/mcp-tool-manifest.json`은 `get_table_metadata` 단일 이름), `fixtures/valid/office-profile-default/office-profile.json`의 `allowed_tools`도 `get_table_metadata`/`get_table_count`로 §5와 불일치했다. 정규식을 Segment 단위 Dot Namespacing을 허용하도록 확장(`^[a-zA-Z_][a-zA-Z0-9_]*(\.[a-zA-Z_][a-zA-Z0-9_]*){0,4}$`, 선행 `.`/`/`는 여전히 거부 — `path-traversal` invalid fixture는 첫 글자 검사에서 계속 거부됨)하고, 기존 Manifest Fixture 1건은 §5.1(`db_metadata.get_tables`)에 맞춰 `tool_name`/`input_schema`/`output_schema`를 갱신했다. `db_metadata.get_columns`/`table_count.query`용 별도 Portal Manifest Fixture는 아직 만들지 않았다(Runtime Tool Registry에는 3종 모두 등록됨) — 한 Manifest = 한 Portal 등록 자산이라는 기존 관례상 2건이 더 필요하다. (2) **Request Context ↔ AuditContext 통합**: `packages/schemas/api/mcp-audit-context.schema.json`의 기존 `AuditContext`(`user_id/org/site/role` 평면 구조, `request_id`/`requested_tool` 없음)는 코드 전체 검색 결과 소비자가 전혀 없었으므로(레포 전체 grep 결과 자기 참조뿐), 병행 타입을 새로 추가하지 않고 §3 Request Context 형태(중첩 `user{id, organization_id, site_id, roles[]}`, `request_id`, `requested_tool` 필수)로 제자리 확장했다. `ToolCallRequest.audit_context`가 그대로 이 확장된 형태를 가리키므로 Wire Contract가 하나로 유지된다. `services/office-mcp-server`의 `request_context.py`가 이 최종 형태의 Python 대응 Pydantic Model이다. **(3) Manifest Fixture 2건 추가 (2026-08-15)**: `fixtures/valid/mcp-db-metadata-get-columns/`와 `fixtures/valid/mcp-table-count-query/`를 추가해 §5의 PoC Tool 3종이 전부 Portal 등록 가능한 자산 형태를 갖췄다(`calculator.add` 샘플까지 포함하면 4종). 두 Fixture의 `tool_name`/`server_alias`/`input_schema`/`execution_guards`는 권위 있는 정의인 `services/office-mcp-server`의 `tools_setup.py` 등록값에서 그대로 옮겼다. `table_count.query`는 서버 정책이 `ON_PARAMETER`인데 `mcp-tool-manifest.schema.json`의 `execution_guards`는 boolean 하나뿐이라 그 값을 표현할 수 없다 — Desktop(`electron/mcp-tool-manifest.ts`)이 `true→ALWAYS`/`false→NEVER`로 사상하므로 표현 불가능한 차이를 **엄격한 쪽(`true`→ALWAYS)으로 반올림**했다. `false`로 두면 D-080 등록이 `confirmation_policy_weaker_than_builtin`으로 거절될 뿐 아니라 서버가 실제로 강제하는 것보다 약한 정책을 사용자에게 표시하게 된다(스키마에 `ON_PARAMETER`를 표현할 필드를 추가하는 것은 별도 계약 변경으로 남긴다). 세 정의(office-mcp-server 등록값 / agent-runtime `MCP_TOOL_SPECS` / Portal Manifest Fixture)를 타입 시스템이 전혀 연결하지 않으므로 신규 `tests/contract/test_mcp_tool_fixtures.py`(16 tests)가 드리프트를 막는다 — 등록된 모든 Tool에 Fixture가 존재하는지, `tool_name` 중복이 없는지, 필수 필드·허용 property 이름·`additionalProperties` 거부 여부가 일치하는지, `input_schema`에 신원 필드가 없는지, 선언된 확인 정책이 서버 정책보다 약하지 않은지를 검사한다(정확한 `input_schema` 동일성은 의도적으로 검사하지 않는다 — Manifest는 사람이 읽는 카탈로그 문서라 설명 문구를 더 갖는다). | 두 신규 Fixture를 **실제 Portal DB에 등록·승인해 카탈로그에 노출시키는 것은 아직 하지 않았다**(`calculator.add`가 2026-08-14에 받은 종단 검증에 해당하는 절차) — 이번 작업 시점에 portal-api/office-mcp-server가 기동돼 있지 않았고 라이브 데모 DB를 임의로 변경하지 않았다. `services/agent-runtime/config/office-profile-default/office-profile.json`의 구 Tool 이름·Port 문제는 이후 세션에서 해소됨(현재 `allowed_tools`는 §5의 4종, endpoint `8500`). | M06/M10/M11 |
| D-050 | Portal Web 웹폰트(Pretendard/Noto Sans KR) 반입 방식 | **PoC 가정 (2026-08-05, 디자인 시스템 Stage 1)**: `ui_style_guide.md` §6.1은 Pretendard/Noto Sans KR을 지정하지만, 공개 CDN에서 webfont를 내려받는 것은 금지된다(폐쇄망 배포 대상, 이번 세션 중 서명되지 않은 바이너리가 macOS Gatekeeper에 격리된 사례 참고 — 출처 미검증 다운로드는 회사 PC에서 위험). `app/globals.css`의 `--font-sans`는 `Pretendard, "Pretendard Variable", "Noto Sans KR", Inter, -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`로 설정해, 해당 폰트가 OS 또는 내부 미러 `@font-face`로 제공되면 사용하고 없으면 시스템 한글 폰트로 안전하게 대체된다. 실제 Pretendard 노출을 위해서는 사내 npm/asset 미러에서 Pretendard Variable woff2를 내려받아 `public/fonts/`에 자체 호스팅하고 `@font-face`를 추가해야 한다(미실행) | 사내 미러 경로 확정, 라이선스 확인(Pretendard는 OFL), `@font-face` 추가 후 실측 폰트 로딩 검증 | M01 |
| D-051 | Desktop Client 디자인 토큰의 Portal Web과의 중복 정의 | **PoC 가정 (2026-08-06, 디자인 시스템 Stage 3)**: `apps/desktop-client`는 `apps/portal-web`과 별개 패키지이며 모듈 간 내부 폴더 직접 Import를 금지하는 CLAUDE.md 구현 원칙 2에 따라 `apps/portal-web/app/globals.css`를 Import할 수 없다. 이번 Stage에서 Desktop Client(`apps/desktop-client/src/styles.css`, `src/ui.tsx`)를 `ui_style_guide.md`에 맞춰 재구성하면서, 브랜드/중립/상태/자산 Accent 컬러 값과 Card/Typography 토큰(`--radius-card`, `--shadow-card`, `--text-page-title` 등)을 `app/globals.css`에서 **값 그대로 복사**해 Desktop 전용 `@theme` 블록에 다시 선언했다(이전에는 Desktop이 자체 Sky-blue 계열의 별도 Brand 색상을 쓰고 있어 두 화면이 다른 제품처럼 보였다). 두 파일의 토큰 값이 향후 어긋나도(예: Portal에서 `--color-brand-500`을 바꾸고 Desktop 쪽 갱신을 잊는 경우) 빌드 타임에 감지할 방법이 없다 | 공용 `packages/design-tokens`(또는 `packages/schemas`에 준하는 공개 계약) 패키지를 만들어 두 앱이 동일한 토큰 원본을 참조하도록 전환 — 현재는 계약이 아닌 값 복사이므로 드리프트를 막는 장치가 없음 | M01/M04 |
| D-052 | M05 agent-runtime → M10 office-mcp-server 연동 (README §15 인수 시나리오 11단계) | **DECIDED (2026-08-06)**: `services/agent-runtime`에 `MCP_TOOL_CALL` Workflow 단계를 추가해 M10을 실제로 호출하는 첫 소비자를 만들었다. 다섯 가지 하위 결정: (1) **ANALYZE(Tool 선택) 방식**: 02-desktop-and-agent-runtime.md §5.2 "Tool Calling이 약한 로컬 모델은 Runtime의 명시적 Workflow와 Schema 기반 호출을 사용한다"에 따라 모델 판단이 아닌 **명시적 필드** 방식을 택했다 — `POST /local/v1/runs`의 `input.mcp_tool`/`input.mcp_tool_input`/`input.mcp_confirmed`를 호출자(궁극적으로 Desktop UI)가 직접 채운다. 자유형 모델 출력을 Tool 호출로 파싱하는 코드는 전혀 없다. 이 값은 네트워크 호출 전에 Office Profile 허용목록(`mcp_tools.resolve_allowed_alias`)과 로컬 정적 JSON Schema 사본(`mcp_tools.validate_tool_input`, jsonschema Draft202012)으로 검증되며, 실 LLM/MCP Server 없이 단위 테스트 가능하다(`tests/integration/agent_runtime/test_mcp.py`). (2) **Tool Schema 소스**: office-mcp-server 내부 모듈을 직접 Import하지 않는다는 CLAUDE.md 구현 원칙 2와, 이 PoC에 별도 Tool Registry 조회 수단이 없다는 점(D-034와 동일한 이유) 때문에, `services/agent-runtime/src/agent_runtime/mcp_tools.py`가 `tools_setup.py`의 3개 PoC Tool `input_schema`/§8.4 확인 정책을 **손으로 복사**해 보관한다 — M10이 Tool을 추가/변경하면 이 사본도 손으로 갱신해야 하는 Drift 위험이 있고(문서화된 한계), 실 MCP Server가 동일 입력을 자체 권위 있는 Schema로 다시 검증하므로 이중 방어선이다. (3) **`service_id` UUID 불일치**: 07-data-api-contracts.md §1과 `local-runtime-api.yaml`의 `StartRunRequest.service_id`는 UUID를 명시하지만, D-034에 따라 Service Registry가 없어 실제로는 호출자가 주는 임의 문자열(Hosted Chat Slug 등)이 그대로 쓰인다. `workflow.py`의 `_derive_service_uuid`가 이미 UUID 형식이면 그대로 통과시키고, 아니면 `uuid5(NAMESPACE_URL, f"urn:agent-runtime:service:{service_id}")`로 **같은 입력→같은 UUID**가 되도록 결정적으로 파생한다 — 매 호출 임의(random) UUID를 새로 만들어 감사 추적성을 잃는 대신, 진짜 Registry가 생기기 전까지 최소한 동일 논리 서비스는 감사 로그에서 서로 연결되도록 하는 절충이다(실제로 실험해보니 office-mcp-server의 `RequestContext.service_id`/`mcp-audit-context.schema.json` 모두 `type: string`일 뿐 `format: uuid` 강제가 없어 원본 문자열을 그대로 넘겨도 오늘은 거부되지 않지만, 계약이 UUID를 명시하는 이상 형식을 지키는 쪽을 택했다). `service_version`/MCP 호출용 `user` Context도 마찬가지로 실 Registry/인증이 없어 PoC 상수(`0.0.0-poc-unregistered`, D-015 Mock User Context, role=`USER`, org=Office Profile의 `org`)를 사용한다. (4) **§8.4 사용자 확인 정책**: 등록된 3개 PoC Tool 중 `db_metadata.*` 2개는 `NEVER`(자동 승인)이고 `table_count.query`는 `ON_PARAMETER`다. `WAITING_FOR_USER` Run 상태(02-...md §5.3)는 이번 범위에서 구현하지 않았다 — 대신 `NEVER`가 아닌 Tool은 `confirmed=true`가 이미 실려오지 않는 한 항상 `MCP_CONFIRMATION_REQUIRED`로 **명확히 거부**한다(자동 승인도, 무한 대기도 아님). README §15 시나리오의 "DB Metadata 질문"은 `db_metadata.get_tables`/`get_columns`(둘 다 `NEVER`)로 충족되므로 인수 시나리오 자체는 막히지 않는다. **UPDATE (2026-08-08, D-061)**: `WAITING_FOR_USER`를 구현해 이 제약을 해소했다 — 상세는 D-061 참고. (5) **Agent 선택**: `config/standard-agent`(`mcp_allowed:false`, 기존 4개 게시 챗봇 사용 중)는 완전히 그대로 두고, 신규 `config/standard-db-agent`(`mcp_allowed:true`) + 전용 `config/standard-db-prompt`(Tool 근거 Placeholder `{{tool_evidence}}` 포함 — `standard-prompt`는 미변경)를 추가했다. `POST /local/v1/runs`의 `input.agent_profile`(생략 시 기존과 동일하게 `standard-agent`)로 선택하며, 미지원 값은 `AGENT_PROFILE_UNKNOWN`으로 명확히 거부한다. 이 필드는 `local-runtime-api.yaml`의 이미 개방된(`additionalProperties` 제한 없는) `input: object`에 담기므로 별도 Contract 변경 없이 추가했다. **부수 수정**: D-049에서 범위 밖으로 남겨졌던 `services/agent-runtime/config/office-profile-default/office-profile.json`의 구 Endpoint(`8300`)·구 Tool 이름(`get_table_metadata`/`get_table_count`) 버그를 `fixtures/valid/office-profile-default/office-profile.json`과 동일하게(`8500`, `db_metadata.get_tables`/`db_metadata.get_columns`/`table_count.query`) 수정했다. `mcp.call.started`/`mcp.call.completed` SSE Event(이미 `local-runtime-api.yaml`의 `RuntimeSSEEvent` enum에 존재)는 원본 Tool 결과/입력을 담지 않고 `tool_name`/`success`/`duration_ms`/`row_count`/`truncated`/`error_code`만 담는다. Trace ID는 Run → `audit_context.trace_id` → `X-Trace-ID` HTTP Header까지 전파를 코드로 확인했으나(`tests/integration/agent_runtime/test_mcp.py::test_trace_id_propagates_into_mcp_request_context`), 실제 office-mcp-server(:8500) 재기동 후 그 Audit Event에도 같은 Trace ID가 남는지는 이번 세션에서 서비스 재기동 없이는 검증하지 못했다(§15 12단계 최종 확인은 세션 소유자가 실 서비스로 재확인 예정) | Manifest Fixture 2건(`db_metadata.get_columns`/`table_count.query`, D-049 잔여 항목) 정리와 함께 실제 Tool Registry 조회로 `mcp_tools.py`의 정적 사본을 대체, 실 Service Registry 도입 후 `_derive_service_uuid` 제거. ~~`WAITING_FOR_USER` Run 상태 구현 후 `ON_PARAMETER`/`ALWAYS` Tool 실사용 허용~~ — **D-061로 완료(2026-08-08)** | M05/M10 |

| D-053 | M07 청킹 전략(Recursive/Markdown/Parent-Child) ID 체계와 크기 단위 | **DECIDED (2026-08-07)**: (1) **크기 단위**: `chunk_size`/`chunk_overlap`/`parent_chunk_size`/`minimum_size`(신규)를 모두 문자(UTF-8 code point) 단위로 확정했다 — 기존 코드는 `text.split()` 공백 토큰 수를 크기로 썼는데, 한국어는 공백 기준 분리가 실제 텍스트 길이를 잘 반영하지 못한다(스페이스 없는 긴 한글 구간이 흔함). 스키마의 기존 `chunk_size` 기본값 512/`chunk_overlap` 기본값 64는 이미 문자 단위를 전제로 한 값으로 보고 그대로 유지했고, 새로 추가한 `parent_chunk_size` 기본값 2048은 이전 하드코딩값(parent=1024단어/child=256단어, 비율 4:1)의 비율만 유지해 문자 단위로 옮긴 값이다(정확한 이전 문자수 재현이 아니라 "단어 기반 크기 산정 자체가 결함"이었으므로 비율 유지를 택함) — `packages/schemas/profiles/indexing-profile.schema.json`에 단위를 description으로 명시했다. (2) **`minimum_size` 신규 필드**: §2.5 Recursive의 "너무 짧은 마지막 청크는 앞 청크와 병합" 요구를 위해 계약 우선으로 스키마에 추가(1~4096, 기본 64). (3) **ID 체계**: `chunk_id`/`parent_id` 충돌 버그(내용만 해싱해 다른 위치의 동일 문장이 같은 id로 충돌 → Chroma upsert 시 유실)를 `f"{base}|{index}|{anchor}|{text}"` 해싱으로 교체했다(`services/indexing-runtime/src/indexing_runtime/chunkers/ids.py`) — 위치(index)가 동일 텍스트 충돌을 막고, 동일 입력에 대한 결정적 해시가 재인덱싱 시 안정성을 보장한다(재처리 순서가 바뀌어도 index/anchor/text가 같으면 동일 id). Recursive/Markdown 전략은 실제 Parent Store를 두지 않지만(§2.5가 Parent-Child에만 요구), `chunk_id = parent_id + ...` 공식을 일관되게 적용하기 위해 문서 단위의 합성(비영속) `parent_id`를 내부 해시 조합에만 사용한다(파일로 저장되지 않고 `parent_id` 메타데이터 키도 설정하지 않음). (4) **Loader/Chunker 책임 분리**: 기존에는 Loader(`loaders/__init__.py`)가 H1/H2만 인식해 분할까지 수행했다(§2.3/§2.5 위반, H3 무시). Loader는 이제 파일 전체를 `{document_id, content, metadata}` 공통 출력(§2.3)으로만 반환하고, 분할은 전적으로 `indexing_runtime.chunkers`(전략별 모듈)로 이동했다. (5) **Parent-Child의 Heading 메타데이터**: Parent-Child는 크기 기반 분할을 유지하되(§2.5가 헤딩 기반을 요구하지 않음), 각 Parent가 걸치는 헤딩 Section의 Title Path를 `chunkers/heading.py`로 조회해 `section`/`title_path` 메타데이터에 태깅한다 — 그렇지 않으면 현재 게시된 HR 정책류 챗봇의 Citation `section` 값이 실제 운영 재인덱싱 후 전부 빈 문자열이 되는 회귀가 발생하기 때문이다. (6) **Profile 전달 경로**: Indexing Profile Registry가 없으므로(D-034 계열과 동일한 공백), `run_pipeline(..., profile: dict | None = None)`과 `IndexJobRequest.profile`(선택 필드)을 추가했다 — 부분 dict를 `indexing_runtime.profile.resolve_profile`이 `DEFAULT_PROFILE`(parent_child, chunk_size=512, chunk_overlap=64, parent_chunk_size=2048, minimum_size=64, language=ko) 위에 병합·검증한다. 이름/버전/embedding_model_alias를 요구하는 전체 스키마 `required` 검증은 의도적으로 적용하지 않는다(이 병합 대상은 "등록된 Profile"이 아니라 "이번 실행이 쓸 청킹 설정 부분집합"이기 때문). `portal-api`의 `_trigger_indexing`을 포함한 기존 모든 호출자는 `profile`을 생략하므로 동작이 바뀌지 않는다. `chunk_overlap >= chunk_size`, `parent_chunk_size < 256`, 알 수 없는 `chunking_strategy`, 범위를 벗어난 값은 `ValueError`로 거부한다 | Indexing Profile Registry 도입 시 `resolve_profile`을 실제 등록된 Profile 조회로 교체하고 전체 스키마 `required` 검증 적용, 재인덱싱 후 D-046 `min_relevance_score`/평가 데이터셋 Recall@5·forbidden_hit_rate 재확인(세션 소유자가 수행 예정) | M06/M07/M08/M09 |

| D-054 | M09 `packages/knowledge-packager`(Package Assembler)의 `bm25.pkl` 배포 형식과 관련 잔존 유출 | **발견 (2026-08-07, `packages/knowledge-packager` 최초 구현 중)**: (1) **`bm25.pkl`은 Python pickle이다** — `services/indexing-runtime`이 `pickle.dump({"bm25": BM25Okapi(...), "chunk_ids": [...], "chunk_texts": [...], "chunk_metadata": [...]}, f)`로 기록한다. Knowledge Package는 배포 채널(향후 M03 Offline Bundle)을 거쳐 신뢰할 수 없는 목적지에 도달할 수 있으므로, 수신측이 `package-knowledge verify`를 실행할 때 이 파일을 `pickle.load`하면 GLOBAL/REDUCE/BUILD opcode가 임의 모듈 import와 임의 콜러블 호출을 유발하는 임의 코드 실행이 된다. **PoC 대응**: `knowledge_packager.bm25_inspect`가 `pickletools.genops`로 opcode 스트림만 disassemble해 dict/list/str/int/float 등 JSON-safe 값만 재구성하고, GLOBAL/STACK_GLOBAL/REDUCE/NEWOBJ/BUILD류는 실행 대신 불투명 placeholder로 치환하는 "안전한 pickle peek"을 구현해 `chunk_ids` 목록과 개수만 추출한다(어떤 모듈도 import하지 않고 어떤 콜러블도 호출하지 않음 — 단위 테스트로 `__reduce__`가 `eval`을 참조하는 악성 pickle을 실제로 안전하게 읽어냄을 확인). `verify`는 이 파일을 절대 `pickle.load`하지 않는다. **실측으로 드러난 두 가지 추가 잔존 유출**(모두 최초에는 예상하지 못했고, 실제 데이터로 `package-knowledge build`를 실행하며 발견함): (2) **Chroma 자체도 완전히 정리되지 않는다** — `source_path` 절대경로 유출(§4.1 스캐너 FATAL 대상, `data/indexes/d9e660b7-.../parents.json`에서 실제로 트립됨)을 고치려고 `Collection.update()`로 복사본의 metadata를 재작성해도, Chroma의 내부 `embeddings_queue` 테이블(모든 add/update를 보존하는 append-only 로그)에는 원본 `add()` 시점의 절대경로가 그대로 남는다 — `sqlite3`로 `embeddings_queue.metadata` 컬럼을 직접 조회해 확인했고, 그 뒤 `VACUUM`을 실행해도 제거되지 않음을 확인했다(해당 row가 아직 참조되는 살아있는 row이지 Freed Page가 아니기 때문). `chroma.sqlite3`도 `bm25.pkl`과 동일하게 `package-policy.yaml`의 `known_residual_leak_artifacts`에 등록해 FATAL 대신 WARNING으로 하향한다. (3) **Chroma HNSW 세그먼트 바이너리(`data_level0.bin`)의 초기화되지 않은 메모리 유출** — 실제 재택근무 정책 데이터(`d9e660b7-ca76-4f46-899e-2e1621bac139`)로 빌드해보니, `source_path`를 전혀 담지 않아야 할 순수 벡터 그래프 파일인 `data_level0.bin`(HNSW가 용량을 미리 할당해두는 파일)에서도 빌드 호스트 절대경로 바이트열이 그대로 발견되었다 — HNSW 라이브러리가 사전 할당한 메모리 영역을 0으로 채우지 않아, 같은 프로세스가 이전에 다룬 문자열(파일 경로 등)의 잔여 바이트가 우연히 파일에 섞여 들어간 것으로 추정된다. 이는 특정 메타데이터 필드와 무관한, 파일 포맷 자체의 구조적 문제라 `knowledge_packager.relativize`가 고칠 방법이 없어 **의도적으로 FATAL로 유지**한다(수리 불가능한 것을 조용히 WARNING으로 낮추지 않음) — 실제로 `--relativize-source-paths`를 켜도 이 FATAL 1건은 그대로 남는다(재현: 아래 실측 참고). (4) **부수 발견 — 검증 리포트 자체가 2차 유출 경로였다**: `knowledge_packager.scanner`의 최초 구현은 발견된 매치 주변 문맥만 자르고 매치된 문자열 자체는 그대로 노출해, 그 excerpt가 `package-manifest.json`의 `verification` 블록에 그대로 기록되면서 **package-manifest.json 자신이 동일한 절대경로를 다시 유출**했다(직접 확인: 방금 빌드한 package-manifest.json이 자기 자신을 재스캔하면 FATAL이 뜸). `scanner._redact_excerpt`가 매치 구간 자체를 `[REDACTED]`로 치환하도록 수정해 해소했다(CLAUDE.md "Log에 Secret을 기본 저장하지 않는다" 원칙의 연장). (5) **정정 (2026-08-07)** — 이전 판(D-054 최초 항목)은 "Chroma는 복사본을 새 프로세스가 처음 열 때 한 번만 자기 자신을 변형하고, 같은 파일을 다시 열면 no-op이다"라고 기록하고, 이를 근거로 `manifest_file_list_consistency`/`checksum_integrity` 두 검사에서 `index/chroma/**` 전체를 제외했다. **이 "두 번째 open부터는 no-op" 주장은 실측으로 반증되었다**: 같은 손대지 않은 Package의 `index/chroma/` 트리 전체를 해시해, 연속된 `package-knowledge verify` 3회에 대해 비교한 결과는 다음과 같다 —

```
verify 전:      1df8a593dc01300e
verify 1회 후:  59395746b8532eb6
verify 2회 후:  5c2976d199fb601f
verify 3회 후:  ea5d24e1ce09768f
```

즉 Chroma는 매번 열 때마다(첫 번째뿐 아니라 그 이후로도 계속) 자기 자신의 바이트를 재작성한다 — "settle"되어 안정화되는 시점이 없다. 이 잘못된 전제 위에서 내린 "그러므로 `index/chroma/**`는 checksum 비교에서 제외해야 한다"는 결론도 함께 폐기한다: 그 제외 때문에 Package의 실제 데이터 대부분(Vector Index 자체)이 `checksum_integrity`의 검증 범위 밖에 있었고, 전송 중 Vector Index가 변조된 Package도 `checksum_integrity`를 그대로 통과했다 — 정확히 이 검사가 보장해야 할 속성이 이 하위 트리에는 적용되지 않고 있었다(실측: 재택근무 정책 실 데이터로 빌드한 Package가 15개 파일 중 4개만 선언·검증되고 있었음). **해소**: `knowledge_packager.index_reader.read_chroma_snapshot`이 이제 `index_dir/chroma`를 직접 열지 않고, `tempfile.TemporaryDirectory`에 만든 **일회용 복사본**을 열어 checks 2-4(Record 수/Chunk ID 일치/Child-Parent 참조 무결성)를 수행한 뒤 즉시 버린다 — `build()`의 자체 Chroma read도 동일 함수를 거치므로 동일하게 보호된다. 이로써 Package 자신의 `index/chroma/**` 바이트는 어떤 프로세스가 몇 번을 열어도 절대 변형되지 않고, `manifest_file_list_consistency`/`checksum_integrity` 두 검사 모두 더 이상 이 경로를 제외할 필요가 없어져 제외 로직 자체를 제거했다 — `chroma.sqlite3`와 모든 HNSW 세그먼트 파일이 다른 모든 파일과 동일하게 Manifest에 선언되고 checksum으로 검증된다(신규 단위 테스트: `chroma.sqlite3` 내부 1바이트 변조, HNSW `data_level0.bin` 1바이트 변조 모두 `checksum_integrity` FAIL로 탐지됨을 확인; `verify()` 연속 3회 실행이 동일한 결과를 반환하고 Package 트리 전체 해시가 실행 전후로 불변임을 확인) | `bm25.pkl`을 실행 가능한 pickle이 아닌 비실행 포맷(JSON 기반 BM25 통계 직렬화 또는 유사한 안전한 포맷)으로 교체 — PoC 범위에서는 §4.1 검증 가치가 이미 실증되었고(안전한 peek로 개수·ID 일치성 검증 가능), 실제 포맷 교체는 M07 indexing-runtime과의 계약 변경이 필요해 별도 작업으로 미룬다. Chroma `embeddings_queue` 잔존 유출은 Chroma 자체의 Compaction/Vacuum 기능이 성숙하기 전까지 구조적으로 해소 불가능하므로 "허용된 잔존 위험"으로 문서화하는 것 외 대안이 마땅치 않다. `data_level0.bin` 초기화되지 않은 메모리 유출은 Chroma 업스트림 이슈로 보고할 가치가 있으나 이번 세션 범위 밖. **(6) 정정 (2026-08-07, 같은 날 후속 세션) — 위 "참고(범위 밖으로 남긴 채 기록)" 문단은 틀렸고, 후속 작업으로 미룰 수 있는 사안이 아니었다**: 그 문단은 "chroma.sqlite3가 구조적으로 손상될 정도로 변조되어야만" `verify()`가 예외로 중단된다고 적었지만, 실측 결과는 정반대였다 — 실제 재택근무 정책 Package의 `chroma.sqlite3` **정중앙에서 딱 한 바이트만 XOR-flip**해도 SQLite의 btree 구조가 깨져 `chromadb.errors.InternalError: Error executing plan: ... database disk image is malformed`이 즉시 발생했다. "구조적 손상"은 극단적인 예외 케이스가 아니라 **전송 중 한 바이트 변조라는, 이 검사가 막으려는 가장 현실적인 공격 그 자체**였다. 게다가 그 예외는 `verify()`와 CLI 밖으로 그대로 전파되어 Python traceback 전체 — 이 저장소의 build-host 절대경로(`/Users/<user>/.../builder.py` 등 site-packages/`.venv` 경로 다수 포함)를 그대로 노출 — 를 표준출력에 남겼다. 즉 "탐지"가 아니라 "크래시"였고, 이 패키지가 막으려던 바로 그 유형의 경로 유출을 이 패키지 자신이 저질렀다. 근본 원인은 (5)에서 고친 것과 다른 축의 순서 문제였다: `builder.verify()`가 `checksum_integrity`/`manifest_file_list_consistency`를 실행하기 **전에** `index_reader.read_chroma_snapshot`(따라서 `chromadb`의 실제 파일 open)을 무조건 먼저 호출하고 있었다. 당시 존재하던 탬퍼 테스트들은 이 경로를 실제로 검증하지 못했다 — `test_verification.py`의 테스트는 `run_all_checks()`를 미리 파싱된 `VerificationInputs`로 직접 호출해 `chromadb`를 아예 거치지 않았고, `test_builder.py`의 테스트는 (주석에 그 의도가 명시돼 있었듯) SQLite의 btree 구조를 다치지 않도록 일부러 문서 텍스트 payload 안의 바이트만 골라 변조해 실제로는 절대 손상되지 않는 파일만 만들고 있었다. **수정**: `builder.verify()`를 2단계로 재구성했다 — (1) 정합성 게이트: `manifest_file_list_consistency`/`checksum_integrity`를 Chroma를 열거나 `bm25.pkl`/`parents.json`을 읽기 **전에**, 이미 디스크에서 읽은 원시 바이트와 Manifest 선언 해시만으로 먼저 실행한다. 둘 중 하나라도 실패하면 그 자리에서 즉시 반환하고, 나머지 5개 검사는 실행하지 않는다 — 단, 무언의 PASS도, 조작된 FAIL도 아닌 `NOT_RUN`이라는 별도 상태(`details.not_run=true`와 사유 메시지)로 명시적으로 보고한다. (2) 정합성이 통과한 뒤에도 Chroma open·JSON 파싱·pickle opcode peek 전체를 단일 `try/except Exception`으로 감싸, 그 안에서 발생하는 어떤 실패든(`content_check_error` 상세와 함께) FAIL로 보고할 뿐 다시는 raise하지 않는다. 예외 메시지(서드파티 `chromadb` 메시지 포함)는 새로 추가한 `scanner.sanitize_text`(기존 절대경로/Secret 정책 패턴을 재사용하는 redaction)를 통과한 뒤에만 리포트/CLI에 출력하고, CLI에도 마지막 방어선으로 `except Exception` 가드를 추가해 어떤 경우에도 traceback이 사용자에게 노출되지 않게 했다. **실측 재검증(모두 실제 재택근무 정책 Package로 CLI를 통해 수행)**: (a) `chroma.sqlite3` 정중앙 1바이트 변조, (b) HNSW `data_level0.bin` 정중앙 1바이트 변조, (c) `chroma.sqlite3`를 절반 크기로 truncate — 세 시나리오 모두 `package-knowledge verify`가 `checksum_integrity` FAIL로 해당 파일명을 정확히 지목하며 종료코드 1로 끝났고, 표준출력·표준에러 어디에도 `Traceback`이나 `/Users/`가 없음을 확인했다. `verify()` 3회 연속 실행에 대한 Package 전체 트리 바이트 불변성도 다시 확인했다((5)에서 확보한 보장이 이번 변경으로 깨지지 않음). 신규 단위 테스트 5개 추가 — `tests/unit/knowledge_packager/test_builder.py`에 정중앙 바이트 플립(2건)·truncate(1건)·정합성 통과 후 콘텐츠 읽기 실패(1건), 신규 파일 `tests/unit/knowledge_packager/test_cli.py`에 CLI 종단 테스트 2건(그중 하나는 실패한 `verify` 전체 출력에서 `/Users/`와 `Traceback`을 grep해 부재를 직접 단언). `manifest_file_list_consistency` 실패(예: 선언된 파일이 디스크에서 사라짐) 역시 이제 예외를 던지지 않고 동일한 정합성 게이트로 우아하게 보고되도록 `test_verify_detects_file_removed_from_package`를 함께 고쳤다. 전체 단위 테스트 518→523개, 전부 통과, `ruff check .` 0건(신규 `# noqa` 없음), `make e2e-test`(16 passed/11 skipped)·`make security-test`(63 passed/1 skipped) 동일 유지 **(7) 해소 (2026-08-10, `bm25.pkl` → `bm25.json` 포맷 교체 — D-054의 근본 원인 자체를 제거)**: (5)/(6)이 다뤘던 것은 전부 `bm25.pkl`을 pickle인 채로 두고 그 주변(peek/scanner/checksum 순서)을 방어하는 완화책이었다. 이번 세션은 최초 제안된 실제 수정 — pickle을 아예 그만 쓰는 것 — 을 구현했다. **새 포맷**: `services/indexing-runtime`의 신규 `bm25_store.py`가 `bm25.json`(plain JSON: `tokenized_corpus`/`chunk_ids`/`chunk_texts`/`chunk_metadata`, `BM25Okapi` 인스턴스는 저장하지 않음)을 쓰고, `pipeline.run_pipeline`이 더 이상 pickle을 만들지 않는다(`BM25Okapi` 생성 자체를 색인 시점에서 제거 — 검색 시점에만 재구성). `services/search-runtime`은 동일 이름의 독립 `bm25_store.py`(M07/M08 상호 Import 금지, D-067의 `chroma_client_cache.py` 중복과 동일 선례)로 `bm25.json`을 읽어 `rebuild_bm25(tokenized_corpus)`로 `BM25Okapi`를 결정적으로 재구성한다 — `BM25Okapi.__init__`은 입력에 대한 순수 함수(idf/doc_freqs/avgdl 계산에 무작위성 없음)이므로 점수는 이전 pickle 기반 구현과 항상 동일함을 라운드트립 테스트(`tests/unit/indexing_runtime/test_bm25_store.py`, `test_convert_bm25_format.py`)와 아래 실측으로 확인. **재구성 비용 실측**: 실제 데이터(재택근무 정책 4개 chunk, LLM 요약 문서 64개 chunk)에서는 JSON 읽기+`BM25Okapi` 재구성이 호출당 4.7ms — 이미 Ollama 임베딩 호출이 지배하는 요청(200~300ms)에서 무시 가능한 수준. 그러나 합성 대형 코퍼스로 직접 측정한 결과, 이 비용은 코퍼스 크기에 선형이며 legacy pickle.load보다 유의미하게 크다: 1,000 chunk = JSON+재구성 30ms vs pickle.load 3ms, 5,000 chunk = 168ms vs 18ms, 20,000 chunk = 697ms vs 68ms(약 10배). 더 중요한 정직한 발견: `hybrid_search()`는 **원래부터(이번 변경 이전에도) 매 검색 요청마다 bm25 파일을 다시 읽고 다시 구성했다** — 요청 간에 캐시된 적이 없다. 즉 "색인 로드 시 1회성 비용"이라는 가정은 틀렸다 — 이는 **매 쿼리마다** 반복되는 비용이며, 이번 포맷 교체로 그 비용이 코퍼스 크기에 비례해 pickle 대비 최대 10배 커진다. **완화책은 이번 범위에 포함하지 않고 제안만 한다**(CLAUDE.md 원칙 4 "한 PR은 하나의 계약 변경으로 제한"): `services/search-runtime`에 D-067의 `chroma_client_cache.py`와 동일한 정신으로 재구성된 `BM25Okapi`를 index 경로+`bm25.json`의 mtime(또는 checksum) 키로 캐싱하는 LRU를 추가하는 것을 후속 결정으로 남긴다(아래 참고). PoC 실 데이터 규모(수십~수백 chunk)에서는 무시 가능하므로 이번 세션에서 그대로 배포해도 안전하다고 판단했다. **레거시/반입 신뢰 경계**: `search_runtime.settings.ALLOW_LEGACY_PICKLE_BM25`(기본 `true`, env `SEARCH_ALLOW_LEGACY_PICKLE_BM25`) — `bm25.json`이 없고 `bm25.pkl`만 있는(아직 `convert-bm25-format`을 실행하지 않은) 색인에 대해서만 legacy pickle 로딩을 허용할지 결정하는 배포 단위 설정. 허용 시 `search.bm25.legacy_pickle_fallback` WARNING 로그가 매번 남는다(조용한 기본값이 아님). `false`로 설정하면 `search_runtime.bm25_store.LegacyPickleBm25Refused`가 발생해 `main.py`가 이를 `KNOWLEDGE_INDEX_CORRUPT`(07-data-api-contracts.md §8 기존 코드 재사용, 신규 코드 발명 없음) 오류 응답으로 변환한다 — 조용히 빈 결과로 축소되지 않는다(빈 결과는 "근거 없음"과 구분이 안 되어 이 거부를 숨기게 됨). **정직한 한계**: 이 설정이 실제로 막아야 하는 대상 — Desktop Offline Bundle에서 반입된 색인 디렉터리를 가리키는 search-runtime 인스턴스 — 은 오늘 이 코드베이스에 실제로 존재하지 않는다(D-059/D-060이 이미 기록했듯 Desktop의 로컬 실행/검색 배선 자체가 아직 미완성). `services/distribution-service`의 `bundler.collect()`는 `data/indexes/{id}/`의 파일을 그대로 zip에 복사할 뿐 `knowledge_packager`를 거치지 않으므로, 이 세션에서 실제 반입-후-검색 경로를 End-to-End로 검증할 방법이 없다 — 메커니즘(설정 플래그 + 명확한 거부)만 만들어 두고 실제 Desktop 배선이 생기면 그 배포가 반드시 `SEARCH_ALLOW_LEGACY_PICKLE_BM25=false`로 켜야 한다는 것을 문서화했다. `curl`로 :8303(허용, 기본값)과 :8304(`SEARCH_ALLOW_LEGACY_PICKLE_BM25=false`) 두 개의 임시 인스턴스를 스크래치패드의 legacy-pickle-only 픽스처에 대해 직접 띄워, 허용 시 정상 응답 + WARNING 로그, 거부 시 500 `KNOWLEDGE_INDEX_CORRUPT` + ERROR 로그를 각각 실측 확인. **`bm25_inspect`(M09 `packages/knowledge-packager`)**: 완전히 퇴역하지 않고 단순화·확장했다 — pickletools opcode walker(`peek_bm25_pickle`)는 legacy `bm25.pkl`만 남은, 아직 변환되지 않은 색인으로부터 만든 Package를 여전히 안전하게 검증해야 하므로 그대로 유지, 신규 `peek_bm25_json`(순수 `json.loads`, pickle 코드 경로 전혀 없음)과 두 포맷을 자동 판별하는 `peek_bm25_artifact(index_dir)`(`bm25.json` 우선)를 추가해 `builder.py`의 두 호출부(build/verify)를 단일화. **부수적으로 D-054의 오래된 잔존 유출 하나가 함께 해소됨**: `bm25.json`은 plain JSON이므로 `knowledge_packager.relativize.rewrite_bm25_json`(신규)이 `parents.json`과 동일하게 `chunk_metadata`의 `source_path` 절대경로를 완전히 재작성할 수 있다 — 기존에 "unpickle 없이는 고칠 수 없어 WARNING으로 하향"했던 `bm25.pkl` 잔존 유출이, 변환된 색인에 대해서는 더 이상 존재하지 않는다(`package-policy.yaml`의 `known_residual_leak_artifacts`에서 `bm25.pkl`은 여전히 legacy 전용으로 등록 유지). Chroma `embeddings_queue`/`data_level0.bin` 잔존 유출 2건은 이번 범위 밖으로 변경 없음(Chroma 자체의 구조적 한계, 위 (5)/(6) 그대로). **converter**: `indexing-runtime`에 `stamp-classification`과 동일한 정신의 신규 `convert-bm25-format` CLI(`indexing_runtime/convert_bm25_format.py`) — 기존 색인을 재임베딩 없이 제자리에서 `bm25.pkl` → `bm25.json`으로 변환, idempotent(이미 변환됐으면 no-op, 변환 중 중단으로 두 파일이 공존하면 정합성만 재확인 후 stale pickle만 정리), 원본 `bm25.pkl`의 실제 `BM25Okapi.get_scores()`와 재구성된 `BM25Okapi.get_scores()`를 코퍼스 자체에서 뽑은 canary 쿼리로 대조해 일치할 때만(기록 후 재검증까지 2단계) 원본을 삭제 — 검증 실패 시 원본은 항상 보존됨. `stamp_classification.py`도 `bm25.json`을 우선 사용하도록(있으면 pickle 전혀 사용 안 함) 갱신, 없으면 기존 legacy pickle 경로 그대로 유지. **실 데이터 검증**: 스크래치패드에 백업된 실제 색인 4개(`43d83955-...`64 chunk, `a038442d-...`11 chunk, `d9e660b7-...`(재택근무 정책) 4 chunk, `hr-policy-v1` 4 chunk)를 복사해 `convert-bm25-format`으로 전부 변환(전부 `"action": "converted"`, `bm25.pkl` 제거 확인) 후, 변환 전/후 동일 search-runtime 인스턴스(스크래치패드 복사본을 가리키는 `:8302`, 실 Ollama `:11434`/`qwen3-embedding:0.6b` 사용, `data/indexes/`는 세션 내내 미접근)에 동일 질의 4건("장비 지원은 무엇이 있나요?", "재택근무 신청 방법", "생성형 AI란 무엇인가요?", "LLM의 한계는 무엇인가요?")을 실행 — citation 목록(`chunk_id`/`excerpt`/`score`/`similarity` 전부)이 변환 전후 100% 동일함을 직접 대조 확인(`latency_ms`/`trace_id`/`search_id`만 제외). 검증에 사용한 임시 인스턴스(`:8302`/`:8303`/`:8304`)는 모두 종료, 라이브 `:8300`/`:8200`(지시대로 무응답 상태 유지)은 손대지 않음, `data/indexes/`·`apps/portal-api/portal.db`는 세션 내내 읽기 전용. **검증**: `uv run pytest tests/ -q` 838 passed/2 skipped/92 deselected(800 기준선 +38), `uv run ruff check .` 0건, `make contract-test` 30 passed(기존과 동일, `knowledge-package.schema.json`의 `artifact_notes.bm25_pkl_is_pickle`/`bm25_pkl_relativized` 필드는 이름 유지·의미만 갱신이라 스키마 구조 변경 없음) **(8) 후속 (b) 해소 (2026-08-10, 같은 날 후속 세션) — search-runtime에 재구성-비용 캐시 도입**: (7)이 남긴 "정직한 성능 발견"(`hybrid_search()`가 매 질의마다 `bm25.json`을 다시 읽고 `BM25Okapi`를 다시 만들어, 대규모 코퍼스에서 legacy pickle 대비 최대 ~10배 비싸짐 — 20,000 chunk 697ms)을 해소했다. 신규 `services/search-runtime/src/search_runtime/bm25_cache.py`가 D-067 `chroma_client_cache.py`와 동일한 형태(Bounded, Thread-safe LRU, 신규 설정 `SEARCH_BM25_CACHE_MAX_SIZE` 기본 32)의 캐시를 구현하고 `hybrid.py`가 이를 통해서만 BM25를 로드하도록 교체했다. **Cache Key는 색인 디렉터리 경로가 아니라, 신규 `bm25_store.resolve_bm25_source`가 알려주는 실제로 resolve된 BM25 파일의 `(경로, mtime_ns, size)`** — 이 프로젝트가 반복해서 겪은 "조용한 오답" 계열 결함(D-046의 누락된 관련도 임계값, D-062의 기본-허용 분류 공백)과 같은 종류가 캐싱 도입으로 재발하지 않도록 하는 것이 이 설계의 핵심이다: 재인덱싱(`pipeline.run_pipeline`)·`convert-bm25-format`·`stamp-classification` 중 어느 것이 `bm25.json`을 다시 쓰든 mtime/size가 바뀌므로 자동으로 Cache Miss가 되어(별도 무효화 호출이 코드베이스 어디에도 필요 없음) 이전 색인을 계속 서빙하는 일이 없고, pickle→JSON 포맷 전환은 resolve되는 파일명 자체가 바뀌므로(경로 컴포넌트가 다름) 이 역시 확실한 Miss다. Eviction은 Chroma Client Cache와 달리 `.close()`가 필요 없다 — `BM25Okapi`(및 그 근거가 된 `document` dict)는 네이티브 스레드풀·소켓·파일핸들을 전혀 갖지 않는 순수 Python/NumPy 힙 객체이므로 단순 `dict.popitem()`으로 충분하다(근거는 모듈 자체 Docstring에 상술 — Chroma Client Cache의 `.close()`가 필요했던 이유와 대비해서 설명). **실측(이 항목의 최초 실측과 동일 방법론으로 비교 가능하게 측정)**: 실 데이터(LLM 요약 문서 64 chunk) 3.80ms(Uncached)→0.0175ms(Cached Hit, 약 217배), 합성 1,000 chunk 24.13ms→0.012ms(2073배), 5,000 chunk 167.64ms→0.020ms(8281배), 20,000 chunk 681.10ms→0.023ms(29948배) — Cache Hit 비용은 코퍼스 크기와 무관한 상수(dict 조회 1회 + `stat()` 1회). **무효화 실측(이 캐시에서 가장 중요한 속성)**: 신규 단위 테스트 9개(`tests/unit/search_runtime/test_bm25_cache.py`)가 동일 색인 반복 조회 시 동일 객체 재사용과 디스크 재읽기 0회(호출 카운터로 직접 확인)를 증명하고, **`bm25.json`을 실제로 변경(mtime을 강제로 전진시키는 것까지 포함)한 뒤의 조회가 새 `chunk_ids`/새 BM25 통계를 반환값 자체로 반영**함을 확인(내부 카운터가 아니라 실제 콘텐츠로 검증 — 이것이 이 작업이 요구한 "가장 중요한 테스트"), pickle→JSON 전환이 별도 Cache Key로 분리됨을 확인, Bounded Size와 LRU Eviction 순서, Uncached 경로와의 점수 100% 동일(별도 라운드트립), Legacy Pickle 경로도 동일 조건으로 캐싱되고 거부(`allow_legacy_pickle=False`)는 캐싱되지 않음을 확인. 기존 `tests/unit/search_runtime/conftest.py`의 `write_bm25_index`가 `rebuild_bm25`를 Monkeypatch하던 대상을 (호출 위치 이동에 맞춰) `hybrid_module`에서 `bm25_cache`로 옮겼을 뿐 — 기존 68개 search_runtime 테스트는 전부 무수정 통과(모든 테스트가 pytest `tmp_path`를 쓰므로 Cache Key 충돌 없음을 확인). **실 데이터 종단 검증**: 스크래치패드에 백업된 실 색인(`43d83955-...`, 64 chunk)을 복사해 `convert-bm25-format`으로 변환 후, (i) 실 Chroma 벡터스토어 + 결정적 고정 임베딩(Ollama `/api/embeddings`를 동일 Prompt로 반복 호출해도 완전히 같은 벡터를 반환하지 않는, 이 캐시와 무관한 사전 존재 비결정성을 직접 확인했고 — 벡터 축의 특성이지 이 캐시가 다루는 BM25 축과는 무관 — 이를 배제하기 위해 `embed_query`를 고정 벡터로 Monkeypatch)으로 `hybrid_search()`를 직접 호출해 Cache를 매 호출 전 강제로 비운 경우(Cold)와 비우지 않은 경우(Warm, 3회 연속)를 비교 — 4개 실 질의 전부 Citation·Score·순서 100% 일치. (ii) 실 Ollama(`:11434`)를 그대로 쓰는 HTTP 종단(임시 인스턴스 `:8302`, `INDEX_BASE`를 스크래치패드 사본으로 지정)에서도 실제 Citation을 확인했으나, 위에서 확인한 Ollama 자체의 호출간 비결정성 때문에 HTTP 레벨 반복 비교는 이 캐시 하나만의 영향을 격리하는 방법으로는 부적절하다고 판단해 (i)을 이 세션의 정식 무효화/불변성 증거로 채택했다 — 이는 은폐가 아니라 방법론 선택의 근거를 정직하게 남기는 것이다. `:8302`는 검증 종료 후 종료, `data/indexes/`·`apps/portal-api/portal.db`는 세션 내내 미접근(읽기 전용 준수). **검증**: `uv run pytest tests/ -q` 847 passed(838 기준선 + 신규 9)/2 skipped/92 deselected, `uv run ruff check .` 0건, `make contract-test` 30 passed(계약 변경 없음, `SEARCH_BM25_CACHE_MAX_SIZE` 신규 설정 1개만 추가) | (7)로 이 항목의 핵심 권고(비실행 포맷 교체)는 해소됨. (8)로 후속 (b)(재구성-비용 캐싱)도 해소됨. 남은 후속 항목: (a) `data/indexes/`의 실제 색인들(및 게시된 4개 챗봇이 참조하는 색인)에 대한 `convert-bm25-format` 실행은 재기동을 동반하므로 세션 소유자의 몫(stamp-classification과 동일한 운영 절차, docs/implementation-spec/13-windows-local-setup.md에 안내 추가), (b) **해소 — 위 (8) 참고**, (c) Desktop Offline Bundle 반입 후 실제 검색 경로가 생기면 `SEARCH_ALLOW_LEGACY_PICKLE_BM25=false`를 그 배포의 기본값으로 강제할지/설정 문서화만으로 충분한지 재검토, (d) Chroma `embeddings_queue`/`data_level0.bin` 잔존 유출 2건은 여전히 미해결(범위 밖, Chroma 업스트림 성숙 대기) | M07/M08/M09/M11 |

| D-055 | P12 Knowledge 품질의 Evaluation Dataset 편집/저장 | **범위 밖으로 명시 보류 (2026-08-08, P12 Knowledge 품질 화면 구현 중)**: P12 명세는 "검토자는 평가 질문을 추가하거나 기준 미달 사유를 기록할 수 있다"고 요구하지만, 이번 작업에서는 사유 기록(`POST /api/v1/evaluations/{id}/notes`)만 구현하고 **질문 추가/편집은 구현하지 않았다** — Evaluation Dataset 자체가 아직 어떤 저장소에도 없기 때문이다(`packages/evaluation-runner`는 `evaluation-dataset.schema.json`으로 검증된 JSON 파일만 다루고, portal-api의 `evaluation_results` 테이블은 *결과*만 저장한다). CLAUDE.md "테스트 증거 없는 기능은 완료로 표시하지 않는다"·명세 우선순위 규칙에 따라 절반짜리(예: 편집 UI는 있지만 저장 후 다음 실행에 반영되지 않는) 기능을 만들지 않고 이 항목으로 남긴다. 대신 `POST /api/v1/assets/{assetId}/versions/{versionId}/evaluations`는 **PoC 발견 규칙**을 쓴다: `fixtures/valid/*/evaluation-dataset.json`을 스캔해 자신의 `knowledge_asset_id` 필드가 대상 Knowledge Asset의 id와 일치하는 첫 파일을 사용한다(`portal_api.routers.evaluations._find_dataset_path`) — 일치하는 파일이 없으면 `EVALUATION_DATASET_NOT_FOUND`(400)로 명확히 거부하고, 조용히 빈 데이터셋으로 평가를 "성공"시키지 않는다 | Evaluation Dataset을 위한 실제 저장소(신규 테이블 또는 Registry Asset 타입)와 CRUD API/Schema 설계, 그 위에 "질문 추가/편집" UI 구현, 데이터셋 버전 관리(§4.6 버전 비교가 요구하는 `dataset_version` 증가 규칙)와 `review_status`를 `EXPERT_REVIEWED`로 전환하는 검토 흐름 | M01/M02/M09 |
| D-056 | P12 평가 결과 마스킹의 대상 필드와 권한 역할 | **PoC 가정, 2026-08-08 정정**: P12는 "운영 사용자가 원문 전체를 볼 권한이 없다면 평가 결과에서도 **문서 내용**을 마스킹한다"고 요구한다. 최초 구현은 `question`을 마스킹 대상으로 삼았으나 이는 잘못된 판단이었다 — `question`은 평가 질문 데이터셋 작성자(또는 생성 도구)가 작성한 질문이지 문서 내용이 아니며, P12는 같은 절에서 "평가 질문별 기대 문서와 검색 결과"를 전체(당사자 전체)에게 보여야 할 항목으로 명시한다. `question`을 비특권 역할에게 숨기면 그 역할에게 화면의 핵심 표가 비어 보이게 되어 P12 자신의 표시 요구와 정면으로 충돌한다. **정정된 결론**: `evaluation-result.schema.json`의 `per_case[]`는 `case_id`/`question`/`expected_document_ids`/`retrieved_document_ids`/`hit_at_1`/`hit_at_5`/`reciprocal_rank`/`latency_ms`/`returned_count`/`context_tokens`/`forbidden_hit`/`tags`만 담으며, 이 중 어느 필드도 문서 발췌(excerpt)나 문서 본문이 아니다 — 전부 id·판정·시간 등 메타데이터다. 따라서 **오늘 시점에는 마스킹 대상이 존재하지 않는다**. `portal_api.routers.evaluations.get_evaluation`은 이제 `per_case`를 어떤 역할에게도 redact하지 않고, 응답의 `source_masking_note`(그리고 화면)에 "저장된 평가 결과에는 문서 내용이 없어 마스킹이 적용되지 않는다"는 사실을 그대로 명시한다 — 아무것도 마스킹되지 않는데 "마스킹됨" 표시를 보여주지 않는다. `Permission.EVALUATION_SOURCE_VIEW`와 그 역할 매트릭스(CREATOR(소유 여부 무관)·TECH_REVIEWER·SECURITY_REVIEWER·RELEASE_MANAGER·ADMIN에 부여, USER·AUDITOR에는 미부여, `packages/security-policy/src/security_policy/roles.py`)는 **그대로 유지**한다 — 이 권한 자체는 틀리지 않았고, `evaluation-result.schema.json`의 `per_case`에 `retrieved_excerpt`류의 실제 문서 발췌 필드가 추가되는 순간이 이 권한을 실제로 검사(`has_permission(role, Permission.EVALUATION_SOURCE_VIEW)`)해 그 필드를 마스킹 대상으로 삼기 시작할 트리거다. 통합 테스트(`tests/integration/portal_api/test_evaluations.py::TestGetEvaluationPerCaseNotMasked`)는 CREATOR·AUDITOR·(신규 dependency override로 만든) 순수 USER 역할 모두가 동일한 `per_case` 전체를 본다는 것을 검증한다 | D-022(분류등급 기반 접근 제어)가 구현되면 역할 기반이 아닌 분류등급/조직 기반 마스킹으로 재검토. `evaluation-result.schema.json`에 문서 발췌(excerpt) 필드가 추가되면 그 필드에 한해 `EVALUATION_SOURCE_VIEW` 마스킹을 켠다 — 그 전까지는 마스킹 로직을 추가하지 않는다 | M01/M02/M09/M11 |

| D-057 | P16 긴급 Revocation — 재다운로드 시 재검사 범위 | **PoC 가정, 2026-08-08**: 긴급 Revocation은 사유·승인자·효력 시각을 가진 `asset_version_revocations` 레코드(orthogonal 배포 게이트, D-053 계열 결정과 동일하게 상태 머신을 건드리지 않음)로 구현했다. Enforcement는 두 지점에서 일어난다 — (1) **신규 Bundle 생성**: portal-api(`routers/distributions.py`)가 매 요청마다 효력 발생한(`effective_at <= now()`) 회수를 조회해 root와 모든 의존성 item에 `revoked` 플래그를 채워 distribution-service(M03)에 전달하고, `resolver.py`의 `AssetVersionRevokedError`가 이를 차단한다 — ASSET_VERSION root든 SERVICE_VERSION root의 중첩 Knowledge 의존성이든 동일하게 커버된다. (2) **이미 SUCCEEDED로 완성된 Bundle의 재다운로드**: `DistributionRequest`가 자신의 `root_type`/`root_id`만 저장하고 해석됐던 개별 의존성 item 목록(어떤 asset_version_id들이 실제로 패키징됐는지)은 영속화하지 않으므로, 재다운로드 시점에 즉시 재검사할 수 있는 것은 `root_type == ASSET_VERSION`인 경우의 `root_id` 하나뿐이다 — 이 경우는 재검사해서 회수 시 `ASSET_VERSION_REVOKED`로 차단한다. `root_type == SERVICE_VERSION`인 Bundle이 과거에 패키징한 Knowledge 의존성이 그 이후 회수된 경우, 이미 디스크에 존재하는 해당 ZIP의 재다운로드는 오늘 시점에 차단되지 않는다(신규 Bundle 생성 요청은 여전히 차단됨) | 서비스 다운로드 재검사를 완전하게 만들려면 `DistributionRequest`(또는 별도 테이블)에 해석된 의존성 item의 asset_version_id 목록을 영속화해야 한다 — Offline Bundle 자체가 정적 스냅샷이라는 §3.3 설계와 상충하지 않는지, 그리고 이미 배포된 오프라인 Bundle 파일을 사후에 무효화하는 것이 폐쇄망 배포 모델에서 실효성이 있는지(파일이 이미 반출됐다면 재다운로드 차단은 상징적 조치에 가까움) 먼저 결정 필요 | M02/M03 |

| D-058 | Desktop D06/D07(대화/실행, 실행 상세)의 권한 모델 부재로 인한 3가지 생략 | **발견 (2026-08-08, D06/D07 최초 구현 중)**: Desktop Client(M04)에는 아직 어떤 사용자 역할/권한 모델도 없다(D-035가 agent-runtime에 대해 이미 기록한 것과 동일한 공백이 M04에도 그대로 있음, D-022 조직·사업장 권한도 미구현). 이 공백이 D06/D07 명세 3곳과 직접 충돌해, 없는 권한 모델을 지어내는 대신 아래처럼 처리했다: (1) **Citation 발췌 접근 통제**: D06은 "원문 접근 권한이 없으면 발췌를 표시하지 않는다"고 요구하지만 검사할 권한 자체가 없으므로, `ChatScreen.tsx`의 Citation 클릭 상세(`CitationDetailModal`)는 검색 결과에 이미 포함된 발췌를 항상 그대로 표시한다. (2) **파일 첨부 UI**: D06은 "Service가 파일을 허용할 때만 표시"해야 하는데 Desktop에는 Service Definition을 조회할 Registry가 없다(D-034와 동일한 이유) — 파일 첨부 UI 자체를 만들지 않았다(항상 숨겨진 가짜 기능이 아니라 코드에 아예 없음). (3) **D07 상세 수준 분리**: "일반 사용자와 진단 권한 사용자가 보는 상세 수준을 분리한다"고 요구하지만, `RunDetailPanel.tsx`는 역할을 판별할 방법이 없어 항상 단일(진단) 수준만 보여주고 그 사실을 화면 상단 배너에 명시한다 | M04에 실제 역할/권한 모델(또는 최소한 `apps/portal-web`의 개발용 역할 전환기에 준하는 PoC 장치)이 도입되면 위 3곳 모두 재검토 — 특히 (1)은 `evaluation-result.schema.json`에 문서 발췌 필드가 추가되면 마스킹을 켜기로 한 D-056과 동일한 트리거 조건(발췌 필드 존재 + 권한 개념 존재)을 공유한다 | M01/M04/M05 |
| D-059 | agent-runtime의 CORS 정책이 Desktop D06/D07의 "렌더러에서 agent-runtime 직접 HTTP 호출" 아키텍처를 차단함 | **발견 (2026-08-08, D06 실제 브라우저 검증 중)**: `services/agent-runtime/src/agent_runtime/main.py`는 `CORSMiddleware`를 `allow_origins=["http://localhost:3000"]`(D-035, `apps/portal-web`의 Origin)로만 구성한다. Desktop Client의 Vite 개발 서버는 `http://localhost:5174`이고, 패키징된 Electron 앱은 `file://`에서 로드되어 opaque Origin(`Origin: null`)으로 요청을 보낸다 — 둘 다 저 allowlist에 없다. 실측: 브라우저(Playwright, `http://localhost:5174`)에서 `fetch('http://127.0.0.1:8100/health')`를 실행하면 서버가 실제로는 응답하는데도(동일 요청을 이 세션에서 `curl`로 실행하면 200 OK) 브라우저가 `TypeError: Failed to fetch`로 차단한다 — CORS 정책 때문이며 네트워크/타임아웃 문제가 아님을 확인했다. 이 세션은 이 정책이 살아있는 서비스(agent-runtime:8100, 재시작 금지 대상)의 소스이므로 CORS 목록을 수정하거나 서비스를 재시작하지 않았다(수정 시 `uv run uvicorn --reload`가 워커를 재기동시켜 "서비스 재시작 금지" 제약을 위반하게 된다). 대신 (a) 같은 브라우저에서 실행을 시도해 코드가 크래시 없이 한국어 오류 배너("실행 요청에 실패했습니다: Failed to fetch")로 우아하게 실패함을 확인했고 — 이는 D06의 Error 상태 요구를 실제로 충족한다 — (b) `curl`(브라우저가 아니므로 CORS 미적용)로 동일 Wire Contract를 직접 호출해 실제 스트리밍 답변·Citation·중간 취소가 전부 정상 동작함을 확인해, `ChatScreen.tsx`/`agentRuntime.ts`의 이벤트 처리 로직 자체는 올바르다는 것도 함께 검증했다. **결론**: D06/D07이 요구한 "렌더러에서 agent-runtime에 직접 HTTP/SSE로 접속" 아키텍처는 옳지만, agent-runtime의 현재 CORS 허용 목록이 Desktop Origin을 포함하지 않아 브라우저 기반 렌더러(개발 중 Vite뿐 아니라 패키징된 Electron의 `file://`/`null` Origin도 포함될 가능성이 높음)에서는 오늘 시점에 전혀 동작하지 않는다 | M05가 `allow_origins`에 Desktop이 실제로 쓰는 Origin(들)을 추가해야 한다 — 후보: 정확한 값 목록(Vite 개발 포트, Electron의 `null` Origin 처리 방식은 FastAPI CORSMiddleware가 `null` 문자열 자체를 허용 목록에 넣는 방식으로 대응 가능한지 별도 검증 필요) 확정, 또는 Hosted/Local 모드별로 다른 CORS 정책을 두는 방안(D-028 Hosted Runtime 배치와 연계). 실제 Electron 패키지에서 `file://` Origin이 정말 어떻게 나타나는지는 이 머신에서 Electron을 기동할 수 없어(XProtect 격리, 별도 기록됨) 실측하지 못했다 — Electron 기동이 가능한 환경에서 재확인 필요. **갱신 (2026-08-14): 이 전제가 뒤집혔다.** `node_modules`의 Electron v43.3.0 에 quarantine 속성이 없고 정상 실행되며 실제 창까지 뜬다(`scripts/macos/dev-stack.sh start desktop-client` 로 재현 가능). 즉 이 결정을 막고 있던 '실측 불가'는 더 이상 사실이 아니며, 패키징된 Electron 의 `file://`/`null` Origin 을 이 머신에서 직접 확인할 수 있다 — 다음 세션의 실행 가능한 후속 작업이다. 다만 개발 모드(Vite) 렌더러 Origin 은 `http://localhost:5173` 이다(문서에 오래 남아 있던 5174 는 `strictPort` 도입 이후로는 사실이 아니다) | M04/M05 |
| D-060 | Desktop에 설치된 Knowledge를 선택할 때 전달되는 식별자가 검색 인덱스 기준(AssetVersion id)과 다름 | **발견 (2026-08-08, D06 Knowledge 선택 UI 구현 중)**: Electron 브릿지(`window.desktop.listInstalledAssets()`)가 반환하는 `InstalledAsset.assetId`는 Offline Bundle의 `bundle-manifest.yaml`의 `included_assets[].asset_id`에서 그대로 온 값이고, 이는 `services/distribution-service/src/distribution_service/bundler.py`가 `item.asset_id`(부모 **Asset**.id)로 채운 것이다(디렉터리 구조도 `assets/knowledge/{asset_id}/{version}/...`로 Asset 단위다). 반면 `services/agent-runtime`이 실행에 쓰는 `knowledge_id`, 그리고 `services/search-runtime`의 검색 인덱스는 **AssetVersion**.id를 기준으로 저장된다(2026-08-03 세션 이력에서 실측 확인된 버그: 자산 id를 보내면 항상 `INSUFFICIENT_EVIDENCE`가 반환됨). 즉 `ChatScreen.tsx`가 Electron 브릿지 경로에서 설치된 Knowledge를 선택해 실행하면, 오늘 시점의 데이터 모델상 거의 항상 잘못된 식별자가 전달되어 지식 검색이 실패할 것으로 예상된다 — Electron을 이 머신에서 기동할 수 없어(D-059와 같은 제약) 실측은 못 했고, 화면에는 이 사실을 경고 문구로 명시했다(`ChatScreen.tsx`의 Knowledge 선택 영역). **해결 (2026-08-09)**: Contract부터 변경했다(CLAUDE.md 원칙 1) — `packages/schemas/manifests/bundle-manifest.schema.json` 신규(문서화 목적 계약, `api/knowledge-search.schema.json`/D-062와 동일한 전례로 `ai_asset_schemas.validator`의 `SchemaType`에는 연결하지 않음), `included_assets[].asset_version_id`를 정의하고 `asset_id`로 대체(fallback)하는 것을 명시적으로 금지하는 문구를 계약 자체에 남겼다. **Bundler**: `bundler.py`의 `collect()`가 이미 갖고 있던 `item.asset_version_id`(리비전 관리용으로 이미 존재했으나 `included_assets[]` 딕셔너리에는 쓰이지 않고 있었다)를 각 항목에 추가(디렉터리 레이아웃은 그대로 Asset id 기준 유지, 검색 식별자만 분리). STANDARD_LOCAL_COPY(D-034 표준 Agent/Prompt)는 애초에 AssetVersion 행이 없으므로 `null`로 정직하게 남긴다. **Desktop**: `electron/types.ts`의 `IncludedAssetSummary`/`InstalledAsset`에 각각 `asset_version_id`/`assetVersionId` 추가, `bundle-verify.ts`의 `ParsedManifest`도 동일 필드 추가(MANIFEST_SCHEMA 검증에는 필수로 넣지 않아 레거시 Bundle도 계속 반입 가능), `bundle-install.ts`가 `item.asset_version_id ?? null`로 정규화해 설치 레코드에 기록. **ChatScreen**: `chatTypes.ts`에 순수 함수 `resolveKnowledgeSelection`(assetVersionId 있으면 그 값을 knowledge_id로, 없으면 항상 빈 문자열 + `LEGACY_BUNDLE_KNOWLEDGE_ID_REASON` 사유 문자열을 반환하고 **asset_id로 대체하지 않음**)을 추가해 사용, Knowledge 선택 `<select>`의 `<option>` 자체를 assetVersionId 없는 항목에 대해 `disabled`로 렌더링하고 라벨에 "(다시 반출·설치 필요)"를 붙이며, 목록에 그런 항목이 있으면 그 이유(정확히 위 사유 문자열)와 대상 이름을 별도 경고 문구로 항상 표시한다(선택 자체를 막아도 "왜 막혔는지"는 항상 보이게). **검증**: `uv run pytest tests/ -q` 699 passed/92 deselected(+1, `test_bundler.py`에 asset_id≠asset_version_id를 서로 다른 값으로 강제하는 회귀 테스트 추가), `uv run ruff check .` 0건, `make contract-test` 23 passed, `make e2e-test` 15 passed/13 skipped, `make security-test` 63 passed/1 skipped(모두 기존과 동일), `pnpm --filter desktop-client test` 66 passed(+5: `chatTypes.test.ts` 3 신규 + `bundle-install.test.ts` 2 신규 — 신규 테스트 중 하나는 D-060 이전에 실제로 생성된 레거시 실물 Bundle(`valid-bundle.zip`)을 그대로 반입해 `assetVersionId`가 정직하게 `null`로 남는지, 다른 하나는 이번 수정 이후의 코드로 새로 만든 실물 Bundle(`valid-bundle-with-version-id.zip`, distribution_service.bundler를 스크래치패드에서 직접 호출해 생성)을 반입해 `assetVersionId`가 `asset_id`와 다른 실제 AssetVersion id로 채워지는지 각각 실측), `pnpm --filter desktop-client typecheck`/`pnpm --filter portal-web typecheck` 모두 clean. 라이브 :8400/기타 서비스는 재기동하지 않았다(스크래치패드에서 `distribution_service.bundler`를 직접 호출해 실물 Bundle 생성). Service/Agent/Prompt 항목도 동일 Contract로 함께 해소됨(`asset_version_id`가 root/service 항목에도 채워짐을 위 실측 Bundle로 확인) | 해결 완료. 남은 것은 실 Electron 환경(D-059 제약으로 이 머신에서 기동 불가)에서 (1) 실제로 지식 검색 결과가 나오는지, (2) 레거시 Bundle이 설치된 상태에서 드롭다운이 실제로 비활성 옵션으로 렌더링되는지 사람이 최종 확인하는 것뿐 | M03/M04/M05 |
| D-061 | `WAITING_FOR_USER` Run 상태 구현 (D-052 잔여 항목) | **DECIDED (2026-08-08)**: D-052 (4)가 미룬 `WAITING_FOR_USER`(02-desktop-and-agent-runtime.md §5.3)를 구현해, §8.4 `ALWAYS`/`ON_PARAMETER` MCP Tool(현재는 `table_count.query`)이 `MCP_CONFIRMATION_REQUIRED`로 항상 거부되던 제약을 풀었다. **상태 머신**(`services/agent-runtime/src/agent_runtime/workflow.py` `_run_mcp_tool_call`/`_await_confirmation`): 확인이 필요한 미확인 호출은 RUNNING -> WAITING_FOR_USER로 전이하며 `mcp.confirmation_required`(tool_name/summary/deadline) SSE Event를 방출한다. 이후 `POST /local/v1/runs/{run_id}/confirm`(신규, `{"decision": "approve"\|"deny"}`)이 오면 `mcp.confirmation_resolved` Event와 함께 RUNNING으로 복귀 — **approve**는 `confirmed=true`를 실은 기존 검증 경로(허용목록+로컬 Schema 검사, office-mcp-server 자체 §8.4 재검증)를 그대로 타서 실제 호출하고, **deny**는 Tool을 전혀 호출하지 않고 "MCP 요청이 아예 없었던 것"처럼 계속 진행해 D-036 Hallucination Guard가 결정하게 한다(다른 근거가 있으면 SUCCEEDED, 없으면 INSUFFICIENT_EVIDENCE) — **"Denial은 FAILED가 아니다"**가 핵심 설계 결정이다. **만료**(§5.3 "무한 대기를 방지하기 위해 만료시간을 가진다"): 신규 설정 `AgentRuntimeSettings.mcp_confirmation_timeout_seconds`(기본 120초, `AGENT_RUNTIME_MCP_CONFIRMATION_TIMEOUT_SECONDS`로 override, `config.py`) 경과 시 `mcp.confirmation_expired` Event 후 `run.failed`(`MCP_CONFIRMATION_TIMEOUT`)로 확정 종료한다 — main.py가 최근에 겪은 "설정은 있는데 하드코딩이 이를 무시한 버그"를 반복하지 않도록 `routers/runs.py`가 매 호출마다 설정값을 그대로 읽어 `run_knowledge_chat`에 넘긴다. 대기 중에도 기존 `POST /runs/{id}/cancel`이 그대로 동작한다(`RunRecord.cancel_event`를 확인 대기와 함께 `asyncio.wait`). **안전한 요약**: 확인 Panel에 표시되는 `summary`는 `schema`/`table` 같은 구조적 식별자만 담고 `filters`의 실제 값은 절대 포함하지 않는다(개수만 표기) — D-052가 이미 지켰던 "MCP Event는 원본 Tool 입력/결과를 담지 않는다" 성질의 연장. **Contract**: `packages/schemas/api/local-runtime-api.yaml`에 `POST /runs/{run_id}/confirm`(`ConfirmRunRequest`), `RunResponse.pending_confirmation`(tool_name/summary/deadline), `RuntimeSSEEvent.event` enum에 `mcp.confirmation_required`/`mcp.confirmation_resolved`/`mcp.confirmation_expired` 3종을 추가(`WAITING_FOR_USER`는 이미 D-052 이전에 계약에 있었고 구현만 없었다). **이중 방어선 유지**: office-mcp-server(M10)는 전혀 수정하지 않았다 — approve 경로도 여전히 자기 자신의 §8.4 정책을 독립적으로 재검사한다. `standard-agent`(`mcp_allowed:false`, 게시된 4개 챗봇)는 완전히 그대로 — 이 Workflow 분기 자체에 진입하지 않는다. **Desktop D06 확인 Panel**: `apps/desktop-client/src/screens/ConfirmationPanel.tsx`(신규) — Tool 이름·안전한 요약·1초 단위 남은 시간 카운트다운·승인/거부 버튼, 대기 중에도 기존 취소 버튼이 함께 노출된다. `runStages.ts`에 `StageState`로 `"waiting"`을 추가하고 `mcp.confirmation_*`/`mcp.call.*` Event를 반영하도록 상태 머신을 확장(단위 테스트 4개 추가). D06에는 여전히 정식 MCP Tool 선택 UI가 없으므로(Service Registry 부재, D-058과 동일한 이유) 확인 Panel 자체를 검증할 수 있도록 "개발 확인용" 트리거(agent_profile=standard-db-agent + Tool/Schema/Table 입력, Electron 브릿지가 없을 때만 노출)만 추가했다 — 정식 제품 UI가 아님을 라벨로 명시. **검증**: `uv run pytest tests/ -q` 596 passed/91 deselected(+6, 기존 590 유지), `uv run ruff check .` 0건, `make security-test` 63/1 그대로. **`make e2e-test`는 정직하게 기록한다 — 기본 실행(라이브 :8100 대상)은 이제 2 failed/15 passed/11 skipped다**: `test_e2e_02_mcp_service.py`의 두 테스트를 새(올바른) `WAITING_FOR_USER` 동작을 기대하도록 갱신했는데, 라이브 :8100은 이번 세션에서 재기동하지 않은 구코드 그대로라 `WAITING_FOR_USER`에 진입하지 못하고 즉시 `MCP_CONFIRMATION_REQUIRED`로 실패해 `wait_for_run_status(..., "WAITING_FOR_USER")`가 30초 뒤 `TimeoutError`를 던진다 — 이는 테스트/구현의 결함이 아니라 "재기동 금지" 제약과 "이제는 옳은 동작을 요구하는 테스트로 갱신함"이 만나 생기는 예상된 결과다(구코드를 계속 통과시키려고 테스트를 옛 동작에 맞춰 두는 것은 CLAUDE.md 원칙 5 위반이므로 택하지 않았다). 별도로 띄운 agent-runtime:8102 인스턴스(라이브 office-mcp-server:8500/search-runtime:8300/portal-api:8000은 그대로 사용)에 `E2E_AGENT_RUNTIME_URL=http://127.0.0.1:8102 make e2e-test`로 재실행하면 **17 passed/11 skipped**(16 기준선 +1, 신규 `test_e2e_02_user_denies_confirmation_and_run_ends_cleanly` 포함)로 전부 통과 — 라이브 :8100을 재기동하는 순간 기본 `make e2e-test`도 동일하게 17/11이 될 것으로 예상된다(세션 소유자가 재기동 후 재확인 필요, 아래 후속 컬럼에도 기록). 실 office-mcp-server(:8500)에 대해 승인 시 실제 Tool 호출·SUCCEEDED, 거부 시 Tool 미호출·INSUFFICIENT_EVIDENCE(error 없음), 별도 3초 타임아웃 인스턴스로 만료 시 FAILED/`MCP_CONFIRMATION_TIMEOUT`을 `curl`로 직접 확인. Playwright로 Vite(:5174, `VITE_AGENT_RUNTIME_BASE_URL`을 임시 `.env.local`로 :8102를 가리키게 함, 검증 후 삭제)에서 확인 Panel 렌더링·승인(성공)·거부(INSUFFICIENT_EVIDENCE) 실제 화면 전환을 확인. 라이브 :8100/:8500/:5174/:3000/:8000 프로세스는 재기동하지 않았다(끝난 뒤 8102/8103 임시 인스턴스만 종료) | `services/office-mcp-server`의 §8.4 Manifest Fixture 2건(D-049/D-052 잔여)이 여전히 없음, `mcp_tools.py`의 손 복사 Schema Drift 위험은 그대로(D-052와 동일), Desktop 정식 Tool 선택 UI는 Service Registry 도입 후에야 가능(D-058). **라이브 agent-runtime:8100을 재기동하면 `make e2e-test`가 즉시 16/11(구코드 통과)에서 2 failed로 바뀌었다가, 재기동 후에는 17/11로 정상화될 것으로 예상됨** — 세션 소유자가 재기동 후 `make e2e-test` 재실행으로 확인 필요 | M04/M05/M10 |
| D-062 | Knowledge Search Filter/ACL (04-knowledge-platform.md §3.8, M11 "분류등급 기반 접근 제어" 공백 해소) | **DECIDED (2026-08-08)**: `services/search-runtime`(M08)의 `SearchRequest`가 `metadata_filters: dict`를 호출자가 완전히 통제하는 자유 필드로 노출하면서 ACL이 전혀 없던 상태(main.py가 이 필드를 검색에 전혀 반영조차 하지 않던 죽은 필드였음)를 해소했다. **§3.8 5단계를 정확히 이 순서로 구현**(전부 `services/search-runtime/src/search_runtime/access_control.py`, `main.py`가 호출): (1) `validate_access_context` — `access_context`가 아예 없으면 최소 권한(`PUBLIC_INTERNAL`)으로 기본 처리(하위 호환, "검사 생략"이 아니라 "가장 낮은 등급만 통과"), 있는데 `clearance`가 없거나 알 수 없는 값이면 통합 버그로 간주해 `KNOWLEDGE_ACCESS_DENIED`로 거부. (2) `forced_allowed_classifications` — 검증된 clearance와 서비스 설정(`ALLOW_UNKNOWN_CLASSIFICATION`)만으로 계산, 요청 Body에서 오는 경로가 전혀 없어 이것이 "덮어쓸 수 없음"의 근거. (3)/(4) Profile Default Filter(`retrieval_profile.metadata_filters`, D-053 방식의 caller가 직접 넘기는 이미 해석된 Profile dict — Profile Registry가 없으므로)와 사용자 `metadata_filters`. (5) `merge_business_filters` — 동일 필드에 서로 다른 값이 오면(예: `department` HR vs Legal) 그 필드는 어떤 값도 통과 못 하는 공집합이 된다(둘 다 동시에 만족해야 하므로 이것이 논리적으로 가장 제한적인 해). **강제 ACL Filter의 불변성**: `reject_acl_override`가 `metadata_filters`와 `retrieval_profile.metadata_filters` 양쪽에서 `classification`(중앙 정의는 `packages/security-policy.ACL_METADATA_FIELDS`) 키를 발견하면 조용히 버리지 않고 `KNOWLEDGE_ACCESS_DENIED`(`details.reason=acl_field_override_attempted`)로 요청 자체를 거부한다. **Classification 5단계**(`packages/security_policy.Classification`: PUBLIC_INTERNAL/INTERNAL/CONFIDENTIAL/RESTRICTED + UNKNOWN)는 M11 소유의 공개 API로 M07/M08 양쪽이 내부 폴더 Import 없이 패키지로 소비한다(CLAUDE.md 원칙 2). `services/indexing-runtime`은 색인 시점에 Knowledge Manifest의 `classification`(포털 업로드 원문 값 — `Asset.classification` DB 컬럼이 아님, 그 컬럼은 별개의 "미기재 시 INTERNAL" 기본값을 이미 갖고 있어 이 기능과 섞으면 안 됨)을 `index-meta.json`과 모든 Child/Parent Chunk Metadata에 스탬프하며, 없거나 인식 불가한 값은 **절대 INTERNAL 등으로 추측하지 않고** UNKNOWN으로 기록한다(`pipeline.run_pipeline`). **UNKNOWN/레거시 인덱스 처리**: `search_runtime.settings.ALLOW_UNKNOWN_CLASSIFICATION`(env `SEARCH_ALLOW_UNKNOWN_CLASSIFICATION`, 기본 `False`) — UNKNOWN Chunk는 기본적으로 **모든 호출자에게 차단**(Fail-closed). 이는 이번 기능 이전에 만들어진 인덱스 전부(게시된 4개 챗봇 `remote-work-guide`/`langchain`/`remote-work-approved`/`unapproved-test` 포함)에 실제 동작 변화를 일으킨다 — 이 서비스들을 재기동하는 순간 검색 결과가 0건이 된다(재시작 금지 제약으로 이번 세션에서는 재기동하지 않았으므로 현재 라이브 데모는 계속 정상 동작 중임을 `make e2e-test`/`make security-test` 재실행으로 확인함). **운영자 조치(필수, 재기동 전에)**: 신규 `stamp-classification` CLI(`services/indexing-runtime/src/indexing_runtime/stamp_classification.py`, `pyproject.toml`의 `[project.scripts]`로 등록)를 각 인덱스 디렉터리에 대해 실행 — `stamp-classification data/indexes/<asset_version_id> --classification INTERNAL`(또는 실제 등급) — `index-meta.json`과 `bm25.pkl`의 모든 Chunk Metadata(search-runtime의 실제 읽기 경로), 존재하면 Chroma Collection Metadata까지 재임베딩 없이 제자리에서 갱신한다(idempotent). `ALLOW_UNKNOWN_CLASSIFICATION=true`로 우회하는 것도 가능하지만 이는 "레거시 콘텐츠를 전원에게 공개해도 좋다"는 배포 단위의 명시적 결정이어야 하며 기본값이어서는 안 된다 — 조용히 허용하는 것은 이 기능이 막으려는 바로 그 상황이다. **정직성 한계(중요)**: 이 PoC에는 신원 계층이 없다(D-015 Mock User Context). `access_context.clearance`는 호출자(agent-runtime, evaluation-runner, 또는 이 HTTP 엔드포인트에 네트워크로 접근 가능한 누구든)가 **주장**하는 값이며 실제 세션/인증과 대조 검증되지 않는다. 이번 작업은 주장된 Clearance가 요청 Body를 통해 조용히 상향될 수 없도록 만드는 **메커니즘**을 제공할 뿐, 신원 계층이 생기기 전까지는 "인가된 사용자만 열람"을 보장하는 완전한 통제가 아니다 — `access_control.py` 모듈 Docstring과 `search_runtime.settings.ALLOW_UNKNOWN_CLASSIFICATION` 옆에도 동일하게 명시했다. `services/agent-runtime`(M05, 유일한 실제 호출자)은 `StartRunRequest.user_context`(호출 서비스가 채우는 신뢰된 필드, 채팅 질문 본문 `input`이 아님)의 `clearance`를 그대로 전달하고 없으면 `AgentRuntimeSettings.default_search_clearance`(기본 `INTERNAL` — 데모 Knowledge 기본 분류·게시된 챗봇과의 연속성 근거를 설정 자체에 문서화)로 대체한다. `packages/evaluation-runner`의 `HttpSearchClient`는 전체 코퍼스 품질 측정이 목적이므로 기본 `RESTRICTED`(모든 등급 통과)를 주장한다(override 가능). **Contract**: `packages/schemas/api/knowledge-search.schema.json`에 `access_context`(clearance enum 4종)·`retrieval_profile`·`metadata_filters` 설명을 추가(이 스키마는 `ai_asset_schemas.validator`에 연결되어 있지 않아 Contract Test 대상은 아니었고 이번에도 아님 — 문서화 목적의 계약). 오류코드는 신규 발명 없이 07-data-api-contracts.md §8의 기존 `KNOWLEDGE_ACCESS_DENIED`를 재사용(ACL 재정의 시도와 잘못된 access_context 둘 다 "접근 권한 없음"의 인스턴스로 봄). **검증**: `uv run pytest tests/ -q -m "not e2e and not security"` 650 passed/92 deselected(+54: security_policy Classification 16, indexing_runtime stamping+CLI 10, search_runtime access_control+ACL filtering 27, evaluation_runner access_context 1), `uv run ruff check .` 0건, `make contract-test` 20 passed(그대로), `make security-test` 63 passed/1 skipped(그대로), `make e2e-test` 15 passed/13 skipped(그대로, 라이브 :8300/:8200 미재기동이므로 예상대로 구코드 그대로 통과). 라이브 :8302/:8202에 이 세션만의 인스턴스를 띄우고 스크래치패드 복사본(`data/indexes/`는 손대지 않음)으로 실측: CONFIDENTIAL로 표시한 Chunk가 INTERNAL Clearance 호출자에게서 실제로 빠지고 CONFIDENTIAL Clearance에는 나타남, `metadata_filters`/`retrieval_profile.metadata_filters`에 `classification`을 넣은 요청이 403 `KNOWLEDGE_ACCESS_DENIED`(`acl_field_override_attempted`)로 거부됨, 레거시(UNKNOWN) 복사본이 기본 정책에서는 0건·`ALLOW_UNKNOWN_CLASSIFICATION=true`에서는 4건 모두 반환됨, 같은 레거시 복사본에 `stamp-classification --classification INTERNAL` 실행 후에는 기본(Fail-closed) 정책에서도 4건 모두 반환됨을 모두 `curl` 실측으로 확인. 검증 인스턴스·스크래치패드 파일은 종료/삭제 완료, 라이브 :3000/:8000/:8100/:8200/:8300/:8500 프로세스는 재기동하지 않았다 | 실 신원 계층 도입(D-001 Portal SSO/OIDC 연동, D-015 MCP Mock User Context 해소)과 함께 `access_context.clearance`를 세션에서 검증된 값으로 교체, 게시된 4개 챗봇·기존 인덱스에 대한 `stamp-classification` 실제 실행(재기동 포함, 세션 소유자가 별도로 수행), Chroma `where=`를 통한 Vector Store 단의 Push-down 필터링(현재는 §3.5 "가능한 경우"를 PoC 규모에서 미적용으로 판단, hybrid.py 주석 참고), 조직·사업장 범위 필터(D-022)와의 통합 | M05/M07/M08/M11 |
| D-063 | ServiceVersion 자체 검토 체인 구현 + 게시 Gate 강제 여부 (D-041 후속) | **DECIDED (2026-08-08)**: `routers/reviews.py`의 AssetVersion TECHNICAL→SECURITY→RELEASE 체인을 **포크하지 않고 그대로 재사용**해 ServiceVersion에도 적용했다. 신규 `POST /service-versions/{id}/submit`(기존 `submit_asset_version`과 동일한 구조, `Permission.SERVICE_SUBMIT_REVIEW`만 다름 — CREATOR 소유자/ADMIN)이 `ReviewRequest(subject_type=SERVICE_VERSION)`을 연다. `decide_review`/`cancel_review`는 신규 `_load_review_subject()` 헬퍼로 `subject_type`에 따라 `AssetVersion`/`ServiceVersion` 중 알맞은 테이블을 읽도록 일반화했을 뿐, RBAC(`STAGE_PERMISSION`)·순차 단계 강제·"사유 필수"·"PENDING 아닌 재결정 차단"·감사 이벤트는 완전히 동일한 코드 경로를 그대로 탄다. `service_versions`에 Alembic 마이그레이션(`ce52d6d1f75a`, `make migration`/`make migrate`)으로 `approved_at`/`updated_at`을 추가(기존 6개 행은 `server_default=CURRENT_TIMESTAMP`로 backfill, 상태는 모두 DRAFT로 보존됨을 재확인). **게시 Gate는 설정으로 존재하되 기본값은 OFF — 의도된 PoC 결정**: 신규 `Settings.require_service_version_approval: bool = False`(`portal_api/config.py`, env `PORTAL_REQUIRE_SERVICE_VERSION_APPROVAL`)를 `routers/services.py::_run_validation`이 읽어 `service_version_review_status`라는 새 체크 항목을 추가한다 — `True`일 때만 ServiceVersion.status != APPROVED를 `DEPLOYMENT_VALIDATION_FAILED`(기존 오류코드 재사용, 신규 발명 없음)로 차단하고, `False`(기본)일 때는 절대 차단하지 않지만 체크 자체는 항상 응답에 포함되어 실제 검토 상태(예: "DRAFT — 아직 검토·승인되지 않았습니다")를 숨기지 않는다. 이 게이트는 `POST /deployments/{id}/publish`와 `POST /deployments/{id}/resume`(재개 시 재검증) 양쪽에 동일하게 적용된다. **기본값이 False인 이유**: CLAUDE.md가 명시한 발표 MVP 흐름("등록된 Knowledge로 챗봇을 빠르게 구성하고, 실제 Knowledge Preview 테스트 후 내부 URL로 게시")은 즉시 게시를 전제하며, 이미 게시되어 있는 4개 ACTIVE 데모 챗봇(`remote-work-guide`/`langchain`/`remote-work-approved`/`chatbot-chura7`, 전부 ServiceVersion.status=DRAFT)이 이 기능을 켜는 순간 전부 재게시 불가 상태가 된다 — 운영 전환 시 반드시 `True`로 켜야 한다. **Contract**: `packages/schemas/api/portal-openapi.yaml`에 `POST /service-versions/{id}/submit` 경로 추가, `ValidationResult.checks[].code` 필드 문서화(구현에는 있었지만 계약에 누락되어 있던 기존 drift도 함께 수정), `ServiceVersion`/`ServiceVersionSummary`/`ServiceVersionDetail`에 `approved_at` 추가, `publish_deployment` 설명에 이 Gate 명시. `security_policy.roles`에 `Permission.SERVICE_SUBMIT_REVIEW` 추가(CREATOR 보유) — `ASSET_SUBMIT_REVIEW`를 재사용하지 않고 별도로 둔 이유는 향후 두 권한이 다른 역할로 분리될 가능성을 열어두기 위함. UI: `/reviews`가 더 이상 `subject_type=ASSET_VERSION`으로 하드코딩 필터링하지 않음(SERVICE_VERSION도 같은 목록에 표시, 유형 드롭다운으로 좁힐 수 있음), `/reviews/[id]`가 두 subject_type을 모두 정확한 한국어 라벨로 렌더링, `/services/[versionId]`에 소유자용 "검토 요청" 액션 추가(비활성 사유 항상 표시). **검증**: `uv run pytest tests/ -q` 662 passed/92 deselected(+12, 기존 650 그대로 유지), `uv run ruff check .` 0건, `make contract-test` 20 passed(그대로), `make e2e-test` 15 passed/13 skipped(그대로 — `resume_deployment`의 `_run_validation` 호출 인자 변경이 라이브 :8000의 suspend/resume E2E를 깨지 않음을 실측 확인), `make security-test` 63 passed/1 skipped(그대로). 라이브 검증은 재시작 금지된 :8000 대신 이 세션 전용 :8003 인스턴스(포트만 다르고 동일한 `portal.db` 사용)에서 실행: throwaway 서비스를 만들어 submit→TECHNICAL(APPROVE)→SECURITY(APPROVE)→RELEASE(APPROVE)까지 전 구간을 curl로 실측(최종 `status=APPROVED`, `approved_at` 채워짐 확인), SECURITY_REVIEWER가 TECHNICAL 단계를 결정하려는 시도가 403 PERMISSION_DENIED로 거부됨을 확인, 플래그 OFF에서 미검토(DRAFT) ServiceVersion 게시가 202로 성공(검증 응답에는 "아직 검토·승인되지 않았습니다" 메시지 포함)함과 플래그를 `PORTAL_REQUIRE_SERVICE_VERSION_APPROVAL=true`로 켠 별도 :8003 재기동에서는 동일한 미검토 버전 게시가 400 `DEPLOYMENT_VALIDATION_FAILED`로 거부되고 이미 APPROVED된 버전은 여전히 202로 성공함을 모두 확인. 검증에 사용한 throwaway Service/ServiceVersion/Deployment/ReviewRequest/ReviewDecision/AuditEvent는 모두 DB에서 직접 삭제해 정리했고, 세션 시작 시점과 동일한 6개 `service_versions`(전부 DRAFT)·5개 데모 `service_deployments`가 그대로임을 재확인했다. :8003 인스턴스는 검증 후 종료했다 | 운영 전환 시 `PORTAL_REQUIRE_SERVICE_VERSION_APPROVAL=true`로 전환하고 4개 데모 챗봇을 실제로 검토·승인 처리하는 마이그레이션 절차 필요, ReviewRequest `assigned_to` 자동 배정과 동시성 제어는 D-041 본문과 동일하게 여전히 미해결 | M01/M02/M11 |
| D-064 | P13 다운로드 이력의 Client IP 최소 감사값 (01-portal-and-distribution.md §2 P13) | **DECIDED (2026-08-09)**: 스펙 문구 "Client IP 또는 기기 정보의 최소 감사값"에서 기기 정보(User-Agent 등 Fingerprinting)는 채택하지 않고 **Client IP만, 그리고 딱 한 곳에만** 기록한다 — `routers/distributions.py::download_distribution`(`GET /api/v1/distributions/{id}/download`)이 `request.client.host`를 그 요청이 남기는 4개 `AuditEvent`(성공/미준비 실패/회수 실패/타인 다운로드 거부) 각각의 `metadata.client_ip`에만 넣는다. **기존 스키마 변경 없음** — 신규 컬럼이 아니라 이미 있던 `audit_events.metadata`(JSON) 안에 키 하나를 추가하는 것뿐이라 Alembic 마이그레이션이 필요 없었다. **의도적으로 하지 않은 것**: (1) `DistributionRequest`(Bundle 요청 그 자체) 행에는 IP를 절대 기록하지 않는다 — 요청 생성(`POST /distributions`)과 실제 파일 전송(`GET .../download`)은 별개 행위이고 스펙이 "다운로드"의 감사값을 요구하므로 다운로드 시점에만 남긴다. (2) User-Agent/기기 Fingerprint는 추가하지 않는다 — CLAUDE.md 로그 규칙("Log에... 기본 저장하지 않는다"의 최소주의)과 스펙의 "또는"(OR) 문구가 이미 IP 하나로 충분함을 시사한다. (3) 애플리케이션 로그(logger.info 등)에는 IP를 쓰지 않는다 — 이 값은 감사(Audit) 전용이며 CLAUDE.md가 구분하는 두 저장소(운영 로그 vs 감사 이벤트) 중 감사 이벤트에만 존재한다. (4) `rbac.py`의 범용 `PERMISSION_DENIED:DOWNLOAD_READ`(역할 자체가 권한이 없는 경우) 경로는 손대지 않았다 — 그 이벤트는 목록/상세 조회에서도 발생해 "다운로드 시도"로 단정할 수 없으므로 P13 이력 병합 대상에서도 제외했다(같은 이유로 IP도 넣지 않음). **하위 호환**: 이 기능 이전에 기록된 `BUNDLE_DOWNLOADED` 행은 `metadata`에 `client_ip` 키 자체가 없다 — `GET /api/v1/distributions/download-history`는 이를 `None`으로 반환하고 Portal은 "미기재"로 렌더링한다(추측하거나 빈 문자열로 채우지 않음, 실제 라이브 `portal.db`의 2026-08-03 다운로드 행으로 실측 확인). | 운영 전환 시 프록시/로드밸런서 뒤에서는 `request.client.host`가 프록시 자신의 주소가 될 수 있어 `X-Forwarded-For` 신뢰 체인 정책이 필요함(현재 D-001 PoC 환경엔 프록시가 없어 범위 밖) | M02/M11 |
| D-065 | P15 관리자 설정 편집 기능 미구현 (01-portal-and-distribution.md §2 P15) | **DECIDED (2026-08-09)**: P15는 8개 하위 화면(사용자·역할 매핑/조직·사업장/Office Profile/허용 모델과 Endpoint Alias/자산 크기·확장자 정책/승인 Workflow/보안등급과 보관기간/Package Trust·Signature 설정)을 요구하지만, 이 PoC에는 `users`/`sites`/`roles` 테이블이 어디에도 없다. 저장소가 없는 값에 대한 편집 폼을 만드는 것은 동작하는 것처럼 보이지만 실제로는 아무것도 저장하지 않는 화면이 되어 CLAUDE.md 원칙 5("테스트 증거 없는 기능은 완료로 표시하지 않는다")와 정면으로 충돌한다. 대신 `GET /api/v1/admin/settings`(`portal_api/routers/admin.py`)+`/admin/settings`(Portal)를 **읽기 전용 "정책·구성 현황"** 화면으로 구현했다 — 8개 하위 영역 각각을 AVAILABLE(실제 설정/정책 파일을 요청 시점에 그대로 읽어 출처와 함께 표시) 또는 NOT_IMPLEMENTED(사유 + 이 결정 id)로만 보고하며, 빈 폼이나 그럴듯한 placeholder는 만들지 않는다. **저장소 부재로 편집 기능이 없는 하위 영역**: (1) 사용자·역할 매핑 — 역할별 권한 매트릭스(`security_policy.ROLE_PERMISSIONS`)는 실제로 표시하지만, 어떤 사용자가 어떤 역할을 가졌는지 저장하는 사용자 디렉터리가 없다(Test Identity Adapter가 Bearer Token→역할을 매 요청 고정 매핑할 뿐). (2) 조직·사업장 — 조직·사업장을 등록·관리하는 테이블이 없다. 사업장 id는 `distribution_requests` 등 개별 행의 자유 문자열로만 존재하고, `office-profile.json`의 `sites` 배열은 그 프로필이 적용되는 사업장 라벨을 선언할 뿐 조직·사업장 자체의 저장소가 아니다(Office Profile 절에는 참고용으로 그대로 노출됨). (3) 보안등급과 보관기간 중 **보관기간**만 — 보안등급(Classification 5단계, ACL_METADATA_FIELDS)은 `security_policy.classification`으로 실제 구현되어 있지만(D-062), 문서/Audit 보관기간을 저장·집행하는 코드나 설정은 어디에도 없다(`office-profile.json`의 `audit_retention_days`는 선언값일 뿐 실제 삭제/보관을 집행하지 않음). (4) Package Trust/Signature 설정 — PKI/서명키 자체가 없다(D-016: Checksum만 필수, Signature Hook만 존재; D-048: Desktop Installer 코드 서명 인증서도 미발급) — 관리할 Trust Store 대상이 없다. **실제로 AVAILABLE로 구현된 4.5개 영역**: Office Profile/허용 모델·Endpoint Alias(`services/agent-runtime/config/office-profile-default/office-profile.json`을 요청마다 재읽기), 자산 크기·확장자 정책(`packages/knowledge-packager/config/package-policy.yaml`을 `yaml.safe_load`로 직접 읽음 — `knowledge_packager` 패키지 자체는 의존성으로 추가하지 않음, 그 패키지가 끌어오는 `chromadb`가 정책 YAML 하나 읽자고 붙이기엔 너무 무거운 무관한 의존성이라 판단; `apps/desktop-client/electron/bundle-verify.ts`의 `ARCHIVE_EXTENSIONS`/`EXECUTABLE_EXTENSIONS`/`DEFAULT_SIZE_CAP_OPTIONS` 리터럴을 정규식으로 파싱 — TypeScript를 Python이 Import할 수 없어 선택한 방법이며, 형식이 바뀌면 값을 추측하지 않고 `parse_error`로 보고. **후속(2026-08-09, 리팩터)**: 이 정규식 파싱은 CLAUDE.md 원칙 2/3 위반으로 판단되어 제거했다 — 다른 모듈의 소스 코드 "포맷"에 결합되는 것은 Import보다도 더 깨지기 쉽다(M04가 리포맷/리팩터만 해도 이 화면이 조용히 오류 상태로 전락). 대신 정책 값 자체를 `packages/schemas/policies/bundle-install-policy.json`(+ `bundle-install-policy.schema.json`)으로 옮겨 M02/M04가 각자 그 데이터 파일을 직접 읽는 구조로 바꿨다 — 값 자체는 완전히 동일(2GB/500MB/ratio 200). `bundle-verify.ts`는 이제 이 JSON을 모듈 로드 시점에 한 번 읽어 `ARCHIVE_EXTENSIONS`/`EXECUTABLE_EXTENSIONS`/`DEFAULT_SIZE_CAP_OPTIONS`를 채운다(내보내는 이름·형태는 그대로 유지) — 컴파일 결과물(`dist/electron/`)과 Vitest 실행(`electron/`)이 서로 다른 디렉터리 깊이를 가지므로 고정된 상대경로 대신 `pnpm-workspace.yaml`을 찾아 올라가는 `findRepoRoot` 헬퍼로 두 경우 모두 올바르게 경로를 찾는다. `_read_desktop_bundle_policy`(`routers/admin.py`)는 이제 정규식 3개와 `_eval_byte_expr` 없이 `json.load`로 같은 파일을 직접 읽는다. **검증**: `pnpm --filter desktop-client test` 61 passed(기존 56 + 신규 5, 공유 JSON과 TS 소비자가 일치하는지 확인하는 회귀 테스트 포함) / `typecheck` clean, `uv run pytest tests/ -q` 698 passed/92 deselected(+3: 신규 Contract Test), `uv run ruff check .` 0건, `make contract-test` 23 passed(+3), `make e2e-test` 15 passed/13 skipped(그대로), `make security-test` 63 passed/1 skipped(그대로), `pnpm --filter portal-web typecheck` clean. 라이브 :8003 인스턴스로 P15 재조회해 2GB/500MB/ratio 200 값과 `parse_error: null`이 그대로임을 확인 후 종료), 승인 Workflow(`security_policy.review`의 TECHNICAL→SECURITY→RELEASE 체인 + `Settings.require_service_version_approval` 게이트 현재값, D-063), 보안등급(위 (3) 참고). **Secret 비노출**: 스펙의 "실제 Secret 값은 화면에서 재표시하지 않는다"를 위해 신규 `security_policy.redact_if_secret`/`looks_like_secret`(`packages/security-policy/src/security_policy/redaction.py`)을 추가해 Office Profile의 모든 endpoint 문자열에 방어적으로 적용했다. `knowledge_packager.scanner.sanitize_text`가 이미 유사한 기능을 갖고 있었지만 그 함수는 `knowledge_packager.__init__`의 공개 계약(`__all__`)에 없어 재사용은 CLAUDE.md 원칙 2(모듈 간 내부 폴더 직접 Import 금지) 위반이었을 것이다 — 대신 M11(`security_policy`, 이미 RBAC/Classification 정책을 소유)에 작고 독립적인 헬퍼를 새로 두었다(패턴 자체는 두 파일이 유사하지만 목적이 다르다: `knowledge_packager`는 Package 빌드 바이트 스캔용, 이건 화면에 렌더링하기 직전 짧은 설정 필드 값 검사용). **RBAC**: 신규 `Permission.ADMIN_SETTINGS_READ`(ADMIN 전용, `ROLE_PERMISSIONS`의 다른 어떤 역할에도 나열되지 않고 `_ALL_PERMISSIONS`를 통해서만 ADMIN에 도달) — 거부 시 다른 모든 endpoint와 동일하게 `PERMISSION_DENIED:ADMIN_SETTINGS_READ` DENIED `AuditEvent`가 기록된다(성공 조회는 별도 감사 행을 남기지 않음 — `list_audit_events`/`list_asset_version_lifecycle`과 동일 관례). **Nav**: `app/_components/nav-links.tsx`의 "관리 (준비 중)" 플레이스홀더 3개 중 "시스템 설정"을 제거하고 거버넌스 섹션에 실제 `/admin/settings` 링크("관리자 설정")로 대체했다(데이터셋/사용자 관리는 여전히 준비 중으로 남음). **검증**: `uv run pytest tests/ -q` 695 passed/92 deselected(+23: security_policy redaction 5 + roles 권한 매트릭스 회귀 확인 포함, portal_api admin settings 통합 테스트 8), `uv run ruff check .` 0건, `make contract-test` 20 passed(그대로), `pnpm --filter portal-web typecheck` clean. 라이브 :8003 인스턴스로 ADMIN 실제 조회(7-역할 매트릭스·`miracom-default` Office Profile·2GB/500MB Desktop 정책·TECHNICAL→SECURITY→RELEASE 체인 모두 실측 확인), 5개 비-ADMIN 역할 전원 403+감사 기록, 4개 NOT_IMPLEMENTED 섹션 모두 사유+결정 id와 함께 노출을 curl로 확인 후 인스턴스 종료. 세션 시작 시점과 동일한 `portal.db`(distribution_requests 17/assets 4/service_deployments 5/service_versions 6)임을 재확인 | **UPDATE (2026-08-10): P15는 더 이상 전체가 읽기 전용이 아니다.** "인덱싱 임베딩 모델"(D-075가 신설한 `INDEXING_EMBED_MODEL`/`SEARCH_EMBED_MODEL` env 설정) 하나만 저장소가 생겨(신규 `platform_settings` key/value 테이블, Alembic 리비전 `eba969aa8e91`, 특정 설정 전용이 아니라 향후 다른 소규모 admin 설정도 재사용 가능한 일반 저장소) 실제로 편집 가능해졌다 — 위 (1)~(4)의 "저장소 부재로 편집 기능이 없는 하위 영역" 판단은 그 4개 영역 한정으로 여전히 유효하며, 나머지 3.5개 영역(Office Profile/Endpoint Alias, 자산 크기·확장자 정책, 승인 Workflow, 보안등급)도 여전히 읽기 전용이다(각각의 저장소가 여전히 없거나 다른 모듈 소유이기 때문). 신규 `GET /api/v1/admin/embedding-models`(indexing-runtime의 `GET /indexing/v1/models`를 proxy — Ollama 설치 모델 + 이름/family 기반 `embedding_capable` 휴리스틱 판정)와 `PUT /api/v1/admin/indexing-embedding-model`(그 판정에서 `embedding_capable=false`인 모델은 `400 VALIDATION_ERROR`로 거부, indexing-runtime 미응답은 `503 MODEL_UNAVAILABLE`) 신규 — ADMIN 전용(신규 `Permission.ADMIN_SETTINGS_WRITE`, 읽기 권한과 분리), 저장 성공/모델 거부 둘 다 감사 기록(다른 P15 절과 달리 이 영역은 실제 상태를 바꾸므로). `routers/assets.py::_trigger_indexing`이 이 설정값을 실제로 indexing-runtime에 전달하도록 확장(미설정 시 키 생략, indexing-runtime 자체 기본값 적용 — 하위 호환). 이 설정을 바꿔도 **이미 만들어진 인덱스는 바뀌지 않는다**(각 인덱스는 자신의 `index-meta.json`에 기록된 모델로만 검색됨, D-075) — Portal UI(`/admin/settings`)가 이 사실과 "모델 계열을 바꾸면 `SEARCH_QUERY_INSTRUCT_PREFIX`도 재검토해야 한다"(D-046)는 경고 두 가지를 항상 표시한다. 상세는 progress-log.md 2026-08-10 M01/M02/M07 항목 참고 | 실 사용자 디렉터리(D-001 SSO/OIDC)·조직·사업장 저장소(D-022)·보관기간 정책·PKI(D-016/D-048)가 생기면 각 섹션을 편집 가능한 화면으로 전환. `embedding_capable` 판정은 Ollama `/api/tags`가 모델 용도를 명시하지 않아 이름/family 휴리스틱에 의존하는 한계가 있음(오탐/누락 가능성) — Ollama가 향후 이 메타데이터를 노출하면 휴리스틱을 대체할 것 | M01/M02/M07/M11 |
| D-066 | P00/P04/P05 화면 미구현이 어디에도 기록되지 않았음 (01-portal-and-distribution.md §2) | **DECIDED (2026-08-09)**: 12-poc-acceptance-report.md 재작성 중 발견 — 명세 §2의 화면 목록 중 **P00 로그인/접근 거부(`/login`, `/forbidden`), P04 자산 유형 선택(`/assets/new`), P05 자산 등록 Wizard(`/assets/new/:type`)** 3개는 라우트 자체가 존재하지 않는데, 이 공백이 progress-log.md·open-decisions.md·인수 보고서 어디에도 적혀 있지 않아 "P01~P18 전부 구현됨"으로 잘못 읽힐 수 있었다(실제로 이번 세션에서 한 번 그렇게 보고되었다가 정정됨). 각각의 사유는 이미 내려진 다른 결정에 종속된다: **P00** — D-001(SSO/OIDC 미연동, Test Identity Adapter가 Bearer Token→역할을 고정 매핑)에 따라 로그인 화면이 대체할 인증 진입점이 없다. 권한 부족 안내는 전용 `/forbidden` 라우트 대신 각 화면의 Permission 상태로 구현되어 있어(CLAUDE.md UI 규칙 준수) 기능적 공백은 아니지만, 명세가 요구한 전용 라우트는 없다. **P04/P05** — D-034(Agent/Prompt/Service Registry 부재)에 따라 Knowledge 외 자산 유형(AGENT/PROMPT/MCP_TOOL)은 Portal에서 등록할 대상 Registry가 없다. 그래서 범용 `/assets/new`+유형별 Wizard 대신 Knowledge 전용 `/knowledge/new`만 구현되어 있다. Registry 없이 범용 Wizard를 만들면 제출 후 저장될 곳이 없는 화면이 되므로(D-065와 동일한 판단) 구현하지 않는다. **영향**: 데모 시나리오는 Knowledge 챗봇 중심이라 막히지 않지만, "Portal에서 모든 자산 유형을 등록할 수 있다"는 인상은 사실이 아니다 — 인수 보고서 §2/§3에 이 사실을 명시했다. **UPDATE (2026-08-09): P04/P05 해소됨** — D-034가 부분 해소되어(Agent/Prompt Registry 등록·조회 구현, 상세는 D-034 UPDATE 참고) 더 이상 "제출 후 저장될 곳이 없는 화면"이 아니게 되었다. `apps/portal-web/app/assets/new/page.tsx`(P04, 5개 유형 목록 — Knowledge/AI Service는 기존 전용 화면으로 직접 연결, Agent/Prompt/MCP Tool은 P05로)와 `apps/portal-web/app/assets/new/[type]/page.tsx`(P05, 기본정보→Manifest 입력→파일 업로드→검증→제출 5단계 Wizard, `POST /api/v1/manifests/validate`로 제출 전 필드별 오류 표시, `POST /api/v1/assets` 재사용)를 신규 구현했다. `knowledge`/`service` 타입으로 P05에 직접 진입하면 전용 화면으로 안내하는 상태를 보여준다(무음 리다이렉트 아님). "의존성 확인" 단계는 Agent/Prompt/MCP Tool Manifest Schema 어디에도 다른 자산을 참조하는 필드가 없어 비활성 상태로 표시(`services/new`의 `PLACEHOLDER_STEPS` 관례 재사용). `pnpm --filter portal-web typecheck` clean. **P00은 여전히 미해결**(D-001 SSO 미도입 그대로) | D-001 SSO 도입 시 P00 구현. P04/P05는 해소 완료 — 남은 것은 D-034 UPDATE의 "남은 것"(Service Composer가 Registry 기반 Agent/Prompt를 아직 선택할 수 없음)뿐, 화면 자체의 추가 작업은 없다 | M01/M02 |
| D-067 | indexing-runtime/search-runtime의 Chroma Client 누수로 인한 Production 장애 | **DECIDED/FIXED (2026-08-09)**: 실제 운영 장애로 발견 — `indexing-runtime:8200`이 uptime 2일 7시간 시점에 **4095 threads, 3344 open fds**로 `/health` 무응답 상태에 빠졌다(프로세스는 살아있으나 응답 없음). 원인은 `chromadb.PersistentClient`를 호출마다 새로 생성하고 절대 해제하지 않은 것 — `pipeline.py:98`(Indexing Job마다 1개), `hybrid.py:134`(**Search Query마다 1개** — search-runtime은 2일 시점에 이미 1041 threads로 장기적으로 더 심각), `stamp_classification.py:109`(CLI 호출마다 1개, 단명 프로세스라 상대적으로 저위험이나 일관성을 위해 함께 수정). Chroma의 Rust Binding은 Path별 Thread Pool을 갖는 `System`을 스폰하는데, 이 Thread는 Python `threading` 모듈에 등록되지 않아 `threading.active_count()`로는 전혀 보이지 않고 `ps -M`같은 실제 OS 도구로만 보인다 — Repro 스크립트 작성 중 이 함정에 실제로 걸렸다(최초 버전은 `threading.active_count()`를 썼다가 200회 호출에도 완전히 flat한 curve가 나와 재현 실패로 오판할 뻔했다). **측정(재현, scratchpad/repro_chroma_leak.py, `data/indexes/hr-policy-v1`을 scratchpad로 복사해 사용, 운영 `data/indexes/`는 건드리지 않음)**: 동일 Path를 200회 반복 호출 — chromadb 1.5.9의 `SharedSystemClient`가 Path(persist_directory)별로 `System`을 이미 프로세스 전역 캐싱하고 있어 threads/fds가 **24/16에서 전혀 증가하지 않음**(이 케이스만으로는 재현되지 않는다 — 중요한 반증). 실제 운영 재현은 **서로 다른 Knowledge Index Path**(실제 운영에서 여러 Knowledge 자산을 순차적으로 인덱싱/검색하는 것과 동일한 모양) 200개를 각 1회씩 호출 — threads 24→2617(+2593, 호출당 +12.97), fds 16→2012(+1996, 호출당 +9.98), 완전히 선형 성장, 상한 없음. **수정**: `indexing_runtime.chroma_client_cache`/`search_runtime.chroma_client_cache`(신규, 서비스별 중복 — 아래 배치 이유 참고) — Path(resolve된 절대경로)를 Key로 하는 Thread-safe LRU. 같은 Fix로 재실행한 동일 재현(다른 Path 200개, Max Size=32 기본값)은 threads/fds가 처음 32회까지만 증가(427/326)하고 그 이후 **완전히 flat**하게 유지됨(200회 끝까지). **Eviction/Disposal**: `chromadb.api.client.SharedSystemClient.clear_system_cache()`는 Class 전역 Dict를 통째로 새 Dict로 교체할 뿐 `system.stop()`을 호출하지 않아 — 이걸 Eviction에 썼다면 Thread Pool은 계속 실행되는데 참조만 사라지는 **더 나쁜, 복구 불가능한 누수**가 됐을 것(코드 직접 확인). 대신 Chroma가 실제로 지원하는 경로인 evicted Client의 `.close()`를 호출 — 이는 `SharedSystemClient`의 Refcount를 감소시키고 0이 되면 `System.stop()`을 실제로 호출해 Thread Pool을 해제한다. Regression Test(`test_eviction_actually_closes_the_client_not_just_forgets_it`)로 "Evict된 Client에 아무 메서드나 호출하면 실패한다"(System이 실제로 内部에서 정지됐다는 증거)는 것까지 확인함 — 단순히 Dict에서 지워지기만 한 게 아님을 Private 속성이 아니라 공개 API 동작으로 증명. **Staleness 분석**: Client를 재사용해도 새로운 Staleness는 생기지 않는다 — 별도 Client/Process가 같은 Path에 대해 Collection을 delete+recreate+반입한 뒤, 오래 살아있는 원본 Client로 `get_collection()`을 다시 호출하면(이미 `hybrid.py`/`pipeline.py` 모두 매 호출마다 `get_collection()`을 새로 부른다 — Collection 객체 자체는 캐싱하지 않음) 새 데이터를 즉시 반영함을 직접 검증(4개→3개 갱신 확인). 애초에 chromadb 1.5.9 자체가 Path별 `System`을 프로세스 전역에서 이미 공유/재사용하고 있었으므로(위 "동일 Path 200회" 반증 참고), 이번 Fix가 Client Wrapper 계층을 캐싱한다고 해서 그 아래 이미 공유되던 상태의 신선도가 달라지지 않는다 — 이전에 존재했을 수 있는 어떤 Staleness든(예: 재시작이 필요했다는 관찰) 이번 Fix로 인해 새로 생기거나 악화되지 않는다. **배치 결정**: M07/M08은 서로의 내부 폴더를 Import할 수 없다(CLAUDE.md 원칙 2). 이 저장소에서 chromadb를 다루는 기존 3곳(`pipeline.py`, `hybrid.py`, `packages/knowledge-packager`의 `index_reader.py`/`relativize.py`)이 이미 각자 직접 `chromadb`를 Import하는 선례이자 공유 Vector Store Adapter 패키지가 없는 상태라, 이 ~90줄짜리 Cache Helper만을 위해 신규 `packages/*` Workspace Member를 만드는 것은 이 누수 수정 범위를 넘어서는 구조 변경으로 판단해 **각 서비스에 동일한 내용을 의도적으로 중복 배치**했다(CLAUDE.md: 한 PR은 하나의 기능/계약 변경으로 제한). Max Size는 하드코딩이 아니라 `indexing_runtime.settings.CHROMA_CLIENT_CACHE_MAX_SIZE`/`search_runtime.settings.CHROMA_CLIENT_CACHE_MAX_SIZE`(env override, 기본 32)로 관리. **검증**: 신규 Regression Test 12개(cache identity/bounded size/LRU order/eviction disposal, 절대 Thread 수에 의존하지 않음) 포함 `uv run pytest tests/ -q` = 741 passed, 92 deselected(기존 729 + 신규 12); `uv run ruff check .` = 0 errors; `make contract-test` 23 passed; `make e2e-test`/`make security-test`는 `indexing-runtime:8200`이 여전히 응답 없음 상태라(재시작하지 않음 — 운영자 재시작 대기) 기대대로 각각 28/64 skipped | 재시작 후 실제 `indexing-runtime:8200` 프로세스에서 Thread/FD 수가 실제로 내려가지는 확인 필요(재시작해야 새 코드가 적용되므로 이번 세션에서는 Live 프로세스를 대상으로 검증하지 못했다 — Scratchpad 재현으로 대체). `CHROMA_CLIENT_CACHE_MAX_SIZE` 기본값(32)은 PoC 규모 추정치이며 운영 규모(동시 Hot Knowledge 자산 수)에 맞춰 재산정 필요. Global Lock 하나로 모든 Path의 최초 생성을 직렬화하는 설계라 매우 높은 동시성에서 병목 가능성 있음 — 현재 PoC 규모에서는 허용 가능 판단, 운영 전 재검토 대상 | M07/M08 |

| D-068 | D08 로컬 자산 관리 — Active Version 전환 (02-desktop-and-agent-runtime.md §D08) | **PoC 가정 (2026-08-09, 최초 기록)**: 설치 레이아웃(`{installRoot}/assets/{folder}/{assetId}/{version}/`)에는 같은 자산의 여러 버전이 무엇을 기준으로 "지금 쓰이는 버전"인지 가리키는 Active Pointer 개념이 아예 없다 — 그래서 D08의 "Active Version 전환" 액션은 실제로 아무 상태도 바꾸지 않는 가짜 성공을 반환하는 대신 **비활성화**하고 이유를 화면에 표시했다. D08의 상태 필터(`electron/asset-status.ts` `computeAssetStatus`)는 그 결과로 "INACTIVE"를 절대 산출하지 않았다(근거 없이 아무 버전이나 비활성으로 표시하지 않음) — REVOKED(revocation 목록 일치)와 INVALID(Checksum 재검사 실패)만 실제 근거로 계산되고 나머지는 전부 ACTIVE였다. **해결 (2026-08-09, D12 구현과 함께)**: 신규 `electron/active-version-store.ts`(`ActiveVersionStore`)가 §2가 명시한 실제 경로 `state/active-versions.json`에 자산별(`assetType`+`assetId` → `activeVersion`) 포인터를 영구 저장한다. 포인터가 아예 기록되지 않은 assetId(레거시 설치, 아직 한 번도 전환 안 함)는 여전히 근거 없이 INACTIVE를 지어내지 않고 ACTIVE로 취급한다 — 원래의 "추측 금지" 원칙은 그대로 유지, 포인터가 생기면 그 근거로만 INACTIVE를 산출한다. **실제로 소비하는 지점 3곳**(포인터만 만들고 아무도 읽지 않는 장식이 되지 않도록): (1) `electron/asset-status.ts::computeAssetStatus`가 포인터를 받아 D08(`AssetsScreen.tsx`)의 Active/Inactive 필터와 D02(`HomeScreen.tsx`, 신규 상태 배지)에 실제 값을 공급한다. (2) `src/screens/ChatScreen.tsx`(D06)의 Knowledge 선택 목록이 `status !== "INACTIVE"`로 필터링되어, 전환 후 남은 이전 버전이 대화에서 조용히 계속 선택되는 일을 막는다. (3) `electron/removal-guard.ts::evaluateAssetRemoval`이 신규 `activePointer` 축을 받아 "다른 설치된 버전이 있는데 Active Version을 먼저 전환하지 않고 제거하려는" 시도를 실제로 차단한다(`asset-management.ts::checkAssetRemoval`/`main.ts`의 `assets:remove`가 배선) — 유일하게 설치된 버전(전환할 대상이 없음)은 예외적으로 허용(제거 시 자산 전체가 사라짐). 제거로 마지막 버전이 없어지면 `ActiveVersionStore.clear()`로 dangling 포인터를 남기지 않는다. D08의 "Active Version 전환" 버튼은 이제 실제로 동작한다(`electron/asset-management.ts::activateAssetVersion` — REVOKED/INVALID/이미 ACTIVE인 버전은 활성화를 거부, 추측 활성화 금지). 신규 D12 화면(`src/screens/UpdateScreen.tsx`)이 동일한 호출로 "전환"과 "Rollback"을 제공한다(오래된 버전으로 다시 전환하는 것은 같은 코드 경로, 라벨만 다름 — `src/screens/updateTypes.ts::activationActionLabel`). 검증: `electron/__tests__/active-version-store.test.ts`(8), `asset-status.test.ts`/`removal-guard.test.ts`/`asset-management.test.ts`의 신규 케이스(총 250+ passed, 아래 progress-log 세션 이력 참고), 실 `importBundle()`로 "실패한 가져오기가 기존 Active 버전을 절대 건드리지 않는다"를 실측한 회귀 테스트 포함. | 해결 완료. 남은 것은 D-069(Smoke Test 정의)뿐 — D12의 "새 버전 임시 설치와 Smoke Test" 단계는 Smoke Test 자체가 없어 여전히 비활성화 상태로 남아 있다(아래 D-069 참고). | M04 |
| D-069 | D08 로컬 자산 관리 — Smoke Test (02-desktop-and-agent-runtime.md §D08) | **PoC 가정 (2026-08-09)**: 자산 유형별로 "Smoke Test 통과"가 무엇을 의미하는지(Knowledge는 표본 질의 1건 검색? Agent는 dry-run 실행? MCP Config는 Tool 목록 조회?) 정의된 계약이 어디에도 없다(`packages/schemas`에 Smoke Test 결과 Schema 없음, D-045도 "Package Smoke Test 통과"를 이미 범위 밖으로 명시). 정의 없이 아무 검사나 돌려 PASS/FAIL을 표시하면 근거 없는 결과를 사실인 것처럼 보여주는 것이므로, D08의 "Smoke Test" 액션은 비활성화하고 이유를 표시한다. **D12 의존성 추가 (2026-08-09)**: D12 업데이트/복구(§D12 "새 버전 임시 설치와 Smoke Test")를 구현하면서 이 미해결 항목이 D08 하나가 아니라 D12에도 그대로 이어짐을 확인했다 — `src/screens/UpdateScreen.tsx`의 Smoke Test 버튼도 D08과 동일한 사유로 비활성화되어 있다. 즉 D12는 "Active Pointer 전환 전에 새 버전이 정상 동작하는지 자동 확인"을 제공하지 못한 채 운영자의 수동 확인에 의존한다 — 이 계약이 정의되기 전까지 D12는 구조적으로 이 한계를 안고 간다. | 자산 유형별 Smoke Test 절차와 결과 Schema를 `packages/schemas`에 계약으로 먼저 정의해야 한다 — Knowledge는 M08 search-runtime 표본 질의, Agent/Service는 M05 agent-runtime dry-run, MCP Config는 M10 Tool 목록 조회가 유력한 후보. 정의되면 D08과 D12 두 곳 모두 같은 계약을 소비하도록 연결한다. | M04/M06/M08/M10 |
| D-070 | D08 로컬 자산 관리 — 제거 전 "진행 중인 Run" 확인 (02-desktop-and-agent-runtime.md §D08 "제거 전 참조 중인 Service와 진행 중인 Run을 확인한다") | **PoC 가정 (2026-08-09)**: "참조 중인 Service" 확인은 실제로 구현했다 — 설치된 모든 Service의 `service-definition.json`(`agent_ref`/`knowledge_bindings`/`mcp_bindings`/`prompt_bindings`)을 로컬에서 읽어 대상 자산을 참조하는지 대조하고, 참조가 있으면 제거를 실제로 차단한다(`electron/service-dependencies.ts`+`electron/removal-guard.ts`+`electron/asset-management.ts::checkAssetRemoval`, `main.ts`의 `assets:remove` 핸들러가 서버 측에서도 다시 강제). 반면 "진행 중인 Run" 확인은 **오늘 시점 원천적으로 불가능**하다 — 원인이 둘 겹친다: (1) Local Agent Runtime(`services/agent-runtime`, :8100)에 실행 중인 Run 목록을 조회하는 API 자체가 없다(`GET /local/v1/runs/{id}` 단건 조회만 존재, 목록 없음). (2) 설사 목록이 있어도 `RunRecord`가 `service_id`만 들고 있고 그 Run이 어떤 Knowledge/Agent/MCP 자산을 쓰는지는 기록하지 않으며, D06 ChatScreen이 보내는 `service_id`도 Service Registry가 없어(D-058) `dev-chat:{knowledgeId}` 형태의 "개발 확인용" 합성값이라 설치된 실제 Service 자산과 연결할 방법이 없다 — Service Registry가 도입되기 전에는 agent-runtime에 목록 API를 추가해도 이 Run이 어떤 설치된 자산에 대응하는지 알 방법이 없다. `evaluateAssetRemoval`(`electron/removal-guard.ts`)은 이 상황을 대비해 순수 로직 자체는 이미 `activeRunUsages: ActiveRunAssetUsage[] | null`을 받아 참조-Service와 동일한 방식으로 차단할 수 있도록 설계·단위테스트(referenced-by-service/run-in-progress/allowed 3가지 케이스)까지 마쳐 두었다 — 실제 배선(`asset-management.ts::checkAssetRemoval`)만 지금은 이 값을 항상 `null`(조회 불가, "확인 결과 없음"이 아님)로 넘긴다. D08 제거 확인 다이얼로그는 이 사실을 감추지 않고 "Local Agent Runtime이 현재 실행 중인 Run과 사용 중인 자산을 연결해 조회할 방법을 제공하지 않아 자동으로 확인할 수 없습니다... 제거 전 진행 중인 대화가 없는지 직접 확인하세요"를 항상 표시한다(`RemovalGuardResult.runCheckAvailable=false`+`runCheckNote`). CLAUDE.md 원칙(다른 모듈 Contract 변경은 별도 PR로 제안)에 따라 이번 M04 작업에서 `services/agent-runtime`은 수정하지 않았다. | Service Registry(D-058) 도입과 함께 agent-runtime에 읽기 전용 Run 목록 API(예: `GET /local/v1/runs?status=active`, 각 Run이 사용한 Service AssetVersion id 포함)를 Contract 변경으로 별도 제안하고, Desktop은 그 응답을 `ActiveRunAssetUsage[]`로 변환해 이미 준비된 `evaluateAssetRemoval`에 연결하기만 하면 된다. | M04/M05 |
| D-071 | 자산 스토어(M04) — Portal 식별 Token 저장 방식 | **PoC 가정 (2026-08-09)**: Desktop이 자산 스토어(Portal 카탈로그 브라우징·설치, `apps/desktop-client/src/screens/StoreScreen.tsx`)에서 쓰는 Portal Base URL/식별 Token(D-001의 `dev-*-token` Bearer)을 `electron/portal-settings.ts`가 Main Process 전용 로컬 파일(`{installRoot}/state/portal-settings.json`)에 **평문**으로 저장한다. OS Keychain(Windows Credential Manager 등, D-005 Windows 타겟과 맞는 선택지) 연동은 이번 범위에 포함하지 않았다 — PoC의 Token 자체가 이미 dev 고정값이고(D-001), 이 파일 하나를 위해 신규 네이티브 의존성(keytar류)을 추가하는 것은 폐쇄망 설치 부담을 늘리는 트레이드오프라 판단을 미뤘다. 대신 이 값이 Token "값"을 노출하는 유일한 경로가 되지 않도록 방어했다: `getToken()`은 Main Process 내부(HTTP 호출 조립)에서만 호출되고 IPC/`preload.ts`의 어떤 브리지 메서드도 이 값을 반환하지 않으며, D11 진단 Bundle(`diagnostic-bundle.ts`)에도 "설정됨 여부+마지막 갱신 시각"만 들어간다(테스트로 검증: `electron/__tests__/portal-settings.test.ts`, `electron/__tests__/diagnostic-bundle.test.ts`의 신규 케이스). | 운영 전환 시 (1) OS Keychain/Credential Manager 연동 여부와 폐쇄망 배포 시 필요한 네이티브 모듈 사전 캐싱 방법, (2) D-001 SSO 도입 시 이 저장소 자체가 사라지는지(SSO Token은 별도 세션 방식일 가능성) 결정 필요 | M04/M11 |
| D-072 | 자산 스토어(M04) — 브라우징 시점 Revocation(긴급 회수) 미표시 | **PoC 가정/알려진 공백 (2026-08-09)**: `GET /api/v1/assets`(카탈로그 브라우징이 쓰는 유일한 조회 API)는 `AssetVersion.status`(DRAFT/.../APPROVED/SUSPENDED/DEPRECATED/RETIRED)만 반환하고, 이와 별도 테이블인 `asset_version_revocations`(P16 긴급 회수)는 조인하지 않는다 — `apps/portal-api/src/portal_api/routers/distributions.py`의 `_effective_revoked_version_ids`/`_has_effective_revocation`이 이 테이블을 실제로 사용하는 유일한 지점이고, 둘 다 Distribution 생성/다운로드라는 **설치 시점** 로직 안에 있다. 그 결과 `src/screens/storeTypes.ts`의 카탈로그 상태 계산(설치됨/설치 가능/업데이트 있음/설치 불가)은 회수 여부를 반영하지 못한다 — 회수된 APPROVED 버전도 브라우징 화면에서는 "설치 가능"으로 보인다. 근거 없이 "REVOKED" 상태를 추측해서 표시하지 않는 쪽을 선택했다(CLAUDE.md: 추측 금지). 실제 안전장치는 설치 시점에 그대로 작동한다: 회수된 버전을 설치 시도하면 `POST /api/v1/distributions`/`GET .../download`가 `ASSET_VERSION_REVOKED`로 거부하고, `electron/store-install.ts`는 이를 그대로 "설치 불가" 결과로 사용자에게 보여준다(설치를 막는 실제 게이트는 우회되지 않음 — 단지 그 사실을 미리 알려주는 화면이 없을 뿐). **해결 (2026-08-09)**: Contract부터 변경했다(CLAUDE.md 원칙 1) — `packages/schemas/api/portal-openapi.yaml`에 신규 `AssetVersionRevocationSummary` 스키마를 추가하고 `AssetVersion.active_revocation`(nullable) 필드로 노출, `GET /assets`/`GET /assets/{asset_id}` 양쪽에 적용됨을 문서화했다. **단일 판정 predicate**: "효력이 있다"(`effective_at <= now()`)를 두 번째로 재정의하지 않기 위해 `apps/portal-api/src/portal_api/models/revocation.py`에 `effective_filter()`(SQLAlchemy WHERE절 조각)와 `is_effective()`(이미 메모리에 있는 값용) 두 함수를 신설하고, 기존에 각자 손으로 `effective_at <= datetime.now(UTC)`를 다시 타이핑하던 세 곳 — `routers/distributions.py`의 `_effective_revoked_version_ids`/`_has_effective_revocation`(설치 시점 강제, D-057), `routers/reviews.py`의 `_revocation_out`(P16 수명주기 화면) — 을 전부 이 공유 함수를 호출하도록 리팩터링했다. 신규 `routers/assets.py::_attach_active_revocations`(카탈로그 페이지당 회수 테이블 1회 배치 조회 — `get_asset`의 기존 `pending_review_id` N+1 회피 패턴과 동일)도 같은 `effective_filter()`를 사용해 세 지점이 절대 다른 판정을 내릴 수 없게 했다 — 미래 시각 회수는 이 filter 자체가 걸러내므로 카탈로그에 미리 나타나지 않는다(설치를 아직 막지 않는 것을 미리 막힌 것처럼 보여주지 않음). **RBAC 경계 유지**: `active_revocation.reason`은 `Permission.LIFECYCLE_READ`(RELEASE_MANAGER/AUDITOR/ADMIN, `security_policy.roles`가 이미 "P16의 audience는 RELEASE_MANAGER"라고 명시한 그 경계)를 가진 호출자에게만 채워지고, 그 외(모든 역할에 부여되는 `ASSET_READ`만으로 이 endpoint를 호출하는 경우 — Desktop 자산 스토어가 쓰는 고정 Bearer Token이 어떤 역할이든 포함됨)에는 `effective_at`만 보이고 `reason`은 `null`이다 — P16 화면이 이미 그어 놓은 경계를 이 새 endpoint가 조용히 넓히지 않도록 한 것(기존 `AssetVersionRevocationOut`을 재사용하지 않고 더 작은 `AssetVersionRevocationSummaryOut`을 새로 둔 이유). **Desktop**: `electron/types.ts`의 `PortalCatalogVersion`에 `activeRevocation: { effectiveAt, reason } | null` 추가(신규 `PortalCatalogActiveRevocation`), `portal-client.ts::fetchCatalog`가 snake_case 응답을 매핑, `storeTypes.ts::computeCatalogItemView`가 `latestApprovedVersion`이 고른 버전 자체에 `activeRevocation`이 있으면 그 버전을 설치 대상에서 제외하고 자산 전체를 NOT_INSTALLABLE + 사유(`revokedInstallReason`, 서버가 `reason`을 마스킹했으면 "관리자에게 문의" 문구로 정직하게 대체)로 표시한다 — **더 오래된 승인 버전으로 조용히 대체(fallback)하지 않는다**(회수를 우회하는 설치가 되어 버릴 것이므로, 아무것도 안 보여주는 것보다 나쁘다는 판단). **검증**: `uv run pytest tests/ -q` 745 passed/92 deselected(+4, `tests/integration/portal_api/test_lifecycle.py`에 카탈로그 신규 테스트 — 효력 있는 회수 노출/역할별 reason 마스킹/미래 시각 회수 비노출/asset 상세도 일관됨), `uv run ruff check .` 0건, `make contract-test` 23 passed(그대로), `pnpm --filter desktop-client test` 204 passed(+6), `pnpm --filter desktop-client typecheck` clean. 라이브 검증은 재시작 금지된 :8000 대신 이 세션 전용 :8003 인스턴스(동일 `portal.db`)에서 실행: throwaway Knowledge 자산을 등록해 TECHNICAL→SECURITY→RELEASE 전 구간 실제 검토 체인으로 APPROVED까지 승인한 뒤, 회수 전 카탈로그가 `active_revocation: null`을 보임을 확인 → P16 회수 endpoint로 실제 긴급 회수(효력 시각 1시간 전) → 카탈로그가 CREATOR 역할(ASSET_READ만)로는 `active_revocation: {effective_at, reason: null}`을, RELEASE_MANAGER 역할(LIFECYCLE_READ)로는 실제 사유까지 포함해 보여줌을 각각 `curl`로 실측. 참고로 이 세션에서 재기동하지 않은 라이브 distribution-service(:8400, D-057 이전 코드)는 이 회수된 버전의 신규 Bundle 생성 요청을 여전히 SUCCEEDED로 처리했지만(라이브 서비스가 구코드라는 사전 조건과 무관한, 재기동 정책에 따른 예상된 결과), portal-api 자신의 재검사 경로(`GET .../download`, D-057이 이미 커버하는 지점)는 이 리팩터링 이후에도 정확히 `409 ASSET_VERSION_REVOKED`로 거부함을 실측해 `effective_filter()`로의 리팩터링이 기존 강제 동작을 조금도 바꾸지 않았음을 확인했다. 검증에 사용한 throwaway Asset/AssetVersion/ReviewRequest×3/ReviewDecision×3/AssetVersionRevocation/DistributionRequest는 모두 DB에서 직접 삭제해 정리했고, 정리 후 데모 카운트(assets 5/service_deployments 5/distribution_requests 17/service_versions 6/asset_version_revocations 0)가 세션 시작 시점과 동일함을 재확인했다. :8003 인스턴스는 검증 후 종료했다. | 해결 완료. 남은 것은 라이브 distribution-service(:8400)를 실제로 재기동해 신규 Bundle 생성 경로에서도 D-057의 `revoked` 플래그 강제가 살아있는지 재확인하는 것뿐(세션 소유자가 재기동 후 수행) — 이 세션의 리팩터링은 그 강제 로직 자체를 건드리지 않았으므로 동작이 바뀔 이유는 없다 | M02/M04 |
| D-074 | Desktop(M04) D01 최초 설정 Wizard/D10 설정 — 필드는 있지만 이 PoC에서 완전히 구현되지 않은 3가지 | **PoC 가정/알려진 공백**: 신규 `electron/desktop-settings.ts`(D01/D10 공용 설정 저장소, `state/desktop-settings.json`)가 02-desktop-and-agent-runtime.md §D01 필드 목록을 전부 저장·검증하지만, 그중 3가지는 값을 저장해도 실제 동작이 정직하게 그대로거나 일부만 구현됐다: **(1) 최대 동시 Run 수** — 오늘 Desktop은 창 하나·대화 하나에서만 Run을 실행할 수 있고(`ChatScreen.tsx`의 `isRunning` 게이트, 다중 창/다중 대화 UI 없음), Local Agent Runtime(`services/agent-runtime`)에도 동시 실행 수를 제한하는 Queue/Semaphore가 전혀 없다(`workflow.py`/`run_store.py` 확인, In-memory Run Store로 개수 제한 없음). 이 값을 사용자가 2 이상으로 바꿔도 실제로 동시에 실행 가능한 Run 수는 전혀 달라지지 않는다 — 그래서 이 필드는 편집 가능한 숫자로 노출하지 않고 `DesktopSettingsPublic.maxConcurrentRuns`(`{value:1, enforced:false, reason}`) 구조로만 노출해 "바꿔도 아무것도 달라지지 않는 설정"을 만들지 않았다(D10 화면에 읽기 전용 + 사유로 표시). **(2) 로그 보관 정책** — `electron/app-logger.ts`는 D11 로그 소스를 계속 이어붙이기만 할 뿐 자동 삭제/보관기간/Rotation이 전혀 없다(무제한 누적). D10/D01 화면은 이 사실을 감추지 않고 "자동 삭제되지 않고 계속 누적됨" 경고를 항상 표시한다 — 실제 Rotation 구현은 이번 작업 범위 밖(app-logger.ts 자체를 바꾸는 것은 D11의 별도 계약 변경). **(3) Office Profile 가져오기(D01 2단계)** — 이 PoC에는 Office Profile 파일을 가져오거나 카탈로그에서 선택하는 기능이 아직 없다(`profilesDir`는 `bundle-install.ts`가 생성만 해 두었고 실제로 쓰는 코드가 없음). D01 2단계는 정직하게 이 사실을 알리고, 대신 Client 표시명/사업장 ID를 직접 입력받아 로컬 설정으로 저장한다 — Office Profile의 실제 내용(모델 Alias 매핑, 허용 MCP Server 목록 등)은 여전히 `services/agent-runtime/config/office-profile-default/office-profile.json`에서만 온다. 이 때문에 D01 4단계("설치된 Chat/Embedding 모델 확인")도 Alias -> 실제 Ollama model_id 매핑을 Desktop이 알지 못해, Alias별 PASS/FAIL을 단정하지 않고 Ollama에 실제 설치된 모델 이름 목록만 보여준다(`connections.ts::listOllamaModels`). | (1) Service Registry/Local Agent Runtime에 동시 실행 제한(Queue/Semaphore)이 추가되면 이 값을 실제로 전달·강제하도록 연결. (2) app-logger.ts에 크기 또는 기간 기반 Rotation 정책 추가 여부와 기본값 결정. (3) Office Profile을 파일(가져오기) 또는 Portal 카탈로그(선택)로 실제 배포하는 형식과 절차 결정 — 결정되면 D01 2/4단계를 그 값을 읽어오는 방식으로 다시 구현. | M04/M05 |

| D-075 | 임베딩 모델을 설정으로 분리, search-runtime의 검색-시점 모델 불일치 결함 수정 | **DECIDED (2026-08-10)**: 임베딩 모델(`qwen3-embedding:0.6b`)이 `indexing_runtime`의 4곳(`embedders/__init__.py::DEFAULT_EMBED_MODEL`, `pipeline.py::run_pipeline` 기본값, `main.py`의 `IndexJobRequest.embed_model`/CLI `--model` 기본값)과 `search_runtime/hybrid.py::DEFAULT_EMBED_MODEL` 1곳, 총 5곳에 리터럴로 하드코딩되어 있던 것을 각 서비스의 `settings.py`(CLAUDE.md 원칙에 따른 기존 plain-`os.environ` 관례를 그대로 따름, 새 의존성 없음)에 `EMBED_MODEL` 설정으로 옮겼다 — `INDEXING_EMBED_MODEL`/`SEARCH_EMBED_MODEL` 환경 변수, 둘 다 기본값은 기존과 동일한 `qwen3-embedding:0.6b`(하위 호환). **더 심각한 부수 결함도 함께 발견해 고쳤다**: `search_runtime/hybrid.py`의 `hybrid_search()`가 각 Knowledge 색인이 `index-meta.json`에 실제로 기록해 둔 `embed_model`(indexing-runtime이 색인을 만들 때 이미 스탬프해 둔 값 — `pipeline.run_pipeline`)을 전혀 읽지 않고 자기 자신의 상수로만 질의를 임베딩하고 있었다. 서로 다른 모델의 임베딩은 비교 불가능하므로, 색인을 만든 모델과 검색이 쓰는 모델이 다르면 오류 없이 그저 관련 없는 결과가 조용히 반환된다 — 발표 데모처럼 항상 같은 값이면 드러나지 않다가, 운영에서 모델을 바꾸는 순간 아무 경고 없이 검색 품질이 무너지는 유형의 결함. 신규 `search_runtime.hybrid.resolve_embed_model(index_base, knowledge_id, configured_default)`이 대상 색인의 `index-meta.json`을 읽어 `(model, source)`를 반환하고, `hybrid_search()`가 이 값으로 `embed_query()`를 호출하도록 수정했다 — `source="index_meta"`(정상, 색인이 기록한 모델을 사용)와 `source="fallback_default"`(색인에 `embed_model` 기록이 없는 구버전 색인 — `SEARCH_EMBED_MODEL` 폴백 사용) 두 경우 모두 `search_runtime`로거에 각각 WARNING/INFO로 로그를 남기고(폴백은 WARNING, 설정값과 색인 기록이 다를 때는 INFO — 색인이 항상 우선하지만 운영자가 드리프트를 알 수 있어야 함), `POST /search/v1/query` 응답에도 `embed_model_applied`/`embed_model_source` 필드를 추가해(`min_relevance_score_applied`와 같은 기존 additive-field 관례) 검색 트레이스에서 바로 확인 가능하게 했다. 시딩된 데모 색인 4개는 전부 `qwen3-embedding:0.6b`를 기록하고 있어 이 수정으로 실제 응답(citations/scores)이 달라지지 않음을 스크래치패드에 복사한 색인 사본에 대해 수정 전/후 코드를 각각 별도 포트(8302/8303)로 띄워 동일 질의로 직접 대조해 확인했다(citation 식별자·순서·반올림 점수 100% 일치, 마지막 자리 유사도 값의 ±0.0001 편차는 같은 신규 코드 인스턴스를 반복 호출해도 동일하게 나타나는 Ollama 임베딩 자체의 부동소수 잡음임을 별도로 확인 — 코드 변경과 무관). `SEARCH_QUERY_INSTRUCT_PREFIX`(D-046)는 Qwen3-Embedding 전용 튜닝값이라 `EMBED_MODEL`을 다른 모델 계열로 바꾸면 함께 재검토해야 한다는 결합 관계를 두 설정의 docstring에 상호 문서화만 해 두었다 — 자동으로 서로 값을 파생시키는 로직은 이번 범위에서 추가하지 않음(과설계 방지, 명시 요청 시에만) | 두 설정을 하나로 자동 파생(예: 모델별 프리셋 테이블)할지 여부, `stamp_classification.py`처럼 기존 색인의 `embed_model` 메타데이터만 사후 교정하는 CLI가 필요할지(실제로는 재색인 없이는 근본적으로 의미 없음 — 벡터 자체가 다른 모델로 다시 계산되어야 하므로 오해를 유발할 여지가 커서 이번에는 만들지 않음) | M07/M08 |

| D-077 | Desktop 대화 고도화(M04/M05) — Hosted Chat은 아직 `history`를 보내지 않음 | **알려진 공백 (2026-08-10)**: `run_knowledge_chat`이 `history`(additive/optional, `[{question, answer}]`)를 받아 멀티턴 대화와 §3.4 Query Rewrite를 지원하게 됐지만, 실제로 이 값을 채워 보내는 호출부는 이번 세션에서 새로 만든 Desktop `ChatScreen.tsx`(`chatTypes.buildHistoryFromMessages`, in-memory 대화 상태에서 직접 구성)뿐이다. Hosted Chat(`services/agent-runtime/src/agent_runtime/routers/chat.py`, `chat_sessions.py`)은 `ChatSession`에 `message_count`만 셀 뿐 실제 질문/답변 원문을 전혀 저장하지 않으므로, `send_message`가 `run_knowledge_chat`을 호출할 때 넘길 `history`가 애초에 존재하지 않는다 — 이번 세션은 그 저장을 추가하지 않았다(CLAUDE.md 원칙 4: 한 PR은 하나의 기능/계약 변경으로 제한, 이번 계약 변경은 이미 local-runtime-api.yaml의 `history` 필드 하나였다). 결과적으로 게시된 Hosted 챗봇(4개)에서는 오늘도 각 메시지가 독립 질문으로 처리된다(회귀는 아님 — 원래도 그랬다). | (1) `chat_sessions.ChatSessionRecord`에 turn 기록(질문/답변)을 추가할지, 아니면 별도 저장소로 분리할지 — Desktop의 `conversation-store.ts`와 저장 위치가 다르다(Hosted는 서버 In-memory/DB, Desktop은 Main Process 로컬 파일)는 점을 고려해 설계. (2) Hosted Chat 세션에 쌓인 질문/답변 원문도 Desktop과 동일하게 민감 데이터로 취급해야 한다 — 로그·진단 export가 있다면 동일한 leak 방지 원칙 적용 필요. (3) `packages/schemas/api/hosted-chat-api.yaml`의 `SendMessageRequest`/세션 계약 변경이 먼저 필요(Contract 우선 원칙). | M05 |
| D-076 | Desktop(M04) D03 Service/Agent 상세 / D13 정보/보안 — 로컬에 소스가 없는 §D03/§D13 필드 (02-desktop-and-agent-runtime.md §D03/§D13) | **PoC 가정/알려진 공백 (2026-08-10)**: 신규 `electron/service-detail.ts`(D03)/`electron/system-info.ts`(D13)가 실제 로컬 데이터(설치 자산 레코드, `service-definition.json`, 설치된 Knowledge 자신의 `index/index-meta.json`)만으로 각 필드를 채우되, 아래 필드는 근거가 없어 `{available:false, reason}`(D03)으로 "미기재"를 명시한다 — 절대 그럴듯한 값을 지어내지 않는다(CLAUDE.md). **D03**: (1) **입력 필드와 허용 파일** — `service-definition.schema.json`(packages/schemas/manifests)에 입력 Form/허용 파일 개념 자체가 없다(D-058과 같은 근본 원인). (2) **요구 Runtime과 Client 버전** — `bundle-manifest.yaml`의 `runtime_requirements`는 D04 가져오기 시점에 RUNTIME_COMPAT 검사로 한 번 확인될 뿐 개별 `InstalledAsset` 레코드에 저장되지 않는다(가져오기 종료 시 그 Manifest 자체가 staging에서 삭제됨, `bundle-install.ts`). (3) **검증·승인·Checksum 상태 중 승인 상태** — Offline Bundle 가져오기는 중단·폐기(Revocation) 여부만 검사해 통과시킬 뿐, Portal의 VersionStatus(APPROVED 등) 값 자체는 로컬에 저장하지 않는다. (4) **Tool별 읽기/변경 위험도** — `mcp-tool-manifest.schema.json`에 세분화된 위험도 필드가 없다(PoC는 CLAUDE.md 원칙 8에 따라 모든 Tool이 읽기 전용이라는 사실 자체를 항상 표시하고, 각 바인딩의 `confirmation_policy`는 service-definition.json에서 실제로 읽어 함께 보여준다 — 이 값은 실존하는 필드). (5) **현재 해석된 Chat 모델** — D-074가 이미 기록한 공백(Desktop에 Office Profile 가져오기가 없어 Alias→실제 model_id 매핑을 모름)을 D03도 그대로 물려받는다(중복 기록하지 않고 참조). 반대로 **업무 목적**(`manifest.description`)과 **사용 예**(`manifest.chatbot_config.suggested_questions`, Knowledge 챗봇 Quick Create 한정)는 Manifest에 실제로 선언되어 있으면 그대로 표시하고, 없으면(선택 필드) 미기재로 표시한다 — 항상 없는 것은 아니다. **Knowledge Embedding 모델**은 지어내지 않고 실제로 읽을 수 있다 — Bundle이 Knowledge 자산의 `index/` 디렉터리 전체를 그대로 설치하므로(`distribution_service.bundler.collect`), 설치된 Knowledge의 `index/index-meta.json`에 색인 생성 당시 스탬프된 `embed_model`(D-075와 동일한 소스)을 그대로 읽어 보여준다. **D13**: (1) **Trust Store 상태** — D-016/D-048이 이미 기록한 "이 PoC에는 PKI가 없다"를 그대로 반영해 `NOT_IMPLEMENTED` + `bundle-verify.ts::checkSignatureTrust()`와 동일한 문구를 재사용한다(초록색 "신뢰됨" 배지를 절대 만들지 않음). (2) **마지막 Revocation List 날짜** — Revocation 항목(`RevocationEntry`) 자체에 발행 일시 필드가 없다(Bundle Manifest 계약 밖) — 대신 `state/revocation-list.json`의 파일 시스템 mtime("이 Desktop이 마지막으로 Revocation 항목을 로컬에 반영한 시각")을 명확한 라벨과 함께 보여주는 것으로 절충했다(발행일의 대체물이 아님을 UI 문구에 명시). (3) **라이선스와 오픈소스 고지** — 손으로 쓴 목록 대신 `apps/desktop-client/package.json`의 직접 `dependencies`를 실제 `node_modules/{pkg}/package.json`의 `version`/`license` 필드로 실시간 해석해 보여준다(허구 없음) — 다만 전이(transitive) 의존성과 License 원문을 포함하는 완전한 OSS 고지는 아니며, 화면이 이 사실을 항상 경고 문구로 표시한다. **Schema 지원 버전**은 `packages/schemas/manifests/*.schema.json` 5개 모두가 공통으로 선언하는 `schema_version: "1.0"` 상수를 그대로 보여준다(참고: `bundle-verify.ts::validateManifestSchema`는 이 버전 번호 자체를 비교·고정하지 않고 필드 존재 여부만 구조적으로 검사한다 — 버전 번호 Pinning은 아직 구현되어 있지 않다는 점도 별도 공백으로 남아 있다). **검증**: `electron/__tests__/service-detail.test.ts`(11)/`system-info.test.ts`(8) 신규, `pnpm --filter desktop-client test` 324 passed(기존 305 + 19), `typecheck` clean(양쪽 tsconfig) | 위 D03 (1)(2)(3)(4)는 각각 Service Definition Schema 확장(입력 Form), Bundle 설치 시 runtime_requirements 영속화, 로컬 승인 상태 캐시, MCP Tool Manifest 위험도 필드 추가가 결정되면 해소 가능. D13의 라이선스 고지는 실제 배포 전 완전한 OSS Notice 생성 도구(license-checker류) 도입 필요, Schema 버전은 D-016/D-048과 함께 PKI/버전 Pinning 정책이 정해지면 재검토 | M04 |
| D-078 | Desktop Agentic Knowledge 챗봇 — 로컬→Hub 유출 방지 경계 (보안 posture, 사용자 명시 요구사항) | **DECIDED (2026-08-11)**: 사용자가 명시적으로 요구한 불변식이다 — "로컬에서 조회하는 데이터를 허브에 넘기면 안 된다"는 네트워크 통제만이 아니라 **개발자 스스로 지켜야 하는 규율**이며, 이후 어떤 변경도 이 경계를 조용히 넓혀서는 안 된다. **불변식**: Hub(중앙 Knowledge Registry, `POST /api/v1/knowledge-search`)로 나가는 모든 질의 텍스트는 오직 "이번 턴에 사용자가 입력한 질문"과 "대화 History의 이전 사용자 질문(`.question`)"으로만 구성되어야 하며, 다음은 **절대** 포함되어서는 안 된다 — (1) 이전 턴의 어시스턴트 답변(`.answer`, 로컬 Knowledge 문서 내용을 실제로 인용하고 있을 수 있음), (2) 로컬 검색이 반환한 Citation/문서 원문, (3) 로컬 대화 맥락으로 재작성된 검색어(§3.4 Query Rewrite의 `rewrite_query_for_search` 출력 — History의 `.answer`까지 반영해 재작성되므로 그 자체로 로컬 문서 내용을 담을 수 있다). **강제 지점(Chokepoint)**: `services/agent-runtime/src/agent_runtime/hub_query.py`의 `UserTypedQuery`(frozen dataclass)/`build_hub_query()`가 유일하게 Hub 질의 텍스트를 만들 수 있는 함수이며 `turn.question`만 읽고 `turn.answer`는 코드상 접근하지 않는다. `services/agent-runtime/src/agent_runtime/adapters/hub_search.py`의 `HttpHubSearchAdapter.search()`는 인자가 `UserTypedQuery` 타입이 아니면(즉 `build_hub_query()`를 거치지 않은 일반 문자열이면) `TypeError`로 즉시 실패한다 — 주석이 아니라 타입 시스템과 런타임 검사로 강제. **동의(Consent)는 기본 Off, 침묵의 편의가 아니다**: Desktop `허브에도 물어보기` 세션 토글은 새로고침마다 초기화되며 영속화하지 않고, 로컬 Stage 1이 Citation 0건일 때만 의미가 있다(로컬에 근거가 있으면 Hub는 애초에 호출되지 않는다). 토글을 켤 때 전송 전 미리보기(`chatTypes.buildHubQueryPreview`, `hub_query.build_hub_query`의 TS 대응)를 보여주고, 실제 전송 시 SSE `hub.query_sent` 이벤트로 전송된 원문을 사후에도 대화창에 노출한다 — 이 이벤트에 원문을 그대로 실어도 안전한 이유 자체가 `UserTypedQuery`의 구조적 보장이다. **회귀 방지 테스트**(다음에 이 경계를 실수로 허무는 변경이 있으면 반드시 실패해야 함): `tests/integration/agent_runtime/test_hub_query.py`(Chokepoint 단위 테스트 — History의 `.answer`에만 마커 문자열을 넣어도 `build_hub_query()` 출력에 나타나지 않음, 일반 문자열 전달 시 `TypeError`), `tests/integration/agent_runtime/test_runs.py::test_hub_lookup_never_leaks_prior_answer_text_to_hub`(실제 Run 전체를 통해 동일 보장을 End-to-End로 재증명 — 마커 문자열이 담긴 이전 답변이 있는 대화에서 실제 Hub 어댑터가 받은 페이로드를 캡처해 마커 부재를 단언), `apps/desktop-client/src/screens/chatTypes.test.ts`(TS 쪽 미리보기 함수도 동일한 마커 테스트). **실 종단 검증(2026-08-11)**: 자체 기동한 portal-api(:8003)+search-runtime(:8302)+agent-runtime(:8102)을 연결해 로컬 0-Citation → 동의 On 시 Hub 자동 질의(사용자 입력 원문만 담긴 `hub.query_sent`) → 4건 Citation 확인, 동의 Off(기본값)일 때는 동일 0-Citation 상황에서 Hub가 전혀 호출되지 않고 `INSUFFICIENT_EVIDENCE`로 종료됨을 SSE 이벤트 스트림 직접 확인으로 증명했다(§progress-log.md 2026-08-11 참고). | 실 신원 계층 도입 시 Hub 질의에 실제 사용자 인증 컨텍스트를 함께 실어야 하는지(현재는 D-062와 동일하게 서버가 고정 `default_search_clearance`만 주장), Hosted Chat(D-077이 이미 기록한 대로 아직 `history` 자체가 없음)이 향후 History를 갖게 되면 동일 Chokepoint를 재사용해야 함, MCP Tool 결과도 향후 Hub 질의에 흘러들 수 있는 새 경로가 생기면(현재는 로컬 전용) 이 불변식을 명시적으로 재검토해야 함 | M01/M02/M05 |

| D-079 | Desktop 설치 자산 활성화 계약 — ZIP 가져오기 이후 Knowledge 검색 경로 연결 | **부분 해소 (2026-08-13, Knowledge는 설치뿐 아니라 채팅 경로까지 완성)**: Knowledge 절반은 공개 계약으로 해소했고(설치 시점 활성화에 더해 이제 채팅 전송 시점까지), MCP Tool 절반은 D-080으로 분리했다. 원래 공백: Portal 등록 → 승인 버전 ZIP → Desktop 15단계 검증 → 설치 기록까지는 동작하지만 search-runtime이 자기 `INDEX_BASE`만 검색하므로 설치된 Knowledge는 오류 없이 영원히 0건을 반환했고, 화면은 그 차이를 말하지 않았다. **해소한 방법**: 계약을 먼저 쓰고(`packages/schemas/api/knowledge-local-index.schema.json`), M08에 `local_index_registry.py`와 `POST/GET/DELETE /search/v1/local-indexes`를 두고, `hybrid.resolve_index_dir`가 색인 경로를 해석하는 유일한 경로가 되게 했다. M04는 `search-runtime-client.ts`(순수 HTTP)와 `knowledge-activation.ts`로 이 API만 호출하며 M08 트리에 쓰거나 내부 모듈을 import하지 않는다. 설치 기록에 `InstalledAsset.activation`(ACTIVE/FAILED, 부재=시도 안 함)을 남겨 '설치됨'과 '활성화됨'을 분리해 표시하고, 실패도 반드시 저장해 나중에 다시 볼 수 있다. **안전 규칙**: 허용 루트 미설정 시 기본 비활성(403), symlink 해석 후 경계 검사, `index-meta.json`의 `knowledge_id` 불일치 거부(D-060), 배포된 `bm25.pkl` 등록 거부 + 등록 경로는 `allow_legacy_pickle=False` 강제(D-054), 중앙 `INDEX_BASE`가 항상 우선하므로 기존 검색 결과가 바뀔 수 없음, Desktop→search-runtime 주소는 loopback 강제(로컬 절대 경로를 원격에 보내지 않는다). 모든 거절은 `details.reason`을 가진 명시적 오류이며 조용한 0건이 되지 않는다. **검증**: `tests/` 923 passed/2 skipped(신규 registry 27 + contract 7), `ruff` 0건, Desktop `typecheck` 클린 + `vitest` 425 passed(기준선 376). 실서비스 종단 검증(자체 기동 search-runtime :8399 + 실제 `d9e660b7-…` 색인 + 실 Ollama): 활성화 전 0건 → 활성화 후 실제 Citation 1건(`장비 지원`, similarity 0.53, `embed_model_source: index_meta`) → 비활성화 후 0건, 그리고 Desktop의 `activateInstalledKnowledge`/`deactivateInstalledKnowledge` 경로로 등록·목록·해제·거절 사유 표시까지 실제 서버 상대로 확인. **채팅 경로 연결(2026-08-13 후속)**: 활성화 상태가 실제 채팅에도 반영되도록 마무리했다 — `chatTypes.ts`가 설치된 Knowledge를 `activation.state === 'ACTIVE'` 기준으로 분리해 비활성 Knowledge id를 더 이상 채팅에 보내지 않고, 제외된 각 자산에 한국어 사유(미시도/실패 메시지/D-060 레거시 Bundle)를 붙인다. ChatScreen이 활성/제외 건수와 활성 0건일 때 차단 상태 + 인라인 활성화 액션을 보여주고, 세션 중 사용 불가해지면 Knowledge 토글을 자동으로 끈다. 신규 IPC `knowledge:reconcileActivations`가 로컬 ACTIVE 상태를 search-runtime의 등록 목록과 대조해 **확정된 불일치**에서만 강등한다(네트워크 실패로는 강등하지 않음 — 사실을 지어내지 않기 위함), `not_registered_on_server`(등록이 사라짐)와 `local_indexes_disabled`(이 배포는 활성화 기능이 꺼져 있음, 계약의 `local_indexes_enabled`)를 구분해 안내한다. `assets:remove`가 제거 전 비활성화를 먼저 시도(연결 불가 시 경고와 함께 진행)하도록 배선해 아래 남은 것이던 (2)를 해소했고, Settings(D10)에 `searchRuntimeBaseUrl` 입력 필드를 추가해 (1)도 해소했다(loopback 강제). D09 연결 상태에 search-runtime을 추가(Knowledge 채팅에 차단 요소로 취급)하고 ChatScreen의 연결 확인이 하드코딩 기본값 대신 설정된 Endpoint를 쓰도록 수정했다. 이 경로가 실제로 작동하려면 렌더러가 search-runtime을 Health Check할 수 있어야 해서(D-079 자체가 채팅 의존성으로 만듦) M08에 `SEARCH_CORS_ORIGINS` CORS 설정도 함께 추가했다(CORS가 없으면 정상 서비스가 영구 '연결 끊김'으로 보임). **검증**: `uv run pytest tests/ -q` 967 passed/2 skipped/92 deselected, Desktop `pnpm test` 442 passed(기준선 376), 양쪽 typecheck 클린. **실측 발견**: 저장소의 `d9e660b7-…` 색인은 아직 `bm25.pkl`만 있어 그대로는 활성화가 거절된다 — Bundle 반출 전에 `convert-bm25-format` 선행 필요(13-windows-local-setup.md §7.1에 기록, 나머지 4개 색인도 동일 상태 — D-081 참고). | 남은 Knowledge 쪽 후속(2026-08-13 갱신 — 이전 (1) `searchRuntimeBaseUrl` 설정 필드 부재, (2) 제거 시 자동 비활성화 미배선은 채팅 경로 연결 작업으로 해소됨): (1) Electron 셸을 이 환경에서 기동할 수 없어(macOS Gatekeeper) 활성화 배지·버튼·D09 search 행의 실제 렌더링은 미검증 — 렌더러 단독 모드에서 회귀 없음만 확인했다. (2) MCP Tool 활성화는 D-080. | M04/M06/M08 |
| D-080 | 설치된 MCP Tool 활성화 계약 — agent-runtime/Office Profile 연결 (D-079에서 분리) | **DECIDED / 구현 완료 (2026-08-14)**: 계약 우선으로 `mcp-tool-registration.schema.json`과 `POST/GET/DELETE /local/v1/mcp-tools`를 추가하고, agent-runtime 동적 레지스트리는 기본 비활성으로 두었다. 등록 Payload에는 Endpoint가 없으며, Office Profile에 이미 존재하는 server alias·`allowed_tools`·READ_ONLY·기본 확인 정책보다 약하지 않은 정책만 허용한다. Desktop은 설치된 manifest를 읽어 이 계약만 등록하고, ZIP 가져오기/허브 설치 직후 자동 연결을 시도하며 설치 성공과 연결 결과를 분리해 표시한다. 설치된 자산 화면은 연결/해제/재시도를 제공하고, `mcp_tool_registration_disabled`는 고장이나 재시도 가능 오류가 아닌 **운영자 설정 필요** 상태로 표시한다. 연결은 agent-runtime이 계약을 안다는 뜻일 뿐 호출 권한을 부여하지 않으며 실제 권한은 계속 Office Profile이 결정한다. 앱 시작·새로고침 시 GET 목록과 로컬 ACTIVE를 재조정하되 Runtime 도달 실패만으로 상태를 추측해 낮추지 않는다. 자산 제거 전 등록 해제를 시도하고 실패해도 경고 후 로컬 제거는 계속한다. | 남은 후속은 Desktop에서 MCP Tool을 대화/Agent 실행에 선택·바인딩하는 별도 UX와 agent-runtime Base URL 설정 일반화다. D-080 자체의 설치→계약 연결은 닫혔다. | M04/M05/M10/M11 |
| D-082 | Knowledge 버전별 인덱싱·검색 전략 선택 | **DECIDED / 구현 완료 (2026-08-14)**: 종전에는 indexing-runtime에 `recursive`/`markdown`/`parent_child`가 모두 구현돼 있어도 Portal이 `profile`을 항상 생략해 모든 Knowledge가 `parent_child` 기본값으로 색인됐고, 검색도 agent-runtime이 `top_k=5`/`alpha=0.5`로 고정했다. Knowledge Manifest에 검증된 불변 스냅샷 `indexing_profile`과 `retrieval_profile`을 additive로 추가했다(기존 `indexing_profile_ref`는 사람이 읽는 Profile 정체성으로 유지). Portal 등록 화면은 **문맥 보존 / 문서 구조 우선 / 짧은 조각**과 **균형 검색 / 키워드 우선 / 의미 우선**을 짧은 설명과 함께 선택하게 한다. portal-api는 선택된 인덱싱 스냅샷을 실제 indexing-runtime `profile`로 전달하고, Hub Search는 Knowledge 버전별 `top_k`/`hybrid_alpha`/`min_relevance_score`를 적용한다. Desktop은 설치 Manifest에서 닫힌 허용 필드만 `knowledge_candidates[].retrieval_profile`로 복사하며 agent-runtime도 숫자 범위를 다시 검사해 각 Knowledge 검색에 적용한다. `access_context`·clearance·신원 필드는 Profile에서 읽거나 전달하지 않아 검색 전략 선택이 ACL 우회 표면이 되지 않는다. 필드가 없는 기존 Knowledge/Hosted 호출은 기존 기본값을 그대로 사용한다. | 현재 선택지는 검증된 세 Preset이다. 임의 숫자 편집, Profile Registry CRUD, 전략별 품질 비교/추천은 후속으로 남긴다. 특히 청킹 전략 변경은 재색인이 필요하므로 승인된 버전 제자리 수정이 아니라 새 Knowledge 버전으로만 수행해야 한다. | M01/M02/M04/M05/M06/M07/M08 |
| D-083 | ANALYZE 단계에서 모델이 MCP Tool을 직접 선택 — TOOL_ROUTE (D-036/§5.2 재검토, 사용자 결정으로 반전) | **DECIDED / 구현 완료 (2026-08-15, 사용자 승인·명시적 반전)**: 02-desktop-and-agent-runtime.md §5.2는 "Tool Calling이 약한 로컬 모델은 Runtime의 명시적 Workflow와 Schema 기반 호출을 사용한다"고 못박았고, `workflow.py`의 ANALYZE는 지금까지 결정론적이었다 — 호출자가 `mcp_tool`/`mcp_tool_input`을 `input`에 명시적으로 선언해야 했고, 이 저장소의 어떤 호출자도 그렇게 한 적이 없어 MCP Tool은 채팅에서 사실상 한 번도 호출되지 않았다. 사용자가 이 트레이드오프를 인지한 채로 반전을 명시적으로 요청해 구현했다. **바뀐 것**: `agent_runtime.tool_router.route_tool_call`(신규, `services/agent-runtime/src/agent_runtime/tool_router.py`) — 질문 하나당 LLM 호출 최대 1회로 Tool 이름 + 인자를 "제안"한다. `workflow.py`에 TOOL_ROUTE 단계(KNOWLEDGE_SEARCH 이후, TOOL_CONFIRM/MCP_TOOL_CALL 이전)를 추가했고, `input.tool_route=true`(기본 false, `StartRunRequest.input.tool_route`, `packages/schemas/api/local-runtime-api.yaml`)로 Run 단위 opt-in이며, 명시적 `mcp_tool_request`가 이미 있으면 라우팅은 아예 실행되지 않는다(명시적 호출자 의사가 항상 우선). **왜 안전하다고 보는가 — §5.2가 막으려던 것과 지금 하는 것의 차이**: (1) 후보는 호출자가 보내는 것이 아니라 `mcp_tools.list_candidate_tools(office_profile)`가 서버 측에서만 계산한다 — D-080 등록/내장 Tool 중 Office Profile의 `allowed_mcp_servers[].allowed_tools`에도 있는 것의 교집합이며, 라우팅은 이미 허용된 것보다 절대 넓어질 수 없다. (2) 모델 출력은 발동이 아니라 제안이다 — 라우팅 결과는 캐릭터 그대로 `{tool_name, input, confirmed:false}`로 변환되어 기존 `mcp_tools.resolve_allowed_alias` → `validate_tool_input` → `confirmation_policy_for` 체크포인트를 **전혀 수정 없이** 그대로 통과한다(§5.2가 지켜온 "모델 출력이 Tool 호출로 해석되지 않는다"의 정신은, "결정이 Runtime의 결정론적 체크포인트를 우회하지 않는다"로 재해석해 유지했다 — 우회한 것은 ANALYZE 단계에서 후보를 고르는 절차뿐이다). `validate_tool_input`이 모델의 인자를 거부하면 재시도하지 않는다 — 한 번 실패하면 그 Run은 Tool 없이 진행한다(TOOL_ROUTE 자체의 preflight 거부 코드 `MCP_TOOL_NOT_FOUND`/`MCP_INPUT_INVALID`에 한해; 이미 dispatch된 뒤의 런타임 오류는 기존과 동일하게 Run을 FAILED 처리). (3) Knowledge 라우팅(fail-open, 실패 시 후보 전부 검색)과 정반대로 **fail-closed**다 — 타임아웃/파싱 실패/후보 밖 이름/모델의 명시적 미선택은 전부 "아무 Tool도 호출하지 않음"으로 귀결되고, 절대 아무 Tool이나 추측해 호출하지 않는다(검색은 읽기라 실패해도 안전하지만 Tool 호출은 행동이라 그럴 수 없다). (4) 라우팅 프롬프트에는 이번 턴의 `question`과 후보 Tool의 `tool_name`/`input_schema`(구조 메타데이터)만 들어간다 — Knowledge Citation, Tool 결과, `history`는 **전혀 읽지 않는다**(hub_query.py가 D-078에서 지키는 것과 동일한 체크포인트 규율을 라우팅 프롬프트라는 다른 목적지에 적용). (5) 모델이 만든 인자는 confirmation_policy_for 결과를 절대 낮추지 않는다(정책 로직 자체는 완전히 그대로) — 대신 승인 대상이 AI가 만든 값임을 알 수 있도록, TOOL_ROUTE에서 나온 요청의 확인 요약 문구에 "이 값은 AI가 질문에서 자동으로 추출했습니다."를 덧붙인다(`_build_confirmation_summary`의 `ai_derived` 플래그, `workflow.py`). 관측성: `mcp.tool_route.selected`(status/reason/tool_name — 원문 프롬프트나 모델 출력 원문은 절대 기록하지 않음)와 `mcp.tool_route.rejected`(preflight 거부) 이벤트를 추가했다. `knowledge.route.selected`가 Hosted `_INTERNAL_TO_HOSTED_EVENT`에서 의도적으로 빠진 것과 동일한 이유로 `mcp.tool_route.selected`/`mcp.tool_route.rejected`도 그 매핑에 넣지 않았다 — `chat.py`의 `send_message`가 `tool_route_enabled`를 절대 전달하지 않으므로 4개 게시된 Hosted 챗봇에서는 이 단계 자체가 절대 실행되지 않고, 따라서 이 이벤트도 절대 발생하지 않는다(회귀 테스트로 확인). `tool_route_skip_threshold`(기본 0)는 Knowledge의 `knowledge_route_skip_threshold`(기본 2)와 의도적으로 다르다 — 후보가 1개뿐이어도 그 인자는 모델이 질문에서 추출해야 하므로 "적으면 건너뛰고 전부 쓴다"는 지름길이 안전하지 않다(config.py). | 이 반전은 이 배포의 MCP Tool 표면이 전부 READ_ONLY(구현 원칙 8)라는 전제에 기대고 있다 — 쓰기/부수효과가 있는 Tool이 추가되면 이 fail-closed 설계를 다시 검토해야 한다. 현재 후보 설명은 `tool_name` + `input_schema`뿐이라 모델이 후보를 오인할 여지가 있다 — Tool 등록에 사람이 읽는 짧은 설명(`label`)을 라우팅 프롬프트에 포함하는 것은 후속으로 남긴다. Desktop UI에서 이 Run 필드를 노출하는 UX는 별도 후속 작업이다(이번 변경은 agent-runtime(M05)에만 한정했다). | M05/M10/M11 |
| D-081 | 기존 Knowledge 색인 운영 조치 — 저장소 실 데이터가 D-079 활성화 기준을 충족하지 못함 | **해소 (2026-08-13, 사용자 승인·결정)**: 사용자가 승인한 두 조치를 실행했다. `data/indexes/`(gitignored 로컬 운영 데이터) 5개 색인(`43d83955-…`/`87827bf9-…`/`a038442d-…`/`d9e660b7-…`/`hr-policy-v1`) 전부에 `convert-bm25-format`을 실행 — 전부 `"action": "converted"`, `legacy_pickle_removed: true`를 보고했고, 재실행 시 전부 `"already_converted"`(멱등성 확인). 이제 5개 색인 모두 `bm25.json`을 갖고 `bm25.pkl`은 남아 있지 않다. `87827bf9-…`는 사용자가 결정한 등급으로 **INTERNAL** stamp — 청크 18개 전부 INTERNAL이 됐다. **측정 근거**(별도 포트에서 자체 기동한 search-runtime으로 확인; 사용자가 쓰던 `:8300`(PID 3400)/`:8000`(PID 86099)은 조치 전후 PID 동일 — 손대지 않음): 변환이 검색 결과를 바꾸지 않아야 한다는 조건을 확인했다 — `d9e660b7`, 질의 `장비 지원은 무엇이 있나요?`, clearance INTERNAL: 변환 전후 모두 인용 1건·섹션 `장비 지원`·similarity **0.5265 → 0.5265**(차이 0.0000, 허용 오차 사용 불필요). 분류 차단 해소도 확인했다 — `87827bf9`, 질의 `AX를 위한 LLM 입문 자료 짧은 요약`, clearance INTERNAL: **0건 → 5건**, 최상위 섹션 `AX를 위한 LLM 입문 자료 짧은 요약`, similarity **0.8991**. 조치 전 `data/indexes/` 전체를 세션 scratchpad(임시 디렉터리이며 영구 rollback 수단으로 취급하지 않는다)에 백업했다. | **여전히 남은 것**: Desktop 앱이 이 PC에 이미 설치해 둔 사본(`~/Library/Application Support/desktop-client/assets/knowledge/2f157a86-…/1.0.0/index`)은 여전히 `bm25.pkl`만 있다 — 그 `knowledge_id`(`d9e660b7-…`)가 중앙에 이미 있어 `central_index_exists`로 어차피 거절되므로 지금 변환해도 이득이 없어 의도적으로 범위 밖에 뒀다. 실행 중이던 `:8300`은 여전히 D-079 라우트가 없는 구버전 프로세스라 활성화 요청이 404 나며, 운영자가 재시작해야 해소된다(라이브 서비스 재시작은 이번에 하지 않았다). 새로 만드는 Offline Bundle은 이제 `bm25.json`을 갖고 나가 D-079 등록을 통과할 조건은 갖췄지만, 실제로 Bundle을 재빌드해 설치까지 증명하지는 않았다. | M03/M04/M07/M08 |
| D-084 | Desktop 로컬 Tool — 사용자가 고른 .py 파일 하나를 정적 분석 후 명시적 확인을 거쳐 로컬에서 직접 실행 | **DECIDED / 구현 완료 (2026-08-15)**. **정정(2026-08-15, 최초 구현 직후)**: 최초 구현은 실행 확인을 렌더러(`LocalToolsScreen`)에만 두었고 Main Process의 `localTool:invoke` 핸들러는 아무 승인도 확인하지 않은 채 spawn했다. Electron에서 신뢰 경계는 Main Process이므로, 브릿지에 도달하는 다른 코드 경로가 승인 없이 사용자 권한으로 Python을 실행할 수 있었고 그것이 바로 원칙 7이 금지하는 상태였다. 추가 시점의 위험 고지(`riskAcknowledgedAt`)는 **기억된 승인**이라 이 자리를 대신하지 못한다 — 이후 호출이 무인으로 실행된다. 지금은 spawn 직전 Main Process가 매번 네이티브 대화상자로 파일 경로·함수·인자를 보여주며 다시 묻고, 거부 시 `user_denied`(오류가 아닌 정상 결과)를 반환한다. 회귀 테스트: `local-tool-isolation.test.ts`의 "execution approval is owned by the Main Process" — 거부 분기가 spawn보다 앞선다는 것까지 확인한다. 구현 원칙 7은 "승인되지 않은 임의 Python 실행"을, 원칙 8은 MCP Tool을 READ_ONLY로 한정한다. 이 기능은 MCP Tool이 아니고 agent-runtime/D-080 등록과도 무관하다 — 사용자가 자기 PC의 `.py` 파일 하나를 네이티브 파일 대화상자로 직접 고르고, 그 실행이 격리되지 않는다는 것을 추가 시점에 명시적으로 확인(체크박스, 구조적으로 강제 — `LocalToolStore.add()`가 `acknowledgedRisk: true` 없이는 저장 자체를 거부한다)한 뒤, 이후 매 호출마다 파일 경로와 인자를 보여주는 확인 단계를 다시 거쳐야만 실행된다. 이 두 조건(추가 시점의 위험 인지 + 매 호출의 신선한 명시적 사용자 행동)을 항상 함께 요구하고, LLM이나 자동화가 대신 트리거할 경로가 구조적으로 없다는 점에서 원칙 7의 "승인된" 쪽에 속한다고 판단했다 — "승인되지 않은" 실행이 되려면 이 둘 중 하나가 빠져야 하는데, 둘 다 코드 경로상 우회할 수 없게 만들었다. **구현한 경계**: (1) **구조적 배제** — `local-tool-signature.ts`/`local-tool-store.ts`/`local-tool-runner.ts`는 `mcp-tool-registration-client`/`agentRuntime`/`installed-assets-store`를 참조하지 않고, `src/agentRuntime.ts`·`src/screens/chatTypes.ts`·`src/screens/ChatScreen.tsx`도 로컬 Tool 식별자를 참조하지 않는다 — `electron/__tests__/local-tool-isolation.test.ts`가 소스 텍스트를 직접 읽어 이 부재를 강제하므로, 나중에 누군가 로컬 Tool을 채팅 Payload에 연결하면 이 테스트가 실패한다. 저장소도 `local-tools.json`으로 `InstalledAssetsStore`(`installations.json`)와 완전히 분리했다. (2) **격리 아님을 매번 고지** — 지정된 정확한 한국어 문구("이 Tool은 격리되지 않은 상태로 실행됩니다 — 사용자의 권한으로, 마치 직접 실행한 것과 동일하게 동작합니다. 시간제한과 출력 크기 제한은 멈추지 않는 프로세스를 막기 위한 것일 뿐, 악의적인 코드로부터 보호하지 않습니다.")를 `src/screens/localToolsTypes.ts`의 `NOT_A_SANDBOX_NOTICE`로 한 곳에서 관리하며 (a) Tool 추가 시점의 투명성 패널과 (b) 매 호출 확인 단계 모두에 노출한다(`LocalToolsScreen.tsx`). 타임아웃(`LOCAL_TOOL_TIMEOUT_MS`, 30초)·출력 크기 상한(`LOCAL_TOOL_MAX_OUTPUT_BYTES`, 256KiB)·전용 임시 작업 디렉터리(`fs.mkdtempSync(os.tmpdir())`, 저장소 경로 아님)를 실제로 적용했다(`local-tool-runner.ts`) — 이것들은 멈추지 않는 프로세스를 막을 뿐 보안 경계가 아니라는 점을 문구 자체가 명시한다. (3) **인터프리터는 설정, 추측 아님** — `pythonInterpreterPath`(`DesktopSettingsFile`, 기본 `null`)를 새 필드로 추가했고, `python`/`python3` 같은 PATH 기본값으로 절대 대체하지 않는다 — 값이 비어 있으면 `invokeLocalTool`은 프로세스를 아예 띄우지 않고 `interpreter_not_configured`를 반환한다(`local-tool-runner.ts::invokeLocalTool`). Settings(D10) 일반 탭에 경로 입력 필드를 추가했다(URL이 아니라 파일시스템 경로라 URL 검증은 적용하지 않음, 저장 시점에 존재를 요구하지도 않음 — Task Brief 판단). (4) **사용자가 준 이름으로 경로를 만들지 않는다** — 절대 경로는 항상 `dialog.showOpenDialog`에서만 나오고, `LocalTool.id`는 항상 `crypto.randomUUID()`이며 파일명/함수명에서 파생되지 않는다(`local-tool-store.ts`, 단위 테스트로 확인). Python 인터프리터 하위 프로세스에 전달하는 `module_path`/`function_name`/`args`는 고정된 `BOOTSTRAP_SCRIPT` 상수(사용자 입력을 텍스트로 절대 보간하지 않음)의 stdin JSON으로만 전달해, 파일 경로나 인자 값이 실행되는 코드 자체를 바꿀 수 없게 했다. (5) **Hub 자산과 항상 시각적으로 분리** — Hub 설치 목록(AssetsScreen)과 다른 별도 화면(`LocalToolsScreen.tsx`, 자산 허브 > 로컬 Tool 새 하위 탭)에서만 나타나고, 각 카드에 "로컬 Tool (검토되지 않음)" 배지를 항상 붙인다. **검증**: `electron/local-tool-signature.ts`(순수, 16개 단위 테스트) — Python `ast` 대신 텍스트/괄호·따옴표 인식 스캐너로 top-level `def`/`async def`, 파라미터 타입 표기(`str`/`int`/`float`/`bool`/`Any`/`list`/`dict`/`Optional`/`Union`/`Literal`/PEP604 `X\|Y`), JSON 리터럴 기본값, `*args`/`**kwargs` 거부, 신원 필드(`user`/`role`/`roles`/`org`/`organization_id`) 거부, 소스 크기(20,000자)·파라미터 수(64개) 상한을 `apps/portal-api/src/portal_api/python_signature.py`의 동작·한국어 오류 형태를 그대로 미러링해 구현했다(그 파일은 import하지 않음 — Python이라 이 프로세스에서 애초에 닿지 않는다). `local-tool-store.ts`(6개)·`local-tool-runner.ts`(11개, 가짜 인터프리터 Node 스크립트로 spawn/timeout/출력 상한을 검증하고, PATH에 `python3`가 있으면 실제 `BOOTSTRAP_SCRIPT` 계약도 종단 검증, 없으면 건너뜀)·`local-tool-isolation.test.ts`(8개)·`localToolsTypes.test.ts`(14개) 전부 통과, `pnpm typecheck` 클린, `pnpm test` 599 passed(작업 시작 시점 기준선 544 + 신규 55). | **잔여 위험, 있는 그대로 말한다**: 이것은 진짜로 격리되지 않은 로컬 코드 실행이다 — 악성이거나 버그가 있는 `.py` 파일은 사용자 계정 권한으로 할 수 있는 모든 것을 할 수 있다. 이 문서와 격리 테스트가 증명하는 것은 "자동화된 경로가 이 기능에 닿지 않는다"는 것이지 "실행되는 코드 자체가 안전하다"는 것이 아니다. 타임아웃/출력 상한/임시 작업 디렉터리는 멈추지 않는 프로세스를 막을 뿐 샌드박스가 아니며, 파일시스템·네트워크·환경변수 접근을 전혀 제한하지 않는다. 정적 분석기는 `ast` 없이 손으로 짠 스캐너라 매우 드문 Python 문법(예: 중첩된 삼중따옴표 문자열을 포함한 기본값, 시그니처 자체에 걸린 함수 호출 형태의 데코레이터 인자)에서 잘못 분류할 가능성이 이론적으로 남아 있다 — 다만 이 파서는 실행 여부에 아무 영향이 없다(입력 Schema 표시에만 쓰이고, 실제 실행은 항상 사용자가 확인한 파일 경로 그대로 로드한다). | M04 |
| D-085 | 게시 전 Health Check와 대표 질문 Smoke Test (10-hosted-chatbot-publication.md §8) | **미결정 (2026-08-16 기록)**: §8은 "게시 완료 전 Health Check와 대표 질문 Smoke Test를 실행한다"고 요구하지만 이를 실행할 계약이 아직 없다. Portal API는 모델을 직접 호출하지 않으므로(루트 CLAUDE.md UI 구현 규칙) M02가 스스로 대표 질문을 물어볼 수 없고, Hosted Agent Runtime(M05)에 "이 Deployment Revision으로 이 질문에 답할 수 있는가"를 묻는 엔드포인트가 필요하다. 결정해야 할 것: (1) 대표 질문을 어디에 저장하는가 — Service Definition의 `suggested_questions`를 재사용할지, 별도 Smoke Suite를 두고 §11.2의 `test_suite_run_id`를 채울지. (2) 실패했을 때 게시를 **차단**할지 경고만 할지 — 차단이면 Ollama 미기동만으로 게시가 막히고, 경고면 §8의 "실행한다"가 사실상 장식이 된다. (3) Update(`POST /deployments/{id}/revisions`)에도 같은 Gate를 적용할지. 이번 Update/RETIRED 구현에서는 **추측해서 만들지 않았다** — 아무것도 물어보지 않고 "정상"이라고 보고하는 Health Check는 없는 것보다 나쁘다. | 게시 경로는 지금도 `_run_validation`(Knowledge 승인·인덱싱 상태 등 정적 Gate)은 통과해야 하므로 완전히 무검증은 아니다. Smoke Test 부재는 "모델이 실제로 답할 수 있는가"를 게시 시점에 확인하지 못한다는 뜻이며, 현재는 위저드의 Preview 1회 완료 Gate(M01)가 사람 손으로 그 역할을 대신하고 있다. | M02/M05 |

## 결정 절차

1. 결정 소유자를 지정한다.
2. 선택지, 보안·운영·비용 영향을 기록한다.
3. 관련 Contract 변경을 먼저 작성한다.
4. 영향 모듈 검토 후 결정 상태를 `DECIDED`로 변경한다.
5. 명세와 Sample Fixture를 수정한다.
6. 구현 PR을 진행한다.
