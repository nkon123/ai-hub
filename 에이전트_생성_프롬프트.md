# 로컬 LLM 에이전트 툴 생성 프롬프트

기존 챗봇(`impact_chat`)에 새 기능을 붙일 때 쓰는 프롬프트 템플릿입니다.
아래 블록을 복사해 **`[여기를 채우세요]` 부분만 작성**한 뒤 Claude에게 넘기면,
LangGraph 흐름 + `@tool` 래핑 + 등록까지 된 단일 파일이 나옵니다.

---

## 사용법

1. 아래 `프롬프트 본문`을 통째로 복사
2. `1. 만들 것` 섹션의 빈칸을 채움 (나머지는 그대로 두기)
3. Claude에게 전달
4. 받은 파일을 `tools/` 에 넣으면 자동 등록됨 — `app.py` 수정 불필요

---

## 프롬프트 본문

````markdown
로컬 LLM 환경에서 동작하는 LangGraph 에이전트를 만들어주세요.
기존 챗봇에 툴로 붙일 수 있는 **단일 파일**로 작성해주세요.

---

## 1. 만들 것

**주제**
[여기를 채우세요 — 예: "인터페이스 오류 알림을 받아 일자별 건수 추이를 조회하고 담당자 메일 초안을 만든다"]

**입력**
[여기를 채우세요 — 툴이 받을 인자. 예: "알림 텍스트 (사내 모니터링 시스템이 보낸 원문)"]

**출력**
[여기를 채우세요 — 예: "건수 추이 표 + 담당자 + 메일 초안. markdown"]

**데이터 소스**
[여기를 채우세요 — 예: "Oracle. TB_IF_LOG (IF_KEY, PROC_DT VARCHAR2 'YYYYMMDD', STATUS, CNT)"]
[없으면 "없음 (텍스트 처리만)" 이라고 쓰세요]

**참고 정보**
[여기를 채우세요 — 샘플 데이터, 실제 텍스트 예시, 매핑 파일 위치 등. 없으면 "없음"]

---

## 2. 실행 환경

- Python 3.10+, Windows 사내망 (폐쇄망, 외부 API 불가)
- LLM: Ollama + `gemma4:e2b` (VRAM 6GB 노트북). 소형 모델 기준으로 설계할 것
- LangGraph, LangChain 1.x (`langchain.agents.create_agent`)
- 기존 챗봇 구조에 붙임:
  ```
  impact_chat/
    app.py            # 수정하지 않음
    config.py         # 공통 설정 (모든 설정은 여기서 import)
    tools/
      __init__.py     # register() 레지스트리
      새파일.py        # ← 이번에 만들 것
  ```

---

## 3. 설계 원칙 (반드시 지킬 것)

### 3-1. LLM은 마지막에만 쓴다

결정적으로 처리할 수 있는 건 전부 코드로 하고, LLM은 규칙이 못 푼 것만 담당합니다.
파싱·조회·집계·분기는 코드로, 애매한 판정과 자연어 문장 생성만 LLM으로.

판정 단계는 이 순서로 필터링하세요:
```
확정 가능한 정적 조건 → 규칙(정규식/패턴) → 남은 것만 LLM
```

### 3-2. LLM 호출 시 지킬 것

```python
ChatOllama(
    base_url=OLLAMA_HOST, model=JUDGE_MODEL, temperature=0,
    num_ctx=8192,       # 기본값 2048 은 프롬프트를 조용히 자른다. 필수
    keep_alive="10m",   # 루프 도는 동안 모델 언로드 방지
)

# tool calling 대신 스키마 강제 디코딩. 소형 모델은 tool calling 이 자주 깨진다
llm.with_structured_output(SomeModel, method="json_schema")
```

- **한 프롬프트에 질문 하나.** 필드가 여러 개면 소형 모델이 모순된 답을 냅니다.
- **계산할 수 있으면 묻지 마세요.** 다른 필드에서 파생 가능하면 코드로 구하세요.
- 판정 대상이 긴 텍스트 안에 있으면 `>>>` 마커로 대상 줄을 표시하세요.
- 프롬프트는 짧게. 규칙이 5개를 넘으면 뒤쪽을 무시합니다.

### 3-3. 판정 근거를 남긴다

모든 판정에 **어느 규칙이 무엇을 보고 그렇게 정했는지**를 함께 저장하세요.
결과만 있으면 오판 원인을 못 찾습니다.

```python
{"result": ..., "decided_by": "rule|llm|static|fallback",
 "rule": "규칙명", "evidence": "판단 근거가 된 실제 조각"}
```

콘솔에도 규칙별 집계와 상세를 출력하세요.

### 3-4. 실패는 보수적으로

LLM 호출 실패나 판정 불가 시, 조용히 넘기지 말고 "확인 필요"로 표시하세요.
**누락은 오탐보다 나쁩니다.**

### 3-5. 용어를 명확히

"모른다"(판단 불가)와 "아니다"(해당 없음)를 같은 이름으로 쓰지 마세요.
`UNCERTAIN` / `EXCLUDED` 처럼 분리하고, 한글 표기를 함께 정의하세요.

---

## 4. 코드 구조

### 4-1. LangGraph 흐름

```python
class State(TypedDict):
    # 입력 + 중간 산출물 + 최종 결과
    ...

def node_1(state: State) -> dict: ...
def node_2(state: State) -> dict: ...

def build_graph():
    g = StateGraph(State)
    g.add_node("node_1", node_1)
    ...
    g.add_edge(START, "node_1")
    ...
    return g.compile()
```

노드는 단계별로 나누되 과도하게 쪼개지 마세요. 각 노드는 콘솔에 진행 로그를 찍습니다:
```
[1] 단계 설명: 결과 요약
[2] 단계 설명: 결과 요약
```

### 4-2. 실행 함수 — dict 반환

```python
def run_xxx(입력..., detail: str = "full") -> dict:
    """detail: full / summary / minimal — 컨텍스트 절약용"""
    result = build_graph().invoke({...})
    return {
        "summary": {...},      # 건수 등 요약
        "items": [...],        # 상세 항목
        "report": "...",       # markdown 리포트
    }
```

### 4-3. 툴 래핑 + 등록

**LLM에게는 툴 하나만 노출**하세요. 소형 모델은 여러 툴을 순서대로 부르지 못합니다.
내부 단계는 일반 함수로 분리하되 `@tool` 을 붙이지 마세요 (나중에 필요하면 붙임).

```python
# 내부 함수 — 재사용 가능하지만 에이전트에 노출하지 않음
def _step_a(...): ...
def _step_b(...): ...

@tool
def my_tool(arg: str) -> str:
    """LLM 이 이 첫 문장을 보고 호출을 판단한다. 다른 툴과 겹치지 않게 쓸 것.

    Args:
        arg: 설명

    Returns:
        markdown 리포트
    """
    return run_xxx(arg, detail="summary")["report"]

register(my_tool,
         label="화면에 표시할 이름",
         view="text",                  # 또는 커스텀 렌더러 키
         detail_endpoint="",           # 전체 결과를 따로 받을 경로 (선택)
         hint="시스템 프롬프트에 붙일 주의사항")
```

### 4-4. 설정은 config.py 에서 import

```python
from config import OLLAMA_HOST, JUDGE_MODEL, USE_LLM
```

파일 안에서 `os.getenv` 를 다시 부르지 마세요. 설정이 이원화되면
한쪽만 고쳤을 때 서로 다른 값을 보게 됩니다.

새 설정이 필요하면 `config.py` 에 추가할 항목을 별도로 알려주세요.

### 4-5. 캐시

같은 입력이 반복되거나 무거운 조회가 있으면 캐시하세요.
**무엇이 비싼지 파악해 캐시 키를 정하세요.** 예를 들어 파일 스캔이 무겁고
특정 인자와 무관하다면, 그 인자를 뺀 키로 따로 캐시합니다.

캐시를 비우는 방법도 함께 제공하세요 (함수 또는 별도 툴).

### 4-6. CLI 단독 실행

챗봇 없이 테스트할 수 있게 `if __name__ == "__main__"` 블록을 넣으세요.

```bash
python 파일명.py --인자 값 --no-llm     # LLM 없이 규칙만 (Ollama 불필요)
```

---

## 5. DB 접근 (해당하는 경우)

- `oracledb` 사용, 연결 정보는 `config.py` 에서 가져옴
- 조회는 반드시 **바인드 변수** 사용 (문자열 결합 금지)
- 연결은 `with` 로 확실히 닫기
- 조회 실패 시 예외를 삼키지 말고 사용자에게 원인을 전달
- **DML(INSERT/UPDATE/DELETE)은 넣지 마세요.** 조회 전용으로 만들고,
  변경이 필요하면 사람이 확인 후 실행하도록 SQL을 출력만 하세요

---

## 6. 안전장치

- **외부로 나가는 것(메일 발송, 메시지 전송, 파일 삭제 등)은 자동 실행 금지.**
  초안만 만들고 사람이 확인 후 실행하도록 하세요.
- 사람·조직 매핑 같은 사실 데이터는 LLM에게 추측시키지 말고 조회하세요.
- 개인정보나 인증정보가 로그·리포트에 찍히지 않게 하세요.

---

## 7. 작업 순서

**코드를 바로 쓰지 마세요.** 먼저 아래를 정리해 확인받은 뒤 진행해주세요.

1. LangGraph 노드 구성 (각 노드가 무엇을 하는지)
2. 어느 단계를 코드로 하고 어느 단계에 LLM을 쓸지, 그 이유
3. State 스키마
4. 애매하거나 정보가 부족한 부분 질문

합의 후 코드를 작성하고, 가능하면 샘플 데이터로 동작을 검증해 결과를 보여주세요.

---

## 8. 출력 형식

- 단일 `.py` 파일
- 주석은 **왜 그렇게 했는지**를 적으세요. 무엇을 하는지는 코드가 말합니다.
  특히 함정을 피하려고 넣은 코드에는 반드시 이유를 남기세요.
- 파일 상단 docstring에 목적·실행법·설계 메모
- 한국어 주석
````

---

## 부록: 자주 밟는 함정

프롬프트에 다 넣지는 않았지만, 결과물을 검토할 때 확인하면 좋은 것들입니다.

| 함정 | 증상 | 대응 |
|---|---|---|
| `num_ctx` 기본값 2048 | 프롬프트가 조용히 잘림. 에러 없음 | 8192 이상으로 명시 |
| `\b` 와 언더스코어 | `IF_A` 에서 `A` 가 매치 안 됨 | `(?<![A-Za-z0-9_])` 로 경계 직접 정의 |
| Windows 경로 이스케이프 | `\t`, `\n` 이 해석돼 경로 깨짐 | `r"..."` 사용 |
| 소형 모델 tool calling | 툴을 안 부르거나 인자를 지어냄 | `json_schema` 강제 디코딩, 툴 최소화 |
| 툴 여러 개 노출 | 중간에 멈추거나 엉뚱한 툴 호출 | 하나의 툴이 전체 흐름을 완결 |
| Flask 로그 순서 | 액세스 로그가 실행 로그보다 뒤에 찍힘 | 요청 완료 후 기록되는 정상 동작 |
| 체크포인터 이력 누적 | 이전 턴 툴 호출이 계속 딸려옴 | 마지막 사용자 메시지 이후만 사용 |
